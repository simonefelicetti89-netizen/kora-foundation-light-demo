import { DemoGuideContent } from '@/components/demo/DemoGuideContent';

// Persistent demo guide — accessible from sidebar at all times, for all roles.
export default function DemoGuidePage() {
  return <DemoGuideContent />;
}
