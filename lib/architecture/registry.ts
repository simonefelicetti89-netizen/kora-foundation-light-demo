// lib/architecture/registry.ts
// CC-003 / B-REG — KORA Architecture Registry (typed, single source of truth).
//
// GOVERNANCE (read before editing):
//   - MASTER PLAN (docs/KORA_OFFICIAL_IMPLEMENTATION_MASTER_PLAN_v2.1.md) = architectural truth.
//   - This file = REPOSITORY = operational truth, describing the CODE as it exists today.
//   - The Architecture Registry (this file, section A) describes CODE COMPONENTS.
//   - The Target Ontology Map (this file, section B) describes DOMAIN OBJECTS the
//     Master Plan's target ontology defines. These are NOT the same thing and must
//     never be conflated — a domain object with no code yet has no registry entry
//     in section A; it only appears in section B as TO_BUILD.
//
// This file DESCRIBES. It does not consolidate, refactor, or delete anything it
// registers. Classifications here are inherited from CC-001 + CC-001R (verified,
// not re-audited from scratch) and do not anticipate any decision reserved for a
// future CC/D-letter (D-A Confidence, D-B Decision Pack, D-C One Truth, D-D worker
// surfaces). Where CC-001R found two competing implementations, both are listed
// with matching `competingWith` and the SAME `decisionRef` — neither is elevated.
//
// D-K (ratify Master Plan v2.1 §3 domain ontology, closing B-SPEC) is recorded
// in docs/21-founder-gate-resolution-log.md §10, not here — that placement was
// explicit founder direction, distinct from the D-A/D-B/D-C precedent above.
//
// docs/ARCHITECTURE_REGISTRY.md is GENERATED from this file by
// scripts/generate-architecture-doc.ts. Never hand-edit that Markdown file.
//
// ── CC-010 CLOSEOUT / D-C RESOLUTION (2026-08-31) ───────────────────────────────
//
// CC-010 — "Audit One Truth esteso" (Master Plan §30, line 863; A-audit type,
// block B-TRUTH, prereq CC-003, gate "Sì → D-C") — STATUS: COMPLETE (audit
// only; remediation is separate, see below).
//
// The audit's required deliverable — a verified inventory of what violates
// One Product / One Truth, surfacing any decision points — already exists:
// Master Plan §13 "ONE PRODUCT / ONE TRUTH" lists the items to eliminate or
// consolidate, marked [tutti VERIFIED], and names exactly one open decision
// point (the 11 synth-only /demo pages — see app-surface.demo below, D-C).
// That inventory has since been substantively extended by real, repo-verified
// B-TRUTH audit work: the Tenant Identity read-path audit (svc.tenant), the
// Demo/Orphan Chain Audit (svc.company-intelligence, svc.company-onboarding,
// svc.report-factory, svc.report-generator), Root Control Room Waves 1–3 +
// Hardening (svc.lifecycle retired, svc.tenant/svc.company-data-intake/etc.
// caller graphs corrected), the synthetic BTI orphan-chain retirement
// (svc.budget-to-human-impact deleted), and the Ingestion/UEF DEMO_RUNTIME
// classification (svc.ingestion-pipeline, svc.uef-review corrected to
// FROZEN). This closes CC-010's own scope: producing and maintaining a
// verified One-Truth inventory. It does NOT mean B-TRUTH remediation itself
// is complete — the §13 list's remediation items (ScoringSimulatorService,
// DemoDataService, DemoScoringAdapter, AccessControlService, CommonsService's
// dual discovery path, KoraContributionService's 2 remaining seeds, the I9
// allowlist at 24/28) remain open. That remediation is CC-018–023's job, not
// CC-010's.
//
// D-C — Master Plan §13's open decision: disposition of the 11 synth-only
// /demo pages (role DEMO_VIEWER) under One Truth — RESOLVED by founder
// decision (2026-08-31). See app-surface.demo below for the resolution text
// and its governing invariant.
//
// CC-018–023 sequence (Master Plan §29 day-by-day schedule reconciled against
// §30's coarse "018-023: One Truth per gruppo di seed" index row, using the
// same main+adversarial composite-label convention §30 already applies to
// 011-012, 013-014, and 041-042): CC-018 = seed-group migration #1 (COMPLETE
// — see svc.worker-pillar-adoption), CC-019 = seed-group migration #2
// (COMPLETE via CC-019A/B, TenantService/legacy [companyId] tenant-identity
// cluster — see svc.tenant), CC-020 = seed-group migration #3 (IN_PROGRESS
// via CC-020A, Company Intelligence legacy chain — see svc.company-intelligence
// / svc.company-onboarding), CC-021 = seed-group migration #4 (not started),
// CC-022 = B-TRUTH closure gate (demo=live, I9=0 — not started), CC-023 =
// adversarial validation + remedies (not started). CC-022/023 are NOT
// additional seed-group migrations.
//
// CC-020A / CC-020 — Company Intelligence Legacy Chain Retirement
// (2026-08-31): founder decision — CompanyIntelligenceService has no future
// canonical role in its current form (zero live/runtime reachable consumers,
// aggregates 6 synthetic/legacy services under an undefined heuristic, no
// canonical replacement, not required by any Gen3 canonical surface).
// Retired outright (RETIRE, not merely left orphaned). As a proven direct
// consequence, CompanyOnboardingService (its only real caller repo-wide)
// became a pure second-order orphan and was retired in the same slice,
// along with its unique seed file (zero other consumers). The other 4
// dependencies CompanyIntelligenceService called internally — TenantService,
// CompanyDataIntakeService, WorkerProvisioningService, ScoringSimulatorService
// — all retain other confirmed callers and were explicitly left untouched;
// this slice removes the orphan aggregator, not the legacy graph beneath it.
// I9: 24->23 files, 36->35 imports (CompanyOnboardingService.ts's sole
// synthetic import; CompanyIntelligenceService.ts itself had none — it was
// never an allowlist entry). CC-020 is not marked fully COMPLETE: this is a
// single, self-contained target with no evidenced CC-020B requirement.
//
// CC-019 was authorized as a controlled multi-step decomposition (this
// registry's own prior audit already found the full TenantService caller
// graph unsafe to migrate as one chain — see svc.tenant notes), using
// sub-slice labels CC-019A/B/C. These are implementation sub-slices of the
// single Master Plan item CC-019, not new Master Plan CC numbers.
//
// CC-019A — Legacy Company Users Surface Retirement (2026-08-31): COMPLETE.
// Retired app/admin/companies/[companyId]/users/page.tsx (100% synthetic,
// read-only demo account display — TenantService + AccountProvisioningService)
// in favor of the already-existing, already-live app/admin/company-users-live
// (Supabase-backed /api/admin/company-users — a strict functional superset:
// real read plus real invite/status mutation the legacy page never had).
// Not relinked in CompanyTabNav: company-users-live is keyed by tenantId
// (analytics.tenant.id, a UUID), while the tab nav only carries tenant_code
// (companyId) — bridging the two would require inventing a new resolver, out
// of scope for a tab cleanup, so the Users tab was removed instead of
// rebuilt. TenantService and AccountProvisioningService implementations were
// not touched; both keep their other callers. I9 unchanged (24/36) — neither
// service reaches zero callers from this slice alone. CC-019B (layout.tsx's
// own tenantService.getTenant() call, shared chrome over the Gen3 canonical
// children) remains open, deferred, not touched by this slice.
//
// CC-019B — Gen3 Shared Layout Tenant Identity Canonicalization (2026-08-31):
// COMPLETE. app/admin/companies/[companyId]/layout.tsx's company_name
// resolution moved from TenantService.getTenant(companyId)?.company_name
// (synthetic, data/synthetic/tenants.json) to analytics.tenant.company_name
// queried by tenant_code = companyId — the exact same canonical source,
// identifier, and inline query shape every Gen3 child
// (workspace/preview/evidence/submissions) already uses independently. This
// closes a real mixed-identity bug: canonical, real, tenant-scoped body
// content was rendering under a header sourced from synthetic data. Not-found
// ownership is unchanged — each child page still owns its own notFound()
// call; the layout only needed a safe display fallback, so an unmatched
// tenant_code still displays the raw companyId (unchanged behavior, just a
// canonical source instead of a synthetic one — never TenantService, never
// synthetic company_name). No new resolver/service abstraction introduced —
// the existing inline per-route pattern was reused verbatim. TenantService
// import removed from layout.tsx entirely; TenantService.ts implementation
// itself untouched, still serving its other 5 callers (admin/pipeline,
// workforce-baseline, WorkforceQuickAccessPanel — all DEMO_RUNTIME —
// ReportFactoryService, CompanyIntelligenceService). I9 unchanged (24/36) —
// TenantService.ts does not reach zero callers from this slice. Invariant:
// Gen3 shared chrome and Gen3 body now use the same canonical tenant
// identity truth. CC-019 is NOT fully complete after A+B: ReportFactoryService
// and CompanyIntelligenceService remain separately-classified TenantService
// consumers (their own pending decisions, out of CC-019's scope), and the 3
// DEMO_RUNTIME consumers are intentionally untouched.
//
// ── CC-005 CLOSEOUT / D-B RESOLUTION (2026-09-02) ───────────────────────────────
//
// CC-005 — "Audit Decision Pack" (Master Plan §28 line 802/858, prereq
// CC-029/N2 + CC-053, gate "Sì → D-B") had already established, in code and
// tests (svc.report-generator / svc.report-factory / lib.decision-pack
// notes above, cc013-canonical-contract.test.ts), that lib/decision-pack/*
// + lib/live/decision-pack.ts is the sole canonical Decision Pack authority
// and that ReportGeneratorService/ReportFactoryService are NOT canonical.
// What remained open — per Master Plan §32's own line, "I perdenti fra i
// tre Decision Pack | dopo D-B | 7 capability migrate o ritirate
// esplicitamente" — was the capability-by-capability disposition of
// ReportGeneratorService's 7 IReportGeneratorService methods. This PR
// (feature/btruth-decision-pack-ratification) closes that gap and formally
// ratifies D-B. It does NOT retire ReportGeneratorService or
// ReportFactoryService — see their own entries below for continued status.
//
// D-B RESOLUTION: CANONICAL_DECISION_PACK_AUTHORITY = lib/decision-pack/
// pdf-data.ts + html-template.ts + pdf-runtime.ts + pdf-strategy.ts +
// lib/live/decision-pack.ts (persistence). Reads only canonical persisted
// analytics.kora_index_result / decision_pack_version / bti_result /
// uef_record rows; never recomputes protected methodology; tenant-scoped
// via authenticated session (never a query/body param); zero data/synthetic
// import at any of these 5 files (proven structurally by
// tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts's
// rewritten "canonical Decision Pack authority has zero dependency on
// legacy report chain" block).
//
// REPORTGENERATOR — 7-CAPABILITY DISPOSITION (audited capability-by-
// capability against current canonical coverage, not assumed):
//   RETIRE (superseded, nothing to port): generate() [legacy stub, already
//     redirects to its own successor], getCurrentCompanyDecisionPack()
//     [pure alias], getDecisionPackVersionHistory() [hardcoded 2-entry
//     fixture, not derived from any real store], getDecisionPackExportActions()
//     [PDF entry superseded by lib/decision-pack's real, working PDF path;
//     Excel/Share/Board-Pack-Draft entries are disabled stubs with zero
//     logic behind them anywhere in the repo, not migration debt].
//   RETIRE AS ASSEMBLY: generateCompanyDecisionPack() — 9 of its 14
//     synthetic sections are already superseded at real DB-backed scale by
//     lib/decision-pack/html-template.ts (KORA Index v3, macroblocks,
//     pillar analysis, eligibility gate, economic relief, recommendations,
//     evidence-gap readiness B18/B19, methodology boundaries, BTI — the
//     last already dead in ReportGeneratorService itself, empty array).
//     Its 3 uncovered sections do NOT automatically migrate: Dynamic
//     Scoring Preview must NOT migrate (DynamicScoringPreviewService's own
//     header: "NOT a full scoring run... do NOT use as a replacement" —
//     migrating it would put an explicitly-non-authoritative preview into
//     final canonical output); People Context / HR KPI is DEFERRED as an
//     adjacent interpretation layer per CLAUDE.md §12.18 (no correlation-as-
//     causality claim, not urgent, not canonical Decision Pack debt); 90-day
//     action plan is a DEFERRED PRODUCT QUESTION (no Master Plan requirement
//     found for it), not migration debt.
//   MIGRATE: getDecisionPackLimitations() — investigated and found ALREADY
//     migrated: every mandatory item (pre_empirical_calibration / not-
//     certified / not-regulatory-grade disclosure, Delphi Study / v0.1
//     provisional-weights disclosure, the exact CLAUDE.md §12.17 CSR/ESG
//     disclaimer, "correlazione ≠ causalità", aggregate-only / no-
//     individual-worker-data disclosure) is already present, verbatim or
//     equivalent, in lib/decision-pack/html-template.ts — built
//     incrementally by B18/B24/B79-B/CC-015 work, distributed contextually
//     next to the section it governs rather than centralized. Proven, not
//     just asserted, by tests/unit/cc005-decision-pack-limitations-
//     migration.test.ts, which also proves the demo-specific items from the
//     same legacy list (synthetic-seed references, "applicazione demo"
//     wording, hardcoded S1 confidence commentary, Dynamic Preview
//     batch-size commentary) are correctly ABSENT from canonical output,
//     live or demo. No new rendering seam was added — adding one would have
//     duplicated disclaimer logic the canonical renderer already owns.
//   DEFERRED PRODUCT REQUIREMENT (not ported, not discarded):
//     getDecisionPackReadiness() — the one capability with no canonical
//     equivalent and genuine product value: automatic draft/review/ready
//     status derivation from data-quality signals (today: UEF pending-ratio
//     + confidence score, both synthetic inputs). lib/live/decision-pack.ts's
//     persistDecisionPack() lifecycle is manual-only today (operator
//     promotes draft→ready→exported via app/api/admin/decision-pack/status/
//     route.ts) — this is a documented, explicit gap, not a silent one.
//     REQUIREMENT (preserve verbatim for any future implementer): "future
//     automated Decision Pack readiness/status derived from canonical live
//     evidence/data-quality signals — NOT ported from ReportGeneratorService's
//     synthetic-input heuristic; must be re-derived from live
//     analytics.uef_record / kora_index_result once the Ingestion/UEF
//     legacy chain (svc.ingestion-pipeline / svc.uef-review) is itself
//     resolved, since porting the old algorithm as-is would import a
//     synthetic dependency into the canonical path." No runtime code, no
//     schema, no UI was added for this in the current PR — it is recorded
//     here, in this durable governance surface, specifically so it is not
//     silently lost when ReportGeneratorService is eventually deleted.
//
// NEXT STEP (not this PR): ReportGeneratorService retirement (delete the 6
// RETIRE-classified methods + the assembly method), ReportFactoryService
// retirement, and the downstream orphaned Ingestion/UEF legacy cluster
// (IngestionPipelineService, EligibilityGateService, UEFReviewService,
// DynamicScoringPreviewService, PreviewScoringAdapter,
// ExplainabilityService's synthetic branch, FinancialGovernanceService) —
// each its own separately-authorized, bounded slice per B-TRUTH's "One PR
// = one bounded closure step" discipline. Final scoring group
// (ScoringSimulatorService, DemoScoringAdapter, DemoDataService,
// AccessControlService) remains untouched and out of scope until true
// end of B-TRUTH (Master Plan §32).

// ── A. ARCHITECTURE REGISTRY — code components ──────────────────────────────────

export type ArchitectureStatus =
  | 'CANONICAL'
  | 'CONSOLIDATE'
  | 'COMPLETE'
  | 'FROZEN'
  | 'FUTURE_CORE'
  | 'LEGACY'
  | 'DEAD'
  | 'INVESTIGATE';

export interface ArchitectureComponent {
  /** Unique, stable identifier. Convention: "<kind>.<name>", e.g. "svc.tenant". */
  id: string;
  /** Domain grouping, e.g. "Worker", "Methodology", "Reporting". */
  domain: string;
  /** Primary code path this entry describes. Required for every code-level entry. */
  primaryPath: string;
  /** One-line purpose. */
  purpose: string;
  status: ArchitectureStatus;
  /** True if the Master Plan (§33 Do-Not-Delete / Future Core) explicitly preserves this. */
  futureCore: boolean;
  /** Other registry ids this component depends on (best-effort, not exhaustive). */
  dependencies: string[];
  /** Other registry ids this component competes with (same capability, different implementation). */
  competingWith: string[];
  /** Future CC/D-letter that owns the resolution decision, if any (e.g. "CC-004 / D-A"). Null if none reserved. */
  decisionRef: string | null;
  /** Free-form context: verification method, caller counts, caveats. */
  notes: string;
  /** Required (non-null) when status === 'DEAD': the condition under which deletion becomes safe. */
  deletableWhen: string | null;
}

// ── Validation ───────────────────────────────────────────────────────────────
// Pure functions — used by tests/unit/cc003-i10-registry-completeness.test.ts.
// Not run automatically at import time: a bad entry should fail a test, not
// crash every page that happens to import this module.

export interface RegistryViolation {
  id: string;
  rule: string;
  detail: string;
}

export function validateArchitectureRegistry(components: ArchitectureComponent[]): RegistryViolation[] {
  const violations: RegistryViolation[] = [];
  const seenIds = new Set<string>();

  for (const c of components) {
    if (seenIds.has(c.id)) {
      violations.push({ id: c.id, rule: 'UNIQUE_ID', detail: `Duplicate id "${c.id}".` });
    }
    seenIds.add(c.id);

    if (!c.primaryPath || c.primaryPath.trim().length === 0) {
      violations.push({ id: c.id, rule: 'PATH_REQUIRED', detail: 'Code-level entry must have a non-empty primaryPath.' });
    }

    if (c.status === 'DEAD') {
      if (!c.deletableWhen || c.deletableWhen.trim().length === 0) {
        violations.push({ id: c.id, rule: 'DEAD_REQUIRES_DELETABLE_WHEN', detail: 'status=DEAD requires a non-empty deletableWhen.' });
      }
      if (!c.decisionRef || c.decisionRef.trim().length === 0) {
        violations.push({ id: c.id, rule: 'DEAD_REQUIRES_DECISION_REF', detail: 'status=DEAD requires a non-empty decisionRef.' });
      }
      if (c.futureCore) {
        violations.push({ id: c.id, rule: 'FUTURE_CORE_NOT_DEAD', detail: 'futureCore=true cannot be paired with status=DEAD.' });
      }
    }

    if (c.futureCore && c.status === 'DEAD') {
      violations.push({ id: c.id, rule: 'FUTURE_CORE_NOT_DEAD', detail: 'futureCore=true cannot be paired with status=DEAD.' });
    }
  }

  return violations;
}

// ── Registry data ──────────────────────────────────────────────────────────────
// Granularity per Master Plan §8: domain-level components, not every helper.
// Purpose/caller evidence inherited from CC-001R (services/*: 55 directories,
// each individually caller-verified this session and in CC-001R).

export const ARCHITECTURE_REGISTRY: ArchitectureComponent[] = [
  // ── Services (55 services/* directories + 1 sub-entry) ────────────────────────
  { id: 'svc.access-control', domain: 'Demo/Access', primaryPath: 'services/access-control/AccessControlService.ts', purpose: 'Demo user registry, session-only, no real auth.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH', notes: 'Zero callers repo-wide (verified). Master Plan §32 names it for removal at end of B-TRUTH alongside demo-data/scoring-simulator.', deletableWhen: null },
  { id: 'svc.account', domain: 'Admin', primaryPath: 'services/account/AccountProvisioningService.ts', purpose: 'NARROWED — My KORA/session-identity demo-persona resolution only (getCurrentDemoUser()). Its former pipeline/admin provisioning role is canonical (see lib.account-provisioning-status-view). CC-00 Worker/Account canonicalization audit (2026-09-05) re-confirmed: DEFER_PRODUCT_DECISION — getCurrentDemoUser() picks which synthetic persona My KORA (a doctrine-declared PREVIEW/synthetic surface with no live session mode) renders as; canonicalizing it means deciding My KORA live-session behavior, reserved for B-WORKER.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH AccountProvisioningService Pipeline Role Migration (2026-09-06)', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): "13 real callers." — this count was stale by the time this migration re-verified it: independent repo-wide re-verification found exactly 2 real callers (getAccountsForCompany() from app/admin/pipeline/_components/PilotLifecycleClient.tsx, getCurrentDemoUser() from app/my-kora/page.tsx), plus 19 other zero-caller methods (createCompanyUserDraft/createCompanyAdminDraft, inviteCompanyUser/revokeInvite/resetInvite, disableUser/suspendUser/deleteDemoUser, getUserAccessProfile, canAccessCompany/canAccessAdmin, getVisibleSections, getAccountStatusBadge/getInvitationStatusBadge, getAccountsForTenant, getKoraStaffAccounts, getWorkerAccountsForCompany, getCompanyAdmins, getPrimaryCompanyAdmin) not removed by this migration — no opportunistic cleanup, out of this migration\'s narrow pipeline-role scope. NARROWING (2026-09-06): the 2 real callers were traced into exactly two distinct responsibilities — PIPELINE_ADMIN_PROVISIONING (getAccountsForCompany, whose caller only ever read accounts.length > 0, fed into the Pilot Lifecycle\'s hasCompanyUser flag) and MY_KORA_SESSION_IDENTITY (getCurrentDemoUser, resolving which synthetic persona/company_id the My KORA preview renders as). Only the pipeline role was migrated: a new pure view builder, lib/live/account-provisioning-status-view.ts, reads Supabase Auth (auth.users + app_metadata.kora_tenant_id/kora_role) via the same filter app/api/admin/company-users/route.ts\'s own GET handler already uses (COMPANY_ROLES = [\'COMPANY_ADMIN\']) — not a new auth system, a read of the existing canonical one. getAccountsForCompany() itself was deleted (pipeline-only, zero remaining callers). getCurrentDemoUser() and its data/synthetic/user-accounts.json dependency are UNTOUCHED — that fixture is still required by the surviving method and is NOT removed from the I9 allowlist (12/20 unchanged by this PR). No tenant_kind branch, no KoraTest special case — the canonical read is identical for any tenant. AdminPreviewService, final scoring, and B-WORKER (WorkerProvisioningService, WorkerAchievementService, WorkerSpaceCapabilityService) are explicitly untouched. See tests/unit/b-truth-accountprovisioning-pipeline-role-migration.test.ts for the regression guard proving both the migration and the scope boundary. CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05, worker/account cluster governance slice): re-examined getCurrentDemoUser() against the frozen resolution "worker/account data truth canonicalized in CC-00; worker UX decision deferred to B-WORKER". Finding: DEFER_PRODUCT_DECISION, not CANONICALIZE and not RETIRE. My KORA has no live session mode today (middleware.ts\'s own comment: "/my-kora is intentionally absent... uses demo-state role switching, not a live Supabase JWT") — getCurrentDemoUser() IS that demo-state switching mechanism, not a stand-in for a missing real one. Canonicalizing it would require inventing My KORA\'s live-session identity model — explicitly B-WORKER\'s decision, not a data-truth substitution this slice is authorized to make. data/synthetic/user-accounts.json remains required, unchanged (8/13 I9 unaffected). No code was modified. See tests/unit/cc00-worker-account-canonicalization.test.ts for the regression guard recording this finding.', deletableWhen: null },
  { id: 'svc.activation-opportunity', domain: 'Worker', primaryPath: 'services/activation-opportunity/ActivationOpportunityService.ts', purpose: 'Rule-based activation opportunity engine, no LLM.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: ['svc.worker-opportunity'], decisionRef: null, notes: '3 callers. Overlap with worker-opportunity not resolved.', deletableWhen: null },
  { id: 'svc.activation-safeguard', domain: 'Methodology', primaryPath: 'services/activation-safeguard/ActivationSafeguardService.ts', purpose: 'CLEAR/WARNING/FLAGGED thresholds per D-21. evaluate() is real and canonical, already used by every live company page; evaluateFromSeed() is the sole synthetic-touching method, called only by the out-of-scope ScoringSimulatorService.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '6 callers. Matches CLAUDE.md thresholds exactly. CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05): confirmed KEEP_INTERNAL_CANONICAL, no code change. evaluate(ar, mar) is a pure, methodology-driven function (lib/methodology-config/v0.1\'s getThresholds()) with zero synthetic dependency — every real caller (app/company/activation, app/company/kora-index, app/company/reports) already uses only this method. evaluateFromSeed(companyId, scenarioId) is the file\'s only method touching data/synthetic/activation-safeguard-results.json, and its sole real caller is services/scoring-simulator/ScoringSimulatorService.ts — explicitly out of scope for this slice ("Do NOT touch final scoring except where required to preserve compile/runtime compatibility"; "STOP if a supposedly worker/account change requires touching final scoring architecture"). Retiring evaluateFromSeed() would require modifying ScoringSimulatorService.ts, which this slice\'s own governance forbids. File remains an I9 allowlist entry, blocked by this final-scoring coupling, not by anything in this slice\'s control. No tenant_kind branch, no new safeguard model invented. See tests/unit/cc00-worker-account-canonicalization.test.ts for the regression guard recording this finding.', deletableWhen: null },
  { id: 'svc.admin-preview', domain: 'Admin/Demo', primaryPath: 'services/admin-preview/AdminPreviewService.ts', purpose: 'NARROWED TO ONE METHOD, within CC-00 (B-TRUTH / ONE PRODUCT, ONE TRUTH — open workstream, not closed by this or any single slice). getPlatformAnalyticsPreview(), getIndexRegistryPreview(), getCompanyPortfolioPreview(), getPartnerNetworkPreview(), getBillingRevenuePreview(), getBenchmarkPreview(), getAdvisorNetworkPreview(), getFounderValidationPreview(), and getAIOnboardingPreview() are all canonical/retired; getPrivacyFilterPreview() moved out entirely (real static policy, inlined at its sole demo caller, itself since retired). getGateStatusPreview() is the sole surviving method — real, accurate, static gate-status configuration (not a synthetic preview), consumed by app/admin/page.tsx. Zero data/synthetic/** imports remain.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-00 AdminPreview Cross-Company Canonicalization Phase 1 (2026-09-06)', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): "7 callers, demo-only." — this count was stale by the time this slice re-verified it: independent repo-wide re-verification found 16 public methods, ALL with real callers (zero dead methods, unlike every prior service migrated in this plan) — see the method-by-method reachability table in this slice\'s own regression test. PHASE 1 (2026-09-06, CC-00): getPlatformAnalyticsPreview() retired — its sole real caller, app/admin/page.tsx, now fetches analytics.tenant (count, no tenant_kind filter) + analytics.kora_index_result (WHERE is_current=true) + analytics.confidence_result (joined via confidence_result_id) + analytics.source_batch server-side and derives the same shape via a new pure view builder, lib/live/admin-cross-company-view.ts\'s buildAdminPlatformAnalyticsView(), decoupled from the legacy method\'s own internal call into getIndexRegistryPreview() (no duplicate business logic — the canonical aggregation is self-contained, not routed through any still-synthetic method). Two fields dropped: active_scenarios (zero real callers, confirmed by repo-wide grep) and the old hardcoded 0.68 placeholder for avg_data_completeness, replaced by a REAL canonical value. Field-parity correction made during this slice: an earlier architecture review had traced avg_data_completeness to analytics.source_batch.completeness_pct and found it null/unpopulated everywhere in the canonical write path (both the real accept route and this project\'s own canonical seed script set it to null) — re-verification for this slice found the CORRECT canonical source is a DIFFERENT column, analytics.confidence_result.data_completeness, a real, methodology-computed component of the Confidence Score formula (lib/kora-engine/confidence-engine.ts, weighted 25%), persisted for real by lib/live/persistence.ts\'s persistKoraComputationResult() for every tenant that has run the canonical scoring pipeline. This was not a fabrication — it was recognizing a real canonical field already existed under a different, initially-unexamined column. getIndexRegistryPreview() is explicitly NOT migrated by this slice: independently discovered security-architecture conflict — one of its two real callers, app/demo/index-registry/page.tsx, is reachable by the DEMO_VIEWER role (lib/auth/kora-session.ts\'s requireDemoAccess()), whose own header comment documents that /demo pages are safe only because they are synth-only ("If Fase 3 introduces a live-data demo tenant, this function must be revisited to prevent live data leakage into the /demo surface") — introducing a live cross-company query there is a security decision this slice does not make unilaterally, and forking a canonical-for-admin/synthetic-for-demo split for the same method was ruled out (ONE PRODUCT, ONE TRUTH). getCompanyPortfolioPreview() remains deferred for a separate, prior reason (real-caller-required fields — territory, is_primary_demo, demo_note — with no canonical equivalent at all). The AI-onboarding cluster (7 methods) remains deferred because every one is hardcoded to the literal string \'meridiana-group\' with no tenant parameter at all — migrating requires a signature change, a distinctly larger, separate slice. Benchmark/advisor-network/partner-network/billing/founder-validation-preview remain deferred — zero canonical model exists for any of them (fully hardcoded literal arrays, not even fixture-backed). No tenant_kind branch introduced anywhere — companies_in_portfolio counts every tenant regardless of kind, matching app/admin/companies/page.tsx\'s own precedent ("every tenant_kind included — no hidden test tenants"). CC-00 remains OPEN — this is one bounded slice, not closure. See tests/unit/cc00-adminpreview-cross-company-phase1.test.ts for the regression guard proving both the migration and the scope boundary. CC-00 — AI-Onboarding Duplicate Retirement (2026-09-06): re-verifying the AI-onboarding cluster (7 methods, all hardcoded to \'meridiana-group\') found that 5 of them do not need canonicalization at all — they duplicate capability that is ALREADY real, live, and canonical elsewhere: getSourceIntakePreview and getMappingIntelligencePreview duplicate app/admin/data-intake/page.tsx (header-labeled "CANONICAL — B154-B: entry point globale Data Intake", KORA_ADMIN-gated); getUefDraftQueuePreview and getHumanReviewPreview duplicate app/admin/uef-review/page.tsx (KORA_ADMIN-gated, real interpreter-generated UEF review); getScoringReadinessPreview duplicates app/admin/pipeline/page.tsx\'s own real, canonical readiness signal (built across PRs #140-#146 of this same plan). All 5 were retired outright (not migrated — nothing to migrate, since the real capability was never uniquely implemented here). Their sole real caller, app/demo/ai-onboarding/page.tsx (a DEMO_VIEWER-gated route, same security tier as Index Registry\'s and Portfolio\'s demo callers), had its now-obsolete UI sections (B, C, E, F, G) removed — no live data was introduced there and no synthetic replacement was added, matching the same DEMO_VIEWER security caution already documented for Index Registry/Portfolio. This retirement required NO founder security decision, unlike Index Registry/Portfolio/getAIOnboardingPreview, because nothing canonical is newly exposed to any audience — something redundant was simply removed. getAIOnboardingPreview (real admin-facing value, feeds app/admin/page.tsx\'s Priority Queue, blocked by the same unresolved DEMO_VIEWER/live-data security question as Index Registry/Portfolio) and getPrivacyFilterPreview (static compliance/policy presentation, not a duplicated live feature) are both explicitly untouched, still deferred. I9 unaffected (12/20 unchanged) — data/synthetic/source-batches.json remains needed by the non-retired getAIOnboardingPreview. See tests/unit/cc00-ai-onboarding-duplicate-retirement.test.ts for the regression guard proving both the retirement and the scope boundary. CC-00 — Index Registry canonicalization (2026-09-06, later the same day): the founder ratified DEMO_VIEWER\'s full retirement (superseding the security reason getIndexRegistryPreview() had been deferred above — see app-surface.demo\'s own entry for the full supersession record). getIndexRegistryPreview() and its IndexRegistryEntry interface are retired; app/admin/page.tsx\'s Intelligence Grid panel now reads canonical analytics.kora_index_result directly via lib/live/admin-cross-company-view.ts\'s buildIndexRegistryView(), reusing the same rows already fetched for Platform Analytics. app/demo/index-registry/ is retired in full. getCompanyPortfolioPreview(), the AI-onboarding survivors, and every Tier C method remain untouched. DEMO_VIEWER role itself not removed — a later slice. See tests/unit/cc00-index-registry-canonicalization.test.ts. CC-00 — Company Portfolio capability salvage + canonicalization (2026-09-12): re-verification found the real capability getCompanyPortfolioPreview() simulated already existed, canonically and more richly, at app/admin/companies/page.tsx ("Company Console" — real, KORA_ADMIN-gated, DB-backed live tenant registry with lifecycle status, users, workforce headcount via personal.workforce_baseline, current KORA Index + Confidence Score + Safeguard, submissions, and Decision Pack status). getCompanyPortfolioPreview() and its CompanyPortfolioEntry interface are RETIRED OUTRIGHT, not canonicalized under a new name — its other real caller, app/admin/page.tsx\'s "Company Readiness Matrix" panel, now reads the SAME already-fetched, already-canonical `registry` array (buildIndexRegistryView() over analytics.tenant + analytics.kora_index_result, already proven by tests/integration/rls-20-admin-cross-company-analytics.test.ts) it already fetches for the Intelligence Grid\'s KORA Index™ Registry panel — no second query, no new method, no new interface field (buildIndexRegistryView()\'s own existing structural guard, in that same RLS-20 file, explicitly asserts entries carry no confidenceScore field — respected, not worked around). Two dead/decorative table columns are dropped from that panel: "CS™" (rendered a hardcoded "—" in every row — never actually wired to real data, even in the synthetic version) and "Fonte" (rendered a hardcoded "Demo" in every row). Field-by-field disposition of the 4 fields with no canonical home: sector — NOT_CANONICAL_TODAY, no schema column exists (analytics.tenant has industry_code, a distinct coarser classification, not equivalent free text — mapping one onto the other would invent semantics); territory — NOT_CANONICAL_TODAY today, but explicitly NOT decoration either: Master Plan v2.1 §"Cosa già esiste" ontology table names it directly — "TERRITORY / LOCAL ENTITY | da tipizzare | nessuno | business | oggi vive come stringa `territory` nei seed [VERIFIED]" — a real, already-identified future ontology object (ProgramDefinition owner_type=territory, territory_ref, per Master Plan §3), deliberately not invented here (this slice does not build territorial architecture) and recorded as a DEFERRED_REAL_CAPABILITY pointing at that Master Plan section, not killed as meaningless; is_primary_demo — DEMO_DECORATION, zero product meaning found anywhere, not replaced by any "featured company" concept; demo_note — DEMO_DECORATION, no canonical notes/narrative concept exists on any tenant record, not invented here. app/demo/portfolio/ (page + layout) is retired in full — zero remaining nav references (app/demo/page.tsx\'s homepage card and lib/navigation/admin-nav-groups.ts\'s "Portfolio Aziende" entry both removed). Two redirect-config corrections in next.config.ts: /admin/portfolio now points at /admin/companies (the real destination) instead of the now-deleted /demo/portfolio; and, found and fixed while editing this same array (not this slice\'s original target, but directly adjacent and free to fix correctly): /admin/index-registry was still pointing at /demo/index-registry, a route CC-00 Index Registry canonicalization retired back on 2026-09-06 — corrected to redirect to /admin, where that capability actually lives now. I9: companies.json and kora-index-outputs.json imports removed from AdminPreviewService.ts (2 of its 3 synthetic imports — source-batches.json remains, still needed by getAIOnboardingPreview) — file count unchanged (12), import count 20->18. Neither fixture became zero-consumer overall: companies.json still needed by services/demo-data/DemoDataService.ts, kora-index-outputs.json still needed by 6 other real consumers — verified by repo-wide grep, not assumed. DEMO_VIEWER role, AI Onboarding, benchmark/network/advisor, B-WORKER, My KORA, and final scoring all explicitly untouched. CC-00 remains OPEN. See tests/unit/cc00-portfolio-canonicalization.test.ts for the regression guard proving both the canonicalization and the scope boundary. CC-00 — Admin Console panel-by-panel canonicalization (2026-09-19): "No panel survives merely because it exists today." Every remaining method re-verified against its actual callers and canonical data support, method by method. getPartnerNetworkPreview() RETIRED outright (deleted, not deferred-in-place): its sole caller was Admin Home\'s "Partner Network" panel; a real canonical network.partner_profile table exists (backs app/partner/workspace) but has no evidence_protocol_status/active_programs equivalent — canonicalizing would mean inventing those fields, out of this slice\'s scope. Deferred as a real NETWORK-track capability. getBillingRevenuePreview() RETIRED outright: zero demo caller ever existed, Foundation Light has zero billing/payment product authority (CLAUDE.md Red Line), and its own panel title already called it "(mock)" — not deferred, there is no future KORA billing capability implied here. getPrivacyFilterPreview() MOVED, not retired or canonicalized: its content is real, accurate, always-true KORA privacy policy (which sensitive-field categories are excluded, pseudonymization applied) — never actually a "preview" of variable state, so it never belonged in a Preview-simulation service; inlined as a local constant directly in its sole caller, app/demo/ai-onboarding/page.tsx, values unchanged. getBenchmarkPreview(), getAdvisorNetworkPreview(), getFounderValidationPreview(), getGateStatusPreview(), and getAIOnboardingPreview() are NOT deleted as methods — each still has a legitimate /demo/** caller (getBenchmarkPreview → /demo/benchmarks, getAdvisorNetworkPreview → /demo/network, getFounderValidationPreview and getGateStatusPreview → /demo/gtm, getAIOnboardingPreview → /demo/ai-onboarding), matching this slice\'s own explicit instruction not to force-retire methods with legitimate residual callers. What changed instead is Admin Home\'s consumption of them: (1) the "Advisor Network" Intelligence Grid panel and its "advisor-queue" Priority Queue item are removed — ADVISOR is a real, active session role (RLS-enforced), but no canonical advisor roster/assignment/specialization model exists anywhere in the schema; the fake per-advisor pending-review counts were being shown as if real, actionable operator signal. Deferred as a real NETWORK-track capability, same track as partner. (2) The "GTM Founder Cockpit" panel is removed outright, not because founder pipeline tracking lacks real value, but because a real, purpose-built, richer internal tool for exactly this already exists — app/admin/founder-validation (B96-B, its own dedicated service/types, already linked from admin nav) — the Admin Home panel was a cruder duplicate showing 100% the same fictional company roster used throughout the demo layer (Meridiana, Nexo Digital, TerraFlex Agri, etc.), not real founder CRM entries. Replaced by a single link to the real tool, not a redesigned panel. (3) The Priority Queue\'s two AI-onboarding-derived signals are corrected: "pending batches" now reads analytics.source_batches_total - analytics.source_batches_approved (the exact same already-fetched, already-canonical aggregate RLS-20 already proves correct — zero new query) instead of getAIOnboardingPreview()\'s single-fake-company count; "scoring blocked" is DROPPED, not migrated, because it was a per-tenant readiness heuristic (approved batch count ≥ 2) with no honest multi-tenant translation — forcing one would invent product semantics this slice does not authorize. (4) The "Platform Analytics" Intelligence Grid panel keeps its data (canonical since Phase 1, 2026-09-06) but loses its "DEMO · dati sintetici" badge — a leftover mislabeling bug that survived two prior slices, not accurate; found and fixed here. (5) "Gate & Methodology" status is kept exactly as-is: real, accurate, static project-governance configuration (matches CLAUDE.md\'s own footer gate-status line) that does not need, and should not pretend to have, a database source — but its former "DEMO" section badge is also dropped, since it was never synthetic. Net effect: Admin Home (app/admin/page.tsx) now carries zero `badgeMode="DEMO"` anywhere — Priority Queue, Company Readiness Matrix, and the Intelligence Grid are 100% canonical; Methodology Governance is accurate static config. No DB-backed test was added: no new canonical query path was introduced anywhere in this slice — the one "new" derived value (pending batches) is arithmetic over two fields RLS-20 already proves correct, and every other change is deletion or relocation. DEMO_VIEWER role, every /demo/** route (none retired this slice), B-WORKER, My KORA, and final scoring are all explicitly untouched. CC-00 remains OPEN. See tests/unit/cc00-admin-console-canonicalization.test.ts for the regression guard proving both the canonicalization and the scope boundary. CC-00 — Residual /demo/** controlled retirement (2026-09-26): the fourth and largest slice of the founder-ratified retirement sequence — "no demo route survives merely because it still has a caller; no demo route is deleted before controlled salvage is complete." Of the 5 methods still standing after the Admin Console slice, 4 lost their sole remaining caller and are retired outright: getBenchmarkPreview() (its caller, app/demo/benchmarks/page.tsx, already stated in its own header comment the exact "no empirical benchmark before Delphi calibration" disclaimer CLAUDE.md §6 requires — nothing to salvage beyond what CLAUDE.md already preserves); getAdvisorNetworkPreview() (its caller, app/demo/network/page.tsx, was 100% hardcoded territory/partner/gap/matching data with zero canonical backing — network.partner_profile has no matching/coverage/gap-diagnostic fields — deferred as a real NETWORK-track capability, same track as partner and advisor); getFounderValidationPreview() (its caller, app/demo/gtm/page.tsx, duplicated the real app/admin/founder-validation tool); getAIOnboardingPreview() (its caller, app/demo/ai-onboarding/page.tsx, was a DEMO_VIEWER-gated route with no remaining unique capability). getGateStatusPreview() is the sole survivor — real, accurate, static gate-status configuration, unchanged, still consumed by app/admin/page.tsx. Retired along with their callers: app/demo/advisor/ (its OTHER dependency, evidenceReliabilityIntelligenceService, is untouched — see svc.advisor-evidence-review\'s own entry for the full disposition), app/demo/gtm/ (Scenario Presenter and Demo Script sections had no remaining canonical duplicate but were judged not worth preserving solely for founder convenience per this slice\'s own explicit instruction; Pilot Package duplicated /pilot + lib/landing/packages.ts; Privacy Story duplicated doctrine already in CLAUDE.md; GTM Pipeline duplicated /admin/founder-validation; Gate & Methodology Status duplicated Admin Home\'s own Methodology Governance panel from the prior slice), and app/demo/guide/ (a pure navigation/doctrine page with zero unique value — its own content only ever linked to real /company/* surfaces, /demo/future-vision, and /pilot; found, independently of this task\'s own prompt, to be the actual href target of the public marketing landing page\'s two "Esplora la demo" CTAs, both retargeted to /demo, along with next.config.ts\'s /demo-guide redirect and app/demo/future-vision/page.tsx\'s own back-link). app/demo/page.tsx (the root demo hub) and app/demo/future-vision/page.tsx are explicitly KEPT — the former still carries real product-value links (SECTION_INTELLIGENCE, SECTION_ROADMAP) after its own Ecosystem/Pipeline/internal-tools sections were removed; the latter is constitutionally protected as a Future Vision static mockup screen under CLAUDE.md §10/§16/Red Line #10, a distinct category from ordinary demo-preview routes. AdminPreviewService.ts is rewritten down to a single flat class with getGateStatusPreview() as its only method — zero data/synthetic/** imports remain, removing it from the I9 allowlist entirely (11->8 files, 16->13 imports; source-batches.json deleted, zero remaining consumers). A pre-existing, unrelated dead-code chain was found and deleted as a transparently-disclosed bonus: components/demo/DemoGuideContent.tsx had zero importers anywhere in the repo, cascading to 3 further orphaned sub-components (PipelineConnectorBanner.tsx, WorkspaceSwitcher.tsx, StakeholderPaths.tsx). DEMO_VIEWER role itself is explicitly NOT removed this slice — a later, separate slice, per this task\'s own explicit constraint. B-WORKER, My KORA, and final scoring untouched. CC-00 remains OPEN. See tests/unit/cc00-residual-demo-retirement.test.ts for the regression guard proving both the retirement and the scope boundary.', deletableWhen: null },
  { id: 'svc.advisor-evidence-review', domain: 'Advisor', primaryPath: 'services/advisor-evidence-review/AdvisorEvidenceReviewService.ts', purpose: 'HISTORICAL — evidence review workflow (B86-B). Retired.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-00 Residual /demo/** controlled retirement (2026-09-26)', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): "Demo-only caller (app/demo/advisor); no live /advisor workspace route exists despite CLAUDE.md documenting one." DELETED (file no longer exists at primaryPath, kept here only as historical record). Its sole caller, app/demo/advisor/page.tsx (AD-01 Advisor Workspace), was retired in full as part of a controlled retirement covering 6 residual /demo/** routes (advisor, ai-onboarding, benchmarks, gtm, guide, network — see app-surface.demo\'s own entry for the full route-by-route disposition). The page\'s OTHER dependency, evidenceReliabilityIntelligenceService (services/evidence-reliability/EvidenceReliabilityIntelligenceService.ts), is explicitly untouched — it is a real, canonical, shared computation service with other confirmed live callers (app/company/kora-index/page.tsx, components/reports/BudgetImpactReport.tsx, lib/live/live-recommendations.ts, lib/live/live-board-actions.ts, services/executive-intelligence/ExecutiveIntelligenceService.ts), unlike this service, whose only caller anywhere was the retired demo page. ADVISOR remains a real, active session role (RLS-enforced) with no canonical advisor roster/assignment/workspace model — that gap (already recorded on TARGET_ONTOLOGY\'s own \'Advisor\' entry) is unaffected by this deletion and remains a deferred NETWORK-track capability, not resolved here. See tests/unit/cc00-residual-demo-retirement.test.ts for the regression guard proving both the deletion and the scope boundary.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.booking-request', domain: 'Commons (legacy)', primaryPath: 'services/booking-request/BookingRequestService.ts', purpose: 'Request/confirm stub — all methods return [] / null unconditionally.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: ['svc.commons.booking'], decisionRef: 'Master Plan §32 Safe Deletion Plan', notes: 'Verified: 24 lines, zero callers anywhere (no import, no dynamic/string reference, no test), no backing DB table. Master Plan §32 independently confirms: "24 L, ritorna []/null [VERIFIED]" — commons/BookingService resta.', deletableWhen: 'After B-REG registry is live and a repo-wide re-grep at CC-003+ time confirms still zero references.' },
  { id: 'svc.bti-intelligence', domain: 'Financial', primaryPath: 'services/bti-intelligence/BTIIntelligenceService.ts', purpose: 'Rule-based narrative interpretation layer on top of BTI records (over/under/balanced pillar investment classification).', status: 'COMPLETE', futureCore: false, dependencies: ['svc.budget-to-human-impact'], competingWith: [], decisionRef: null, notes: '1 caller. Confirmed distinct responsibility from budget-to-human-impact (imports only its result type, not the service) — not a duplicate.', deletableWhen: null },
  { id: 'svc.budget-to-human-impact', domain: 'Financial', primaryPath: 'services/budget-to-human-impact/BudgetToHumanImpactService.ts', purpose: 'Synthetic-seed BTI computation — superseded by the live analytics.bti_result path.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Retire Orphan Synthetic BTI Chain (2026-08-31)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). Its 3 remaining callers — DynamicScoringPreviewService, ReportGeneratorService, CompanyIntelligenceService — were each independently confirmed to have zero reachable callers from any app/ entry point (ReportGeneratorService and CompanyIntelligenceService were already established as orphaned in prior B-TRUTH work this same week; DynamicScoringPreviewService\'s only caller is the orphaned ReportGeneratorService). The real, live BTI path already exists and is independently verified: analytics.bti_result, read directly by app/api/admin/company-workspace/route.ts. Its sole seed file (data/synthetic/budget-to-human-impact.json) was deleted alongside it — zero remaining consumers, proven before deletion. The 3 consumers were trimmed (BTI-specific calls removed, replaced with null-safe equivalents matching their own pre-existing null-handling branches) and left otherwise unredesigned and unretired — their overall registry fate remains a separate, still-open decision (svc.report-generator and svc.company-intelligence stay INVESTIGATE). One previously-unlisted reference was found and fixed: components/kora-index/EconomicReliefPanel.tsx held a type-only import of EconomicReliefSummary, copied locally into the component (plain data shape, no behavior) rather than relocated — the component remains orphaned and otherwise untouched. I9 reduced 25->24 files (37->36 imports) — the first genuine I9 reduction since Root Control Room Wave 2.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.care-economy', domain: 'Intelligence', primaryPath: 'services/care-economy/CareEconomyIntelligenceService.ts', purpose: 'Care-economy LIFE-pillar interpretation.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '3 callers.', deletableWhen: null },
  { id: 'svc.commons', domain: 'Commons', primaryPath: 'services/commons/CommonsService.ts', purpose: 'Commons / collective initiative surface — canonical live discovery only (CC-052 closed 2026-08-31).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '7 callers. Governance Patch 01 (2026-08-31): Master Plan §13 explicitly names this service\'s dual discovery-path issue — "CommonsService, che importa commons-initiatives.json per la discovery mentre getPublishedInitiatives legge dal DB live — due percorsi di scoperta nello stesso servizio" — as something to eliminate/consolidate, and §30 explicitly reserves CC-052 ("Ritiro discovery sintetica CommonsService") for that fix. Status left CANONICAL (the service functions; the defect is tracked via decisionRef, not hidden) pending CC-052\'s own dedicated audit — not reclassified further by this patch. CC-051/B-024 closeout (2026-08-31): migration 024_commons_initiative_fields.sql (opening_grade, geolocation, capacities, cross-company RLS policy commons_post_worker_cross_company_select) is confirmed CLOSED — additive-only, idempotent (ADD COLUMN/INDEX/POLICY IF NOT EXISTS guards throughout except the single ADD CONSTRAINT, standard for a migration applied exactly once via the tracked migration ledger), zero conflicts with migrations 025-048 (only 025 and 046 reference commons.post at all, neither touches 024\'s columns), verified applying cleanly against a fresh Postgres via the mandatory DB-backed CI gate (all 42 migrations through 049). No rollback artifact exists for 024 specifically (supabase/rollback/ has entries for 029-048 but not 024) — additive/backward-compatible nature makes one lower-risk to omit, but this is a real, not a fabricated, gap. tests/unit/b165-commons-initiatives.test.ts verifies the RLS policy and opening_grade CHECK constraints structurally (asserts exact SQL text) but there is no live-DB behavioral test proving the commons_post_worker_cross_company_select policy actually grants cross-tenant visibility against real rows — a known gap, not a blocker to this closeout, worth closing before CC-052 relies on it. The migration file\'s own header ("Gate 2 OPEN — written, NOT applied") is stale and asserted verbatim by b165\'s own "Gate 2 annotation" test; left uncorrected in this pass since fixing it requires also updating that test, both out of this closeout\'s narrow scope. CC-051/B-024 = COMPLETE. CC-052\'s schema prerequisite is satisfied; CC-052 is unblocked. CC-052 (2026-08-31): PARTIAL, not complete. Finding: getPublishedInitiatives() is confirmed the SOLE authority for real worker/company/admin-facing initiative discovery — every genuine "find and join a real initiative" caller (app/api/commons/initiatives/route.ts, app/company/commons/page.tsx via a direct commons.post query) already reads live, zero synthetic dependency; §13\'s "due percorsi di scoperta" concern does not apply to the actual product discovery mechanism today. What remains synthetic is narrower than "discovery": getByPillar/getByType were dead code (zero callers) and are now deleted. The 3 surviving synthetic methods (getInitiatives, getFeaturedInitiatives, getNetworkStats) have exactly 2 real callers left, both already-governed, explicitly-labeled PREVIEW surfaces, not discovery: app/commons/page.tsx (B185 — its own layout.tsx states "shared network preview routes — not public... synthetic demo data labelled \'Rete pilota dimostrativa\'", KORA_ADMIN-only in practice) and app/my-kora/page.tsx\'s featured-commons widget (B81-B PREVIEW route, D-D/CC-024 territory — "Pilot+: WorkerSessionProvider resolves LIVE session... no component changes required — only provider and service swap", i.e. its own documented future is D-D\'s job, not CC-052\'s). Real, verified parity gap found: lib/commons/types.ts\'s CommonsInitiative (synthetic — description, initiative_type, owner_organization, owner_sector, location_type enum, visibility, end_date, capacity, participants_enrolled, status lifecycle open/upcoming/full/completed, activation_potential, verification_possible, tags, and a literal synthetic_demo_data: true type field) has NO live equivalent on commons.post even after migration 024 — CommonsPostWorkerView carries only id/title/body/category/pillar/timestamps/opening_grade/geo/capacity_internal/capacity_cross. Confirmed by app/company/commons/page.tsx\'s own direct live query, which selects a much narrower field set and renders none of the richer fields. Retiring the remaining synthetic import requires either a schema-extension decision (new columns for status lifecycle/activation_potential/verification_possible/participant counts — real new product functionality) or a product-redesign/formal-reclassification decision for these 2 preview surfaces — both explicitly out of this task\'s narrow scope (no new schema, no product-semantics change, no touching D-D/B-WORKER territory). CC_052_COMPLETE = NO. Recommend a founder decision on these 2 preview surfaces\' future before any further synthetic-import retirement here. I9 unchanged by this pass (24 files/36 imports) — dead-code removal does not affect the allowlist since the file itself remains listed. CC-052 FINAL CLOSURE (2026-08-31): field disposition ratified (KEEP/RENAME description/end_date/capacity, DERIVE owner_organization/owner_sector/participants_enrolled/status, DROP visibility/tags, DEFER activation_potential/location_type/stored verification_possible — no schema added for the deferred set, no placeholder values invented). Both remaining callers migrated onto canonical live discovery, zero schema change required: app/commons/page.tsx is now a server component reading the new getPublishedInitiativesAdmin() (this file), enriched via a lib/commons/discovery-view.ts adapter — analytics.tenant join for owner_organization/owner_sector, the pre-existing commons.booking_aggregate_for_promoter() SECURITY DEFINER RPC (services/commons/BookingService.ts, unchanged) for participants_enrolled, and a pure date/capacity derivation for status. app/my-kora/page.tsx\'s widget now fetches the same canonical /api/commons/initiatives route a real session uses, degrading to an honest empty state (not a synthetic fallback) since that page\'s own session is demo-state with no live JWT (B81-B, D-D territory, unchanged by this task). data/synthetic/commons-initiatives.json deleted; the CommonsService synthetic class (getInitiatives/getFeaturedInitiatives/getNetworkStats, getByPillar/getByType already removed) deleted entirely — this file now contains only getPublishedInitiatives and getPublishedInitiativesAdmin, both live. tests/unit/b97b-commons-foundation.test.ts (entirely about the removed class) deleted; tests/unit/cc052-commons-discovery-view.test.ts added for the new adapter\'s pure functions; tests/integration/rls-11-commons-cross-company.test.ts added and wired into the mandatory DB-backed CI gate, closing the exact gap the CC-051/B-024 closeout note above flagged (no live-DB behavioral proof of commons_post_worker_cross_company_select) — proves a WORKER discovers a cross_company post from another tenant but not a company_internal one, with a same-tenant positive control. I9 reduced 24->23 files (36->35 imports) — the first reduction attributable to a full caller migration, not a dead-code trim. Master Plan §13\'s "due percorsi di scoperta nello stesso servizio" defect is resolved: one discovery path, live, RLS-scoped, used identically by a real tenant and a future NO-DEMO-RUNTIME synthetic tenant (Patch 03) alike. CC_052_COMPLETE = YES. The migration file header / b165 "Gate 2 annotation" test staleness noted in the CC-051 entry above remains open, unrelated to this closure, left for a dedicated follow-up.', deletableWhen: null },
  { id: 'svc.commons.booking', domain: 'Commons', primaryPath: 'services/commons/BookingService.ts', purpose: 'Live cross-tenant booking (worker_tenant_id × post_tenant_id pattern).', status: 'CANONICAL', futureCore: true, dependencies: [], competingWith: ['svc.booking-request'], decisionRef: null, notes: 'Master Plan §33 Do-Not-Delete: "vivo e canonico" [VERIFIED]. 5 live API routes, SECURITY DEFINER cross-tenant boundary check (migration 025).', deletableWhen: null },
  { id: 'svc.company-data-intake', domain: 'Ingestion', primaryPath: 'services/company-data-intake/CompanyDataIntakeService.ts', purpose: 'HISTORICAL — pipeline-only Data Intake synthetic summary for the demo Pilot Lifecycle orchestrator, superseded by canonical analytics.source_batch/uef_record reads. Retired.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH CompanyDataIntakeService Canonical Migration (2026-09-05)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-05), PR 3 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan (PR 1 = B-TRUTH KoraTest Canonical Foundation; PR 2 = B-TRUTH TenantService Canonical Migration): independently re-verified before deletion — both real runtime callers (app/admin/pipeline/_components/PilotLifecycleClient.tsx, services/report-factory/ReportFactoryService.ts) individually confirmed, zero type-only callers. Only the 3 fields either real caller actually consumed (batch_count, intake_status, review_required_rows — out of ~16 fields on the legacy getDataReadinessSummary() shape) were migrated, via a new shared pure view builder, lib/live/data-intake-status-view.ts, fed by analytics.source_batch (batch existence + latest batch_status by created_at, matching the same "latest row" selection precedent already used elsewhere in lib/live and the canonical scoring route) and analytics.uef_record WHERE review_status=\'pending_review\' (the same counting query app/api/admin/uef/review/route.ts\'s own GET handler already uses, reused not reinvented). The legacy \'blocked_missing_required_fields\' intake-status value is NOT reproduced — it was a post-hoc heuristic for malformed rows that, in the real pipeline, are already rejected at the canonical upload boundary (PII-scan/validation in accept/route.ts) before a source_batch row is ever created; neither real caller\'s behavior ever depended on this value firing — this is a stale-heuristic retirement, not a capability loss. app/admin/pipeline/page.tsx (a Server Component since the prior PR) fetches this canonical view once and passes it to both PilotLifecycleClient.tsx and, transitively, ReportFactoryService — avoiding a duplicate query, same discipline as the CanonicalTenantStatus parameter added by the prior PR. ReportFactoryService.getDecisionPackFactoryStatus/computeBlockingReasons gained a third parameter, dataIntake: CanonicalDataIntakeStatus, replacing the internal companyDataIntakeService dependency entirely — its own still-synthetic hasKoraIndex/getLatestDecisionPackVersion checks and Decision Pack version source are UNCHANGED, unmigrated, explicitly out of scope (a separate, later slice). No tenant_kind branch introduced — identical view/query shape for KoraTest Srl (tenant_kind=TEST) and any tenant_kind=LIVE tenant. One real gap found and fixed in scripts/koratest-canonical-seed.ts (PR 1\'s own foundation script) while validating this migration against real KoraTest data: it never updated source_batch.batch_status to \'approved\' after scoring completed (unlike the real run-approved-batch/route.ts, which does) — fixed by adding the same update, in its own labeled step. Its 3 sole seed files (data/synthetic/company-budget-fiscal-plans.json, company-raw-data-batches.json, company-raw-data-rows.json) deleted alongside it — confirmed zero remaining real consumers of any of the three. Its type family (CompanyBudgetFiscalPlan, CompanyRawDataBatch, CompanyRawDataRow, CompanyDataReadinessSummary, CompanyDataIntakeStatus, all defined in @/lib/types, not by this file) was deliberately NOT deleted — no opportunistic cleanup, confirmed zero other consumers but left in place, same discipline as every prior B-TRUTH retirement this session. AdminPreviewService, AccountProvisioningService, and ReportFactoryService\'s Decision Pack version source are explicitly untouched — one PR = one bounded migration. CompanyDataIntakeService.ts was an I9 allowlist entry — twelfth genuine I9 reduction via a real caller migration: 14->13 files (24->21 imports). See tests/unit/b-truth-company-data-intake-canonical-migration.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): GOVERNANCE CORRECTION (2026-08-30, Demo/Orphan Chain Hardening): status corrected CANONICAL -> CONSOLIDATE. The previous CANONICAL label conflated two distinct things: (1) the Data Intake PRODUCT DOMAIN, which is genuinely canonical and live — lib/data-intake/* (registered as lib.ingestion-cluster; csv/excel parsers, column mapping, evidence attachment/provenance), analytics.source_batch, analytics.uploaded_record, analytics.uef_record, and the live /admin/data-intake/* and /admin/uef/* API routes, all verified reachable and DB-backed; and (2) THIS SERVICE, CompanyDataIntakeService, which reads 100% synthetic JSON (data/synthetic/company-budget-fiscal-plans.json, company-raw-data-batches.json, company-raw-data-rows.json) and executes none of the canonical path. COMPANY_DATA_INTAKE_SERVICE_IS_CANONICAL = NO. Reading synthetic demo rows does not become canonical merely because the product domain has a live implementation elsewhere. CONSOLIDATE (not LEGACY) because it remains reachable intentionally for the retained pipeline demo surface (KEEP_DEMO decision, B-TRUTH Demo/Orphan Chain Audit) — the same pattern already used for svc.report-factory and svc.tenant — not because it is frozen/incompatible-with-canonicalization; runtime convergence with the canonical ingestion path (lib.ingestion-cluster) remains simply incomplete, not abandoned. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30) terminology correction: 3 runtime callers (app/admin/pipeline/page.tsx, services/report-factory/ReportFactoryService.ts, services/company-intelligence/CompanyIntelligenceService.ts) — none is a production/live caller. Pipeline hardcodes DEMO_COMPANY_ID = \'meridiana-group\'; ReportFactoryService\'s only caller is pipeline (same constant), so its call is demo-rooted too. CompanyIntelligenceService\'s call is unreachable dead code (see svc.company-intelligence — 0 callers). Do not describe any of these as "real"/"production" callers merely to distinguish them from tests; the correct terms are runtime caller (a literal call site) vs. production/live caller (reachable via a real user/admin flow with a real tenant id) vs. demo caller (reachable only through a synthetic-identity demo flow). B-TRUTH Demo/Orphan Chain Audit (2026-08-30): reachability trim — verified per-method, repo-wide (including internal this.* calls), that the only reachable public surface is getDataReadinessSummary plus the internal helpers it actually calls (getBudgetFiscalPlan, getRawDataBatches, getRawDataRows, getNextAction, and 6 private classification predicates). Removed 12 public methods with zero callers anywhere (getAvailableCompanies, getFiscalPerimeterSummary, getRawDataRowsForBatch, getRowsReadyForIngestion, getEligibleCandidates, getLimitedCandidates, getBlockedCandidates, getStructuralPolicyRows, getReviewRequiredRows, getRowsWithMissingFields, validateRawDataBatch, getPipelineLinks) — none reached internally, none tested. tsc + full suite (289 files/11128 tests) unchanged after removal. Previously "6 callers" — stale, pre-dates B-TRUTH Gen 0/1 Retirement Wave 1 and Root Control Room Wave 2/3, which removed several.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.company-intelligence', domain: 'Admin', primaryPath: 'services/company-intelligence/CompanyIntelligenceService.ts', purpose: 'HISTORICAL — cross-service company insight aggregation. Retired (obsolete-capability retirement, not migration).', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-020A Company Intelligence Capability Retirement (2026-08-31, narrowed)', notes: 'CC-020A (2026-08-31) confirmed 0 runtime callers repo-wide, unchanged from the prior INVESTIGATE finding. First pass of this retirement (same day) also deleted CompanyOnboardingService as a claimed \'second-order orphan\' — that was WRONG and has been reverted (see svc.company-onboarding): CompanyOnboardingService is a Master-Plan-anchored competing implementation of svc.company-setup, not disposable just because its only caller was this service. This entry records the corrected, narrower, capability-level decision: (1) CompanyIntelligenceService.resolveRiskLevel() is an ad-hoc heuristic with no Master Plan methodology anywhere in the frozen text; (2) no N1-N14/NB capability needs, supersedes, or overlaps it (checked explicitly against Investment Map, Needs Map, Decision Memory, Economic Case/N11, Cockpit/N12, Portfolio Review/N13 — N13 is about a company reviewing ITS OWN investment portfolio, not KORA managing its client tenant roster; the word \'portfolio\' is shared, the referent is not); (3) no canonical replacement SERVICE exists for this specific aggregation — do not claim otherwise — but every live-relevant field it read already has a live source elsewhere (analytics.tenant.onboarding_status/data_readiness_status/decision_pack_status, already read directly by /api/admin/company-console; analytics.kora_index_result, read by the Gen3 workspace tab; real worker provisioning at /admin/workers). This is an OBSOLETE-CAPABILITY retirement (the business need is gone), distinct in kind from a LIVE-MIGRATION retirement (BTI, Lifecycle, Pillar Adoption) where a real replacement took over the same function — Master Plan §32\'s literal deletion bar (\'live migration complete and verified\') does not literally cover this case; this decisionRef is the explicit, capability-level founder record §32 implicitly requires for it. If KORA ever needs a cross-tenant admin readiness/action-queue view again, it should be built fresh against the live fields named above, not by reviving this heuristic. See also docs/KORA_OFFICIAL_IMPLEMENTATION_MASTER_PLAN_v2.1_PATCH_01.md §5, which documents this same finding at the governance-patch level.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.company-onboarding', domain: 'Company', primaryPath: 'services/company-onboarding/CompanyOnboardingService.ts', purpose: 'Post-provisioning company readiness/status logic — Foundation Light eligibility, privacy-threshold warnings, pipeline readiness checks, next-best-action guidance.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Company Onboarding Canonicalization (2026-09-01)', notes: 'FIXED ARCHITECTURE DECISION (this task, 2026-09-01, explicit founder instruction): CompanySetup and CompanyOnboarding are NOT competing implementations. Distinct responsibilities — CompanySetup is the pre-provisioning wizard/setup flow (services/company-setup/CompanySetupService.ts, in-code templates, zero synthetic dependency); CompanyOnboarding is post-provisioning readiness/status logic. This supersedes the CC-020A / Master Plan §33 "competing implementation" framing recorded in this entry\'s own prior history (see tests/unit/cc020a-retire-company-intelligence.test.ts for that superseded record). Synthetic read (data/synthetic/company-onboarding.json) retired; canonical authority is analytics.tenant (onboarding_status/data_readiness_status/decision_pack_status, already read live elsewhere by /api/admin/company-console) + personal.workforce_baseline (via the already-canonical lib/live/workforce-baseline-view.ts, not re-derived) through a new pure view builder, lib/live/company-onboarding-view.ts. Derived logic preserved: isFoundationLightEligible, getPipelineReadiness, getNextBestAction, getPrivacyThresholdWarnings — all now DB-backed, same LIVE/DEMO-kind-tenant code path (no tenant_kind branch). 7 accessor methods with no canonical equivalent (rich CompanyProfile fields, HR KPI context, program-data budgets — none stored live anywhere in the schema) and zero real callers were retired, not fabricated. Zero real runtime callers existed before or after this port — canonicalized per this task\'s own explicit instruction to do so regardless. See lib/live/company-onboarding-view.ts header for the full KEEP/DERIVE/DROP field disposition and tests/unit/btruth-company-onboarding-view.test.ts / tests/integration/rls-15-company-onboarding-tenant-kind-parity.test.ts for verification.', deletableWhen: null },
  { id: 'svc.company-setup', domain: 'Company', primaryPath: 'services/company-setup/CompanySetupService.ts', purpose: 'Pre-provisioning company setup wizard flow (templates, size bands, draft state) — distinct from svc.company-onboarding\'s post-provisioning readiness logic.', status: 'INVESTIGATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Zero real callers (only route-string literals reference it). Master Plan §32/§33 state explicitly, twice: "company-setup e report-generator restano INVESTIGATE" — this status is unchanged by the B-TRUTH Company Onboarding Canonicalization (2026-09-01), which corrected only the competingWith claim: CompanySetup and CompanyOnboarding are distinct responsibilities (pre-provisioning wizard vs. post-provisioning readiness/status), not competing implementations. See svc.company-onboarding.', deletableWhen: null },
  { id: 'svc.confidence-score', domain: 'Methodology', primaryPath: 'services/confidence-score/ConfidenceScoreService.ts', purpose: 'Confidence Score computation, class-based, weights 40/30/20/10.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: ['lib.kora-engine (confidence-engine.ts)'], decisionRef: 'CC-011 / D-A', notes: 'D-A resolved (CC-004/CC-011): lib/kora-engine/confidence-engine.ts is canonical. Still zero production callers — retained non-canonical pending a formal retirement decision, not deleted or marked DEAD per Master Plan discipline (no callers ≠ dead).', deletableWhen: null },
  { id: 'svc.demo-data', domain: 'Demo', primaryPath: 'services/demo-data/DemoDataService.ts', purpose: 'Central synthetic seed reader.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH', notes: 'One test-only caller; most demo pages import data/synthetic/*.json directly instead. Master Plan §32 names it for removal at end of B-TRUTH.', deletableWhen: null },
  { id: 'svc.dynamic-cv', domain: 'Worker', primaryPath: 'services/dynamic-cv/DynamicCVService.ts', purpose: 'Worker-self-only Dynamic Impact CV.', status: 'FUTURE_CORE', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Master Plan §33 Do-Not-Delete list. 5 callers.', deletableWhen: null },
  { id: 'svc.dynamic-scoring', domain: 'Scoring', primaryPath: 'services/dynamic-scoring/DynamicScoringPreviewService.ts', purpose: 'HISTORICAL — proxy macroblock estimate from live IU batches — explicitly "not authoritative" per own header. Covers services/scoring/PreviewScoringAdapter.ts too (its sole real importer, no dedicated registry entry ever existed for it — folded in here per smallest-existing-convention).', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Preview Scoring Retirement (2026-09-03)', notes: 'DELETED (both files no longer exist at their paths, kept here only as historical record). Prior note ("6 callers, narrow documented use... reachable via IScoringService.ts, PreviewScoringAdapter.ts, lib/scoring-result/index.ts") was STALE/WRONG, corrected here: independently re-verified, IScoringService.ts and lib/scoring-result/index.ts only ever mentioned DynamicScoringPreviewService in comments ("NEVER import..."), never a real import — lib/scoring-result/index.ts\'s actual dispatch only ever branched on environment === \'demo\'/\'live\', never wired a PREVIEW path despite IScoringService.ts documenting a "3 scoring paths" model. The ONLY real import was PreviewScoringAdapter.ts, whose own only real-ish consumer was ReportGeneratorService\'s Decision Pack preview section — retired 2026-09-02. RETIREMENT (2026-09-03): both files independently re-verified to have zero real runtime callers (static, dynamic, barrel, and type-only imports all checked, repo-wide) before deletion. No unique methodology: every DynamicScoringPreviewService computation was either an explicitly self-labelled proxy approximation ("stima proxy", hardcoded WB=0.35/EQ=0.40 placeholders) or direct reuse of activationSafeguardService.evaluate() / scoringSimulatorService.computeKoraIndexV3() (both unchanged, unaffected). ScoringPathMode narrowed from \'DEMO\'|\'PREVIEW\'|\'LIVE\' to \'DEMO\'|\'LIVE\' in services/scoring/IScoringService.ts (confirmed zero other real consumers of the \'PREVIEW\' literal via that type — DemoScoringAdapter/LiveScoringAdapter use only \'DEMO\'/\'LIVE\'). tests/unit/b89b-architecture-hardening.test.ts\'s adapter-conformance suite updated to test only DemoScoringAdapter/LiveScoringAdapter (2, not 3) — coverage for both remaining adapters unchanged. UEFReviewService.ts (DynamicScoringPreviewService\'s sole upstream dependency) is untouched — not retired, not modified; its own real-caller count is now 0 (documented, not acted on) but it remains protected by demo-guard-01\'s separate, deliberate, method-level assertion, a sequencing decision unrelated to this retirement. ReportFactoryService, the rest of the Ingestion/UEF legacy chain (IngestionPipelineService, EligibilityGateService, UEFReviewService), ExplainabilityService live glossary, and the final scoring group are explicitly untouched. Neither file was ever an I9 allowlist entry — I9 unaffected by this retirement (17/27 unchanged). See tests/unit/b-truth-preview-scoring-retirement.test.ts for the regression guard proving both the deletion and the scope boundary.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'tooling.koratest-canonical-seed', domain: 'Ingestion', primaryPath: 'scripts/koratest-canonical-seed.ts', purpose: 'B-TRUTH canonical test tenant input foundation — provisions one canonical test tenant per invocation (default: KoraTest Srl; any other via --fixture=<path>) and pushes deterministic fake INPUT data through the real, unmodified KORA pipeline. Seeds inputs only; every derived output is produced by invoking canonical functions.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH KoraTest Canonical Foundation (2026-09-03)', notes: 'PR 1 of the B95-B/B95-C canonical migration plan (see the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION direction). Additive foundation only — creates a new canonical tenant (analytics.tenant, tenant_kind=TEST, company_name="KoraTest Srl") and its own deterministic input fixture (data/koratest/koratest_input_fixture.json, NOT under data/synthetic/**, NOT reusing lib/live/op001-synthetic-records.ts) via the same input-boundary tables and shapes app/api/admin/data-intake/accept/route.ts and app/api/admin/companies/provision/route.ts already use. Every downstream result — UEF interpretation, eligibility, KORA Index/BTI/activation/confidence, Decision Pack — is produced by invoking the exact same canonical functions a real tenant\'s upload uses (lib/ingestion/raw-to-uef-interpreter.ts\'s interpretUploadedRecord, lib/kora-engine/eligibility-gate.ts\'s classifyEligibilityBatch, lib/live/uef-to-scoring-records.ts\'s buildScoringRecordsFromApprovedUef, lib/kora-engine/run-kora-pipeline.ts\'s runKoraPipeline, lib/live/persistence.ts\'s persistKoraComputationResult, lib/live/decision-pack.ts\'s persistDecisionPack — all imported unchanged, never reimplemented) — no result table is ever directly inserted by this script. The one human-review step (UEF candidate approval) is stood in for by a deterministic, clearly-labeled automated-operator-approval rule identical to app/api/admin/uef/review/route.ts\'s own \'approve\' branch — the interpreted content itself is never touched by this stand-in. tenant_kind is set exactly once (at tenant creation) and is never read by any of the six canonical functions above — confirmed by tests/unit/b-truth-koratest-canonical-foundation.test.ts\'s structural guard, matching the pre-existing precedent in app/api/admin/companies/provision/route.ts (the only existing tenant_kind-conditioned branch in this pipeline skips a real external side-effect, the email invite, never product/methodology logic — this script does not even create a Supabase Auth user). KoraTest Srl uses tenant_code \'KORATEST-01\', deliberately not \'OP-001\' — the one existing hardcoded tenant-identity special case (app/api/admin/scoring/run-approved-batch/route.ts\'s literal block on tenant_code === \'OP-001\') does not apply to it. Idempotent: reuses an existing tenant by tenant_code, reuses an existing source_batch by its natural key (tenant_id, reporting_period, source_name), and skips UEF generation/scoring when already present — no destructive statement anywhere, no cleanup/reset helper (not needed for this PR, not added speculatively). See tests/integration/rls-17-koratest-canonical-foundation.test.ts (DB-backed, skip-safe locally, mirrors RLS-16\'s safety model) for the proof that the real interpreter and scoring engine actually execute and produce non-fabricated, non-uniform output (the fixture deliberately includes a KW_LIMITED and a KW_BLOCKED row, not just eligible ones), and that a second run is idempotent. This PR does NOT retire, migrate, or change the lifecycle status of TenantService, AccountProvisioningService, CompanyDataIntakeService, ReportFactoryService, or AdminPreviewService (the B95-B/B95-C demo-admin cluster) — those remain their own separately-authorized future migration slices (PRs 2-6 of the same plan). I9 unaffected: 15 files/25 imports unchanged — this script imports no data/synthetic/** path and is not itself scanned by the I9 guard (scripts/ is outside app/services/lib/components). B-TRUTH Second Canonical Test Company (2026-09-06, PR 6 of the same plan): generalized to accept a second (or any further) canonical test tenant via a new --fixture=<path> CLI flag, defaulting to the original KoraTest fixture path when omitted (byte-identical default behavior, re-verified against the existing 28-assertion regression suite, 0 regressions). The script\'s substantive logic (tenant_code, company_name, reporting_period, workforce_population, segment_breakdown, rows) was ALREADY fixture-driven, never hardcoded — only the fixture file path itself, plus a handful of cosmetic string-literal labels/prefixes (pseudonym_id/raw_hash/recordId prefixes, the createdBy label, and human-readable notes/log text, previously hardcoded to "koratest"/"KoraTest Srl") needed generalizing, now derived from the loaded fixture\'s own tenant_code/company_name. No canonical pipeline/methodology function was touched. Second canonical test tenant provisioned this way: "Bosco Verde Cooperativa Sociale" (tenant_code \'BOSCOVERDE-01\', tenant_kind=TEST, fixture data/koratest/boscoverde_input_fixture.json) — a clearly fictional cooperative/social-enterprise test company, deliberately distinct from KoraTest Srl (68 vs. 42 workers, 12 vs. 10 rows, a different category/pillar mix, its own separate limited/blocked/incomplete rows) so future cross-company product surfaces (AdminPreviewService, a later, separate PR) have genuine, non-degenerate variation, not two copies of the same numbers. tenant_kind remains operational-safety-only, never a processing branch, for both tenants — the SAME seedCanonicalTestTenant() mechanism (see tests/integration/rls-17-koratest-canonical-foundation.test.ts, generalized in this same PR) provisions either one via a parameter object, not a tenant-specific code path. AdminPreviewService, `/demo` routes, AccountProvisioningService, WorkerProvisioningService, WorkerAchievementService, FounderValidationService, My KORA/session identity, and final scoring are all explicitly untouched by this PR — consumer migration (AdminPreview reading both canonical tenants) is PR 7, not started here. See tests/unit/b-truth-second-canonical-test-company.test.ts for the regression guard proving both the generalization and the scope boundary.', deletableWhen: null },
  { id: 'svc.eligibility-gate', domain: 'Ingestion', primaryPath: 'services/eligibility-gate/EligibilityGateService.ts', purpose: 'HISTORICAL — legacy taxonomy/preprocessing classifier, explicitly NOT the live scoring eligibility engine (own header). Retired.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Eligibility Gate Retirement (2026-09-03)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-03): independently re-verified before deletion — all 4 public methods (classifyAction, classifyActions, getActionTaxonomy, getEligibilitySummary) individually confirmed zero real callers, zero type-only callers. CORRECTION to this entry\'s own prior claims (recorded here, not silently dropped): this service\'s own header, and this entry\'s prior "4 callers" note, both implied real usage — the header specifically claimed "Admin BCM Mapping Review (AI Upload Studio)", "Pre-ingestion operator classification UI", and "Taxonomy exploration" as live consumers. Tracing the actual live routes found this stale: no taxonomy-exploration UI exists anywhere, and the real, scoring-authoritative eligibility engine, lib/kora-engine/eligibility-gate.ts (classifyEligibilityBatch), is used directly — with its own explicit "no duplication" comment — by all 3 real admin data-intake routes (app/api/admin/data-intake/upload-preview/route.ts, preview/route.ts, accept/route.ts). tests/unit/eligibility-gate.test.ts\'s pre-existing "B71 regression guard" describe block independently corroborates that the live scoring pipeline routes through the canonical file, never through this one. Legacy rule nuances present only in the retired file (CCNL/contractual_mandatory override via CCNL_IMPROVEMENT_SIGNALS, Academy/Operations ambiguity detection via hasAmbiguitySignal, keyword-matching against data/synthetic/action-taxonomy.json via findBestTaxonomyMatch) were inventoried and classified: none qualify as MUST_MIGRATE_BEFORE_RETIREMENT — the service\'s own header already disclaimed scoring authority ("IT DOES NOT CONTROL SCORING"), canonical was always the sole scoring-authoritative engine regardless of this file\'s existence, and zero live callers currently exercised any of this file\'s own rules. Its sole seed file, data/synthetic/action-taxonomy.json, deleted alongside it — confirmed exactly ONE real, value-level consumer (this service itself); the other apparent hits (a UI step-label string in app/admin/kora-activation-layer/page.tsx, a code comment in lib/partner-activities/catalog.ts, and a prior retirement test\'s own non-usage assertion) are non-functional, confirmed by direct re-check, not trusted from any prior audit. Its two re-exported data contracts, EligibilityClassificationInput and EligibilityClassificationResult, are defined in @/lib/types (not by this file) and are kept unchanged — no opportunistic cleanup; their prior real importers, IngestionPipelineService.ts and IngestionSimulatorService.ts, were both already independently retired by earlier PRs, leaving zero remaining type-only importers of this file specifically. This is a stale-claim correction and zero-caller cleanup, NOT a canonical methodology deletion — classifyEligibilityBatch and its full BLOCKED/LIMITED/ELIGIBLE keyword tables, INDIVIDUAL_SENSITIVE_SIGNALS privacy-priority check, and B15 UEF-reviewed passthrough logic are entirely unaffected. EligibilityGateService.ts was an I9 allowlist entry — tenth genuine I9 reduction via a real caller migration: 16->15 files (26->25 imports). See tests/unit/b-truth-eligibility-gate-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): 4 callers. F-03/F-04 dependency-blocker resolution (2026-09-02): its two exported data contracts, EligibilityClassificationInput and EligibilityClassificationResult, moved to the neutral @/lib/types module (needed there to make PipelineAnalyzedRow, itself relocated for the F-03 fix, self-contained). This service re-exports both names for backward compatibility with its existing importers (IngestionPipelineService.ts, IngestionSimulatorService.ts) — no behavior change, pure type relocation. B-TRUTH Ingestion Pipeline Retirement (2026-09-03): IngestionPipelineService.ts deleted (unrelated retirement, its own bounded PR) — this service\'s only real value-level caller (classifyAction()) is gone, real caller count drops to 0 (fully orphaned by caller count). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains CANONICAL; its own future disposition remains a separate, later decision.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.equity-access', domain: 'Intelligence', primaryPath: 'services/equity-access/EquityAccessIntelligenceService.ts', purpose: 'EQ-family component explainability.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '6 callers.', deletableWhen: null },
  { id: 'svc.evidence-reliability', domain: 'Intelligence', primaryPath: 'services/evidence-reliability/EvidenceReliabilityIntelligenceService.ts', purpose: 'CS/VR evidence-gap explainability.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '8 callers.', deletableWhen: null },
  { id: 'svc.executive-intelligence', domain: 'Admin', primaryPath: 'services/executive-intelligence/ExecutiveIntelligenceService.ts', purpose: 'Executive-layer synthesis (B77-B).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '4 real callers (functional export computeExecutiveIntelligence — company/kora-index page, decision-pack pdf-data, ExecutiveIntelligencePanel, test).', deletableWhen: null },
  { id: 'svc.explainability', domain: 'Methodology', primaryPath: 'services/explainability/ExplainabilityService.ts', purpose: 'Live methodology glossary (21-concept, static, Italian-first) — getConceptExplanation/listConceptKeys.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Explainability Synthetic Retirement (2026-09-02)', notes: 'Prior note (pre-retirement) claimed "3 callers (ExplainabilityPanel.tsx, MethodologyGlossary.tsx — both real, live...)" — CORRECTED here: independently re-verified, ExplainabilityPanel.tsx is NOT actually rendered/imported anywhere (zero real callers of the component itself; the prior claim conflated a type-only import with real reachability). Only MethodologyGlossary.tsx is a confirmed real, live caller (getConceptExplanation() only). RETIREMENT (2026-09-02): removed the synthetic-backed branch (getExplanation, getTopWeakComponents, getTopStrongComponents, getNextBestActions, getLimitations, getWarnings — all confirmed zero real callers, including after ReportGeneratorService\'s own separate retirement removed their last real-ish consumer) and its sole synthetic import, data/synthetic/explainability-records.json (deleted — confirmed zero real consumers; a nominal, non-functional string-literal reference in the final-scoring-group-protected DemoDataService.ts\'s unwired SeedResourceType stub was the only other mention, left untouched). The live glossary (CONCEPT_GLOSSARY, getConceptExplanation, listConceptKeys, ConceptExplanation) is unchanged, no synthetic dependency, MethodologyGlossary.tsx unaffected. ExplainabilityComponentRef/ExplainabilityAction/ExplainabilityRecord kept as pure type declarations (smallest safe change) since ExplainabilityPanel.tsx still has a type-only import of ExplainabilityRecord, even though that component is itself unreachable. Status stays CANONICAL — the live glossary is real, canonical, and unaffected by this retirement; this was a partial-file retirement, not a service retirement. See tests/unit/b-truth-explainability-synthetic-retirement.test.ts for the regression guard. Ninth genuine I9 reduction via a real caller migration (eighth counting the file itself, per the allowlist header): 18->17 files (28->27 imports).', deletableWhen: null },
  { id: 'svc.financial-governance', domain: 'Financial', primaryPath: 'services/financial-governance/FinancialGovernanceService.ts', purpose: 'HISTORICAL — informational-only financial governance — no payment execution, no Index feed.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH FinancialGovernance Retirement (2026-09-02)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). 1 caller (ReportGeneratorService). B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted — real caller count dropped to 0 (fully orphaned). RETIREMENT (2026-09-02, separate bounded PR): independently re-verified before deletion — zero real runtime callers, zero type-only callers (its locally-defined types — PillarBudgetLine, BTIIndicators, FinancialGovernanceRecord, FinancialGovernanceResult, BudgetSummary — had zero external importers; a same-named but unrelated PillarBudgetLine already exists independently in lib/types/index.ts, confirmed not the same type and not imported from this file). No unique methodology: the service was a pure synthetic-JSON-to-interface mapper (own fields self-labeled informational_only/no_payment_execution/no_fund_custody), never computing anything. Its BTI-shaped output (economic_relief_spend, deep_activation_spend, activation_debt_eur, bti_score, budget totals) is a near 1:1 conceptual match for the real, already-canonical BTI Engine output (analytics.bti_result, read directly by lib/decision-pack/pdf-data.ts) — no capability lost, no methodology migrated because none was uniquely owned. Its sole seed file, data/synthetic/financial-governance.json, deleted alongside it — confirmed zero other consumers before deletion. See tests/unit/b-truth-financial-governance-retirement.test.ts for the regression guard proving both the deletion and the scope boundary (ReportFactoryService, ExplainabilityService, PreviewScoringAdapter, DynamicScoringPreviewService, the Ingestion/UEF legacy chain, and the final scoring group all explicitly untouched). Seventh genuine I9 reduction via a real caller migration: 19->18 files (29->28 imports).', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.founder-validation', domain: 'Admin', primaryPath: 'services/founder-validation/FounderValidationService.ts', purpose: 'Internal founder validation tool, tracks pilot validation signals.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '2 callers. CLAUDE.md-mandated internal/admin-only. CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05): explicitly EXCLUDED from this slice\'s in-scope worker/account cluster — this service tracks founder sales-pipeline/CRM data (leads, LOIs, objections, investment signals), not worker or account identity truth. It remains a genuinely separate I9 residual, unaddressed by this slice, not overlooked: a founder-CRM canonicalization question is a distinct, later decision, not this cluster\'s. No code was modified.', deletableWhen: null },
  { id: 'svc.ingestion-normalizer', domain: 'Ingestion', primaryPath: 'services/ingestion-normalizer/IngestionNormalizerService.ts', purpose: 'HISTORICAL — internal-only ingestion normalizer (raw row → structured normalization) for the legacy demo pipeline.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Ingestion Normalizer Retirement (2026-09-03)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). PRIOR HISTORY: "2 callers. Confirmed real chain: consumed by ingestion-pipeline." B-TRUTH Ingestion Pipeline Retirement (2026-09-03): IngestionPipelineService.ts deleted (unrelated retirement, its own bounded PR) — this service\'s sole real caller was gone, real caller count dropped to 0. RETIREMENT (2026-09-03): independently re-verified before deletion — all 6 IIngestionNormalizerService methods (normalizeRow, normalizeBatch, detectMissingFields, inferSourceType, inferMandatoryStatus, getMissingDataQuestions) individually confirmed zero real callers; zero type-only callers. Its own header always correctly scoped it as internal-only ("should not be imported directly in app/ routes or components") — this retirement is a straightforward orphan-of-an-already-retired-consumer, not a stale-claim correction. Its type family (RawIngestionRow, NormalizedIngestionRow) was confirmed to have zero other real consumers — the one apparent hit, a comment in services/iu-computation/IUComputationService.ts, is prose, not an import; the types themselves were deliberately NOT deleted in this PR (no opportunistic cleanup — they remain pure, harmless data contracts in @/lib/types). No unique live data semantics: the real, canonical live ingestion/data-intake path (MappingConfidenceService, lib/data-intake/missing-field-analysis.ts, classifyEligibilityBatch, operating on the canonical RawUploadedRecord type family) is confirmed entirely independent — it solves related problems (source classification, missing-field detection) via its own separate implementation, never reusing this file\'s logic. Never an I9 allowlist member (no direct data/synthetic/** import) — I9 unaffected by this retirement (16/26 unchanged). EligibilityGateService is explicitly untouched by this PR — remains its own separately-authorized future slice. See tests/unit/b-truth-ingestion-normalizer-retirement.test.ts for the regression guard proving both the deletion and the scope boundary.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.ingestion-pipeline', domain: 'Ingestion', primaryPath: 'services/ingestion-pipeline/IngestionPipelineService.ts', purpose: 'HISTORICAL — isolated demo ingestion pipeline that fed the demo UEF review path — NOT the live UEF ingestion path.', status: 'DEAD', futureCore: false, dependencies: ['svc.ingestion-normalizer'], competingWith: [], decisionRef: 'B-TRUTH Ingestion Pipeline Retirement (2026-09-03)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-03): independently re-verified before deletion — all 5 IIngestionPipelineService methods (analyzeRow, analyzeBatch, getIngestionSummary, getReviewQueue, getKoraReadyRecords) individually confirmed zero real callers; its last real-ish caller, UEFReviewService, was itself retired in the immediately preceding PR. No unique methodology: the private governance-flag derivation logic (deriveDestination/deriveReviewStatus/buildKoraReadyRecord) is a non-authoritative demo approximation of the same eligibility concept lib/kora-engine/eligibility-gate.ts\'s classifyEligibilityBatch already implements for real, live scoring — confirmed independent (zero references), RLS-16-proven correct for both tenant kinds without this file. Its sole seed file, data/synthetic/ingestion-samples.json, deleted alongside it — confirmed zero other real consumers (the two "demo data preserved" prose mentions on this entry and in the allowlist governed an earlier point when this exact retirement had not yet been authorized). Direct consequences (documented, not acted on): EligibilityGateService drops from 1 to 0 real callers (this file was its sole caller); IngestionNormalizerService drops from 1 to 0 real callers likewise. Neither modified nor retired by this PR — each remains its own separately-authorized future slice. demo-guard-01\'s substantive fallback-prohibition assertions (page never imports IngestionPipelineService, no ingestion-samples reference) are unchanged and unweakened; its ingestion-samples.json existence check was updated to reflect the new truth. See tests/unit/b-truth-ingestion-pipeline-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. IngestionPipelineService.ts was an I9 allowlist entry — tenth genuine I9 reduction via a real caller migration: 17->16 files (27->26 imports). PRIOR HISTORY (accurate as of its own time, preserved verbatim): B-TRUTH Retire Orphan Ingestion/UEF Chain (2026-08-31) attempted retirement of this service and STOPPED on finding it deliberately protected. Corrected caller count: 2 real callers — services/uef-review/UEFReviewService.ts (every method routes through getReviewRecords() -> ingestionPipelineService.analyzeBatch(), a hard dependency, not optional) and services/report-generator/ReportGeneratorService.ts (0 production callers itself). The prior "6 callers (..., IUComputationService, ...)" note was stale/wrong: services/iu-computation/IUComputationService.ts only imported the TYPE PipelineAnalyzedRow from this file, had zero value/method dependency on it, and is itself REACHABLE_CANONICAL via lib/kora-engine/run-kora-pipeline.ts — a completely separate, live concern from this demo chain. F-03 dependency-blocker resolution (2026-09-02): that type-only import has now been removed entirely — PipelineAnalyzedRow (and its own EligibilityClassificationResult/Input dependency) moved to the neutral @/lib/types module; IngestionPipelineService.ts re-exports PipelineAnalyzedRow from there for its one remaining real importer (UEFReviewService.ts). IUComputationService.ts has ZERO import of any kind from this file as of this resolution — confirmed by direct grep, not merely type-erasure reasoning. B-TRUTH Ingestion/UEF Classification (2026-08-31): reclassified CANONICAL -> FROZEN (matching the svc.admin-preview precedent for "intentionally demo-only, not evolving"). Zero canonical app/ path imports this service directly — reachability alone would suggest ORPHAN — but tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts is a pre-existing, deliberately authored, currently-passing regression guard proving this is NOT ordinary dead code: it documents a real historical bug (DEMO-DEP-RO) where app/company/kora-index/page.tsx silently fell back to this chain\'s synthetic data when its live fetch was loading/failed, blending demo numbers into a real, session-authenticated company page. That bug is fixed (the live page has zero reference to either service, confirmed) and the test explicitly asserts UEFReviewService.getReviewSummary must NOT be removed, DynamicScoringPreviewService and ReportGeneratorService may keep using it as "isolated demo-only" consumers, and data/synthetic/ingestion-samples.json must NOT be deleted ("demo data preserved"). Invariant this chain\'s isolation enforces: LIVE MUST NEVER FALL BACK TO DEMO UEF DATA. Not retired; not deletable without first revisiting demo-guard-01\'s own explicit test assertions, which is a separate decision from this classification pass. Governance Patch 01 (2026-08-31): status: FROZEN here is a pragmatic reuse of the nearest existing ArchitectureStatus value, not a claim that Master Plan §33\'s official FROZEN definition ("lo sviluppo è fermo, non che la capability sia indisponibile" — development stopped, not unavailable) applies; this component is actively used as a demo data source, not stopped. No dedicated DEMO_RUNTIME status exists in the enum; that gap is recorded for a future, explicitly-authorized schema change, not created here. Also per Patch 01 §3: DEMO_RUNTIME classification here is transitional within the B-TRUTH window, not a permanent exception to the I9=0 / CC-022 closure target. B-TRUTH Ingestion/UEF PR1 (2026-09-02, RLS-16, tests/integration/rls-16-ingestion-tenant-kind-parity.test.ts): PROOF, not retirement. Confirmed for real against local Postgres that a canonical synthetic/example tenant (tenant_kind DEMO, ordinary analytics.tenant row, no OP-001 involvement) can traverse the exact same canonical Ingestion/UEF runtime a LIVE-kind tenant uses — analytics.source_batch, personal.uploaded_record, analytics.uef_record, classifyEligibilityBatch (lib/kora-engine/eligibility-gate.ts, canonical), buildScoringRecordsFromApprovedUef (lib/live/uef-to-scoring-records.ts), runKoraPipeline (unchanged) — and reaches identical material pipeline output (scoringMode, eligibilitySummary, pillarDistribution, koraIndex.value/macroblocks) to a LIVE-kind tenant fed the same deterministic fixture, with matching negative tenant isolation. This is the evidence base for a future PR2 retirement decision on this cluster — it does NOT itself retire, canonicalize, or reclassify svc.ingestion-pipeline/svc.uef-review/svc.ingestion-simulator/svc.eligibility-gate/svc.report-generator, none of which this PR touches. Status remains FROZEN. B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted — real caller count drops from 2 to 1 (UEFReviewService.ts only, still the hard dependency). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains FROZEN, demo-guard-01\'s protection unchanged. B-TRUTH UEFReview Retirement (2026-09-03): CORRECTION — the "the test explicitly asserts UEFReviewService.getReviewSummary must NOT be removed... not deletable without first revisiting demo-guard-01\'s own explicit test assertions" framing above was found to be STALE at retirement time: it described demo-guard-01\'s wording before PR #131 (2026-09-02) rewrote it; the current guard\'s substantive assertions read app/company/kora-index/page.tsx\'s source, not UEFReviewService.ts\'s. services/uef-review/UEFReviewService.ts deleted separately (its own bounded PR) — this file\'s real caller count drops from 1 to 0 (fully orphaned by caller count). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains FROZEN, demo-guard-01\'s protection unchanged and unweakened (verified at retirement time). See svc.uef-review for the full corrected record.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.ingestion-simulator', domain: 'Ingestion', primaryPath: 'services/ingestion-simulator/IngestionSimulatorService.ts', purpose: 'HISTORICAL — rule-based BCM classifier demo path, no LLM, parallel entry point not chained to ingestion-pipeline. Retired.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Ingestion/UEF PR2 — IngestionSimulatorService retirement (2026-09-02)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). Prior "5 callers" note was stale/unverified at time of retirement — re-confirmed by direct repo-wide grep immediately before deletion: ZERO real value/method-level callers anywhere (`ingestionSimulatorService.` matched nothing outside its own file), and exactly ONE real type-only dependency (services/mapping-confidence/MappingConfidenceService.ts imported SourceType from it, feeding the real live app/api/admin/data-intake/upload-preview/route.ts). F-04 dependency-blocker resolution (2026-09-02, prior PR) had already moved EligibilityGateSummary to @/lib/types for the same reason (3 real, live product UI components — EligibilityGatePanel/EligibilitySummaryReport/ActionPlanReport — importing it for type only, already fed real data from liveCtx.eligibility, never from this service). This PR additionally moved SourceType to @/lib/types (kept deliberately distinct from the pre-existing IngestionSourceType there — different value sets, \'lms\' vs \'lms_training\' — merging them would have silently changed MappingConfidenceService\'s real sourceBonus() switch-case behavior) and fixed a duplicate-EligibilityGateSummary-interface bug the F-04 move had accidentally introduced (a second copy alongside an already-existing 2026-05-23 original in lib/types/index.ts, silently tolerated by TypeScript interface merging). data/synthetic/source-batches.json and data/synthetic/ingestion-samples.json are NOT deleted — both remain legitimately imported by other, non-retired files (AdminPreviewService.ts and IngestionPipelineService.ts respectively; the latter is also explicitly protected by tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts). Proof basis: RLS-16 (tests/integration/rls-16-ingestion-tenant-kind-parity.test.ts, Ingestion/UEF PR1) — confirmed a canonical synthetic tenant already traverses the real Ingestion/UEF runtime and runKoraPipeline authority without this service. This is a bounded, single-surface retirement — svc.ingestion-pipeline, svc.eligibility-gate, svc.uef-review, svc.report-generator, svc.dynamic-scoring, svc.company-data-intake, svc.scoring-simulator, and the final scoring group are explicitly NOT touched by this decision and remain their own, separately-verified future slices.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.iu-computation', domain: 'Methodology', primaryPath: 'services/iu-computation/IUComputationService.ts', purpose: 'Canonical IU formula implementation (NM×BC×CQ×EV×CF×AGF), demo + live pipeline paths.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: ['svc.worker-iu-computation'], decisionRef: null, notes: '16 callers. This is the CC-002 I7 golden-case protected component. Overlap with worker-iu-computation not resolved. B-BC (CC-009): BC per action family moved to lib/methodology-config/v0.1.ts (getBCByActionFamily()) — no longer a local literal; I7 golden IU outputs unchanged (verified, 31/31 pass).', deletableWhen: null },
  { id: 'svc.kora-contribution', domain: 'Contribution', primaryPath: 'services/kora-contribution/KoraContributionService.ts', purpose: 'KORA Contribution computation — companion indicator, kept separate from KORA Index.', status: 'CANONICAL', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'B-TRUTH Contribution Protected Port (2026-09-01): synthetic-seed input retired (getContribution/getContributionSummary/getContributionScore/getCollectiveInitiatives/getContributionInitiatives/getSummaryV2 removed, 0 real callers each confirmed by grep before removal); the sole real caller of the retired getSummaryV2 (app/company/contribution/page.tsx) is rewired onto the new async getContributionV2Live(), DB-backed via commons.contribution_event + commons.post (lib/kora-contribution/contribution-pipeline-input.ts). The protected methodology authority (computeContributionV2 / computeProvisionalScore / computeFromPipelineResult) is byte-for-byte unchanged — only its input source moved. 3 real (non-test) importers verified by grep: app/company/contribution/page.tsx, app/api/company/contribution/live/route.ts, lib/kora-contribution/contribution-pipeline-input.ts. Master Plan §33 Do-Not-Delete. The prior "competingWith lib.kora-contribution" claim was corrected by the B-TRUTH Contribution audit (2026-09-01): lib/kora-contribution/contribution-methodology.ts is pure doctrine/policy constants (zero computation, zero competing implementation) — see lib.kora-contribution below.', deletableWhen: null },
  { id: 'svc.life-diversity', domain: 'Intelligence', primaryPath: 'services/life-diversity/LifeDiversityService.ts', purpose: 'LIFE pillar subcategory diversity interpretation.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '8 callers.', deletableWhen: null },
  { id: 'svc.lifecycle', domain: 'Admin', primaryPath: 'services/lifecycle/LifecycleService.ts', purpose: 'Session-only demo audit log shadow.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH Root Control Room Wave 2 (2026-08-30)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). Real audit.audit_log was always a separate, live mechanism — this shadow reached zero runtime callers when its sole consumer (Root Control Room Section J) was retired, and Lifecycle/Audit moved to the Gen 3 workspace tab reading the real table, tenant-scoped, behaviorally verified. Its seed file (data/synthetic/lifecycle-audit.json) was deleted alongside it — it had no other consumer.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.mapping-confidence', domain: 'Ingestion', primaryPath: 'services/mapping-confidence/MappingConfidenceService.ts', purpose: 'AI-ingestion column-mapping confidence — distinct concept from KORA Index Confidence Score.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '2 callers.', deletableWhen: null },
  { id: 'svc.my-kora-preview', domain: 'Worker', primaryPath: 'services/my-kora-preview/MyKoraPreviewService.ts', purpose: 'Worker preview-mode (PREVIEW surface) data.', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-024 / D-D (at the app-surface level, not this service)', notes: '14 callers.', deletableWhen: null },
  { id: 'svc.pib-aggregation', domain: 'PIB', primaryPath: 'services/pib-aggregation/PIBAggregationService.ts', purpose: 'Stage 11 of the 14-stage algorithm: PIB = ΣIU per worker.', status: 'CANONICAL', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: '3 callers. Master Plan §33: prerequisite for canonical CF.', deletableWhen: null },
  { id: 'svc.privacy-visibility', domain: 'Privacy', primaryPath: 'services/privacy-visibility/PrivacyVisibilityService.ts', purpose: 'Suppression / N≥10 gatekeeper.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'after B-INV', notes: '1 caller. Master Plan §32: "dopo B-INV, I2 copre lo strato canonico" — scheduled for likely removal once I2 canonically covers this, per the Master Plan itself.', deletableWhen: null },
  { id: 'svc.report-factory', domain: 'Reporting', primaryPath: 'services/report-factory/ReportFactoryService.ts', purpose: 'HISTORICAL — demo Decision Pack status check for the Pilot Lifecycle orchestrator (app/admin/pipeline).', status: 'DEAD', futureCore: false, dependencies: [], competingWith: ['svc.report-generator', 'lib.decision-pack'], decisionRef: 'B-TRUTH ReportFactoryService Canonical Decision Pack Status Migration (2026-09-06)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). PRIOR HISTORY (accurate as of its own time, preserved verbatim): D-B resolved (CC-005/CC-013): lib.decision-pack is canonical; ReportFactoryService never becomes a second authority. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30) removed its only other caller (root page.tsx) — 1 real caller remains, app/admin/pipeline/page.tsx (a demo caller — hardcoded DEMO_COMPANY_ID). B-TRUTH Demo/Orphan Chain Audit (2026-08-30): reachability trim — verified per-method, repo-wide, that pipeline calls exactly one method (getDecisionPackFactoryStatus); removed 10 other public methods (version history, generation, readiness, sections, export actions, change summary, period comparison, metric deltas, limitations, previous-comparable-version) plus 1 dead private method (isTenantActive) that had zero callers anywhere — not from pipeline, not internally, not from any test (guard-style "not.toContain" assertions only, unaffected). Kept: getDecisionPackFactoryStatus (public, reachable) and getLatestDecisionPackVersion (public, called only internally). Still 100% synthetic (data/synthetic/decision-pack-versions.json, zero DB queries) — that divergence is unchanged, just narrowed to the single reachable method; not a One-Truth violation because the demo tenant (DEMO_COMPANY_ID) has no live analytics.tenant row to read canonical data from in the first place. F-05 dependency-blocker audit (2026-09-02): confirmed by direct grep that ReportFactoryService.ts has NO import of any kind (value or type) from services/report-generator/ReportGeneratorService.ts. CC-005/D-B CLOSEOUT (2026-09-02): this file\'s 2 remaining public methods (getDecisionPackFactoryStatus, getLatestDecisionPackVersion) have no canonical replacement needed (demo-only output, no unique methodology). B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted separately — this file was explicitly untouched at that time, still pending its own future retirement decision. B-TRUTH TenantService Canonical Migration (2026-09-04): getDecisionPackFactoryStatus/computeBlockingReasons gained a CanonicalTenantStatus ({id, isActive} | null) parameter, replacing an internal tenantService.getTenant() call — still-synthetic hasKoraIndex/getLatestDecisionPackVersion checks unchanged. B-TRUTH CompanyDataIntakeService Canonical Migration (2026-09-05): gained a third parameter, dataIntake: CanonicalDataIntakeStatus, replacing an internal companyDataIntakeService dependency — Decision Pack version source still unmigrated, explicitly deferred to PR 4. RETIREMENT (2026-09-06, B-TRUTH ReportFactoryService Canonical Decision Pack Status Migration, PR 4 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan): independently re-verified before deletion — the sole real runtime caller, app/admin/pipeline/_components/PilotLifecycleClient.tsx, was traced field-by-field and found to read exactly ONE field, latest_status ("=== \'ready\'"), out of the legacy 9-field DecisionPackFactoryStatus return shape. Per this migration\'s "map only what the canonical model actually supports, do not fake 1:1 parity with legacy synthetic fields" rule, the blocking_reasons/warnings/next_action/can_generate apparatus (built from a private hasKoraIndex() call into the still-synthetic ScoringSimulatorService demo path) was DROPPED, not migrated — it never had a real consumer. Final scoring is untouched by this migration, not because a canonical replacement was avoided, but because nothing downstream ever needed hasKoraIndex()\'s result. Replacement: a new shared pure view builder, lib/live/decision-pack-status-view.ts, reading directly from analytics.decision_pack_version (status, created_at), fetched once by app/admin/pipeline/page.tsx (Server Component) and passed down as a decisionPack prop — same fetch-once-thread-down discipline as PR 2/PR 3. Version-selection rule: latest by created_at, matching lib/live/data-intake-status-view.ts\'s own precedent and the operator-flow route\'s GET handler for the same table — not invented for this migration. No tenant_kind branch introduced; view is identical for KoraTest Srl (tenant_kind=TEST) and any tenant_kind=LIVE tenant. CanonicalTenantStatus needed no relocation — repo-wide grep confirmed zero remaining real or type-only importers once the getDecisionPackFactoryStatus call site was removed, so the type was deleted along with the file, not moved. Its sole seed file, data/synthetic/decision-pack-versions.json, deleted alongside it — confirmed zero other consumers before deletion. AccountProvisioningService, AdminPreviewService, and B-WORKER are explicitly untouched — one PR = one bounded migration. See tests/unit/b-truth-reportfactory-canonical-decision-pack-status.test.ts for the regression guard proving both the deletion and the scope boundary.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.report-generator', domain: 'Reporting', primaryPath: 'services/report-generator/ReportGeneratorService.ts', purpose: 'HISTORICAL — report content generation — Decision Pack body, sections, metrics, insights, recommendations.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: ['svc.report-factory', 'lib.decision-pack'], decisionRef: 'B-TRUTH ReportGenerator Retirement (2026-09-02)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). D-B resolved (CC-005/CC-013, ratified in PR #131): lib.decision-pack is canonical, ReportGeneratorService was NOT. CORRECTED CALLER COUNT (established pre-retirement): the previous "3 real callers (IScoringService.ts, PreviewScoringAdapter.ts, +1 test)" note was stale — CC-005 verified by direct import-statement search that this was actually ZERO real callers; those two files only mentioned it in comments (never imported it), and the one test did static source-string analysis, never instantiated it. Master Plan §32/§33 stated explicitly, twice: "restano INVESTIGATE" — that classification governed the standing default absent a specific founder decision; this task (B-TRUTH ReportGenerator Retirement, 2026-09-02) is exactly that founder-authorized decision (Master Plan §30\'s "One Truth per gruppo di seed... Sì per gruppo" per-group approval mechanism), independently re-verified before deletion: zero real runtime callers (static, dynamic, barrel, and type-only imports all checked, repo-wide), all 7 capabilities\' disposition re-confirmed unchanged from PR #131. RETIREMENT (2026-09-02): deleted generate(), getCurrentCompanyDecisionPack(), getDecisionPackVersionHistory(), getDecisionPackExportActions(), and the generateCompanyDecisionPack() assembly along with the rest of the file — all 4 RETIRE + 1 RETIRE-as-assembly items from the PR #131 disposition. getDecisionPackLimitations() required no porting (already present in lib.decision-pack, proven by tests/unit/cc005-decision-pack-limitations-migration.test.ts). getDecisionPackReadiness()\'s DEFERRED PRODUCT REQUIREMENT — see the CC-005 CLOSEOUT / D-B RESOLUTION header block above — survives this deletion verbatim in that governance text; no runtime implementation was added. Sole exported type (ReportType) had zero external importers, confirmed by repo-wide grep before deletion — nothing needed relocation. ReportGeneratorService.ts was never itself an I9 allowlist entry (it consumed synthetic data only transitively, via ScoringSimulatorService/IngestionPipelineService/etc., each separately allowlisted) — I9 is unaffected by this retirement (19/29 unchanged). ReportFactoryService, the Ingestion/UEF legacy chain (IngestionPipelineService, EligibilityGateService, UEFReviewService, DynamicScoringPreviewService, PreviewScoringAdapter, ExplainabilityService, FinancialGovernanceService), and the final scoring group are explicitly untouched by this PR — see tests/unit/b-truth-report-generator-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. Direct consequence of this deletion (documented, not acted on): IngestionPipelineService drops from 2 to 1 real caller (UEFReviewService only); UEFReviewService drops from 2 to 1 real caller (DynamicScoringPreviewService only); ExplainabilityService drops from 3 to 2 real callers (both live-safe, static-glossary-only — its synthetic-backed getExplanation() now has zero callers anywhere); FinancialGovernanceService drops from 1 to 0 real callers (now fully orphaned). None of these 4 services were modified or retired by this PR — each remains its own separately-authorized future slice.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.role-permission', domain: 'Privacy', primaryPath: 'services/role-permission/RolePermissionService.ts', purpose: 'Role-based access gatekeeper, CLAUDE.md-mandated.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '2 callers — low relative to "mandatory gatekeeper" status, worth CC-002/I1 follow-up scrutiny.', deletableWhen: null },
  { id: 'svc.scenario', domain: 'Demo', primaryPath: 'services/scenario/ScenarioService.ts', purpose: 'S1–S4 demo scenario switching.', status: 'FROZEN', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '4 callers, demo-only.', deletableWhen: null },
  { id: 'svc.scoring-simulator', domain: 'Scoring', primaryPath: 'services/scoring-simulator/ScoringSimulatorService.ts', purpose: 'Demo-path scoring — reads precomputed synthetic seed.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH', notes: '21 callers (highest usage in this registry) yet Master Plan §32 explicitly schedules removal at end of B-TRUTH — high current usage does not make it the architectural target.', deletableWhen: null },
  { id: 'svc.scoring.facade', domain: 'Scoring', primaryPath: 'services/scoring/IScoringService.ts', purpose: 'Demo/Preview/Live scoring adapter contract — the enforced single interface behind lib/scoring-result.', status: 'CANONICAL', futureCore: false, dependencies: ['svc.scoring-simulator', 'svc.dynamic-scoring'], competingWith: [], decisionRef: null, notes: 'Consumed only via lib/scoring-result/index.ts (useScoringResult). CC-002 found 6 real bypasses of this facade — see I5/M-02 notes, not fixed in CC-002/CC-003.', deletableWhen: null },
  { id: 'svc.submission-feedback', domain: 'Demo', primaryPath: 'services/submission-feedback/SubmissionFeedbackService.ts', purpose: 'Mock aggregate feedback, demo only (own header: "Gate 2 open — no real DB access").', status: 'FROZEN', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '3 callers.', deletableWhen: null },
  { id: 'svc.tenant', domain: 'Core', primaryPath: 'services/tenant/TenantService.ts', purpose: 'HISTORICAL — tenant/organization record access, superseded by canonical analytics.tenant reads. Retired.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'B-TRUTH TenantService Canonical Migration (2026-09-04)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-04), PR 2 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan (PR 1 = B-TRUTH KoraTest Canonical Foundation, 2026-09-03/04): independently re-verified before deletion — all 3 real runtime callers (app/admin/pipeline/page.tsx, components/admin/WorkforceQuickAccessPanel.tsx, services/report-factory/ReportFactoryService.ts) individually confirmed, zero type-only callers. Each migrated onto a canonical analytics.tenant read: app/admin/pipeline/page.tsx became a thin async Server Component (PILOT_LIFECYCLE_TENANT_CODE = \'KORATEST-01\', an ordinary tenant_code lookup, no special-case branching), rendering the new app/admin/pipeline/_components/PilotLifecycleClient.tsx (every other step on that page — worker provisioning, account provisioning, scoring, data intake — is UNCHANGED, still keyed by the pre-existing DEMO_COMPANY_ID = \'meridiana-group\' constant, a separate, later migration slice); WorkforceQuickAccessPanel.tsx now receives its tenant list as a prop from its already-async parent (app/admin/companies/page.tsx), which queries analytics.tenant with no tenant_kind filter (no hidden test tenants); ReportFactoryService.getDecisionPackFactoryStatus/computeBlockingReasons now accept an already-fetched CanonicalTenantStatus ({ id, isActive } | null) parameter instead of calling tenantService.getTenant() themselves, reusing the same canonical read app/admin/pipeline already performs rather than duplicating it, and avoiding making a previously-synchronous method async merely to satisfy an internal dependency; ReportFactoryService\'s own still-synthetic hasKoraIndex/getIntakeStatus/getLatestDecisionPackVersion checks are UNCHANGED, unmigrated, explicitly out of scope (a separate, later slice). No tenant_kind product branch was introduced anywhere in this migration. Its sole seed file, data/synthetic/tenants.json, deleted alongside it — confirmed zero remaining real consumers (3 remaining text hits were governance-comment prose, not imports). Its type family (KoraTenant, KoraTenantStatus, TenantReadiness, ReadinessItemStatus, defined in @/lib/types, not by this file) was deliberately NOT deleted — no opportunistic cleanup, services/worker-space/WorkerSpaceCapabilityService.ts still legitimately imports it, unrelated to this migration, explicitly out of scope (B-WORKER territory). AdminPreviewService, CompanyDataIntakeService, and AccountProvisioningService are explicitly untouched — one PR = one bounded migration; their own future disposition remains separate, later decisions. TenantService.ts was an I9 allowlist entry — eleventh genuine I9 reduction via a real caller migration: 15->14 files (25->24 imports). See tests/unit/b-truth-tenantservice-canonical-migration.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): Competing surfaces are not individually registered (covered under app-surface.admin, "not individually traced per-subdirectory"): app/admin/companies/_components/CompanyConsolePanel.tsx (via /api/admin/company-console), app/admin/company-workspace-live, app/admin/company-users-live. B-TRUTH Tenant Identity read-path audit (2026-08-30): status corrected from the prior CANONICAL — TenantService is 100% synthetic (data/synthetic/tenants.json, zero DB queries); the prior "CANONICAL" classification and "24 callers" count were both stale/wrong. Fresh caller count via direct import search: 15 real callers as of the original audit. B-TRUTH Gen 3 route identity activation (2026-08-30) removed 4 of them — app/admin/companies/[companyId]/{workspace,preview,evidence,submissions}/page.tsx — which were already fully DB-backed (Gen 3, B168.5/B171 consolidation) except for this one stale TenantService existence-gate call; each now queries analytics.tenant directly by tenant_code (no synthetic fallback, is_active=true, behaviorally tested against unknown/synthetic-only ids and DB-error paths). 11 real callers remain, ALL nested under app/admin/companies/[companyId]/* (6 files: layout.tsx, page.tsx, onboarding, data-intake, users, workforce — the "Gen 0/1" pages, 2 entangled services: ReportFactoryService, CompanyIntelligenceService) or driven by a hardcoded DEMO_COMPANY_ID = \'meridiana-group\' constant (app/admin/pipeline/page.tsx), or the demo-labeled WorkforceQuickAccessPanel/workforce-baseline admin page — none of these 11 are safely migratable in isolation without either breaking navigation or creating mixed real/synthetic identity on the same page (Phase 8/9 guard). A real, working, DB-backed competing read path already exists and is production-verified: analytics.tenant queried directly by /api/admin/tenants, /api/admin/company-console (feeding CompanyConsolePanel, explicitly "Operational live tenant registry", filtered to tenant_kind=LIVE), /api/admin/tenants/[id]/promote-to-pilot, and lib/decision-pack/pdf-data.ts — none of these import or depend on TenantService. Route-identity finding, CORRECTED by the Admin Route Convergence audit: app/admin/companies/[companyId]/* is not uniformly non-canonical. Four sub-routes (workspace/preview/evidence/submissions — see above) treat [companyId] as tenant_code and are canonical; the remaining Gen 0/1 pages still treat it as an arbitrary tenants.json key ("meridiana-group") with no resolvable mapping to tenant.id/tenant_code. A separate, second live surface also exists at flat query-param routes (app/admin/company-workspace-live, app/admin/company-users-live) for a different use case (post-provisioning entry with a tenantId uuid) — this is pre-existing architectural duplication, not something introduced here, and not yet reconciled with the Gen 3 [companyId] pattern. Also found (B-TRUTH Admin Route Convergence audit): /api/admin/company-console\'s quickActions had 4 links (viewWorkspace, evidenceArchive, livePreview, submissions) pointing at five flat routes removed by B171 — fixed as part of Gen 3 activation, now pointing at /admin/companies/{tenant_code}/{workspace|evidence|preview|submissions}. quickActions.manageUsers remains unfixed/still broken (/admin/company-users?tenantId=...) — its canonical destination is still architecturally unresolved (app/admin/companies/[companyId]/users is still 100% synthetic; app/admin/company-users-live is read-only and its own header comment claiming B171 consolidation into [companyId]/users is itself stale/incorrect) — left untouched per explicit scope guard. Mutation methods (activate/suspend/archive/restore/deleteDemoTenant) remain pure demo stubs with zero backing implementation in analytics.tenant (no lifecycle state machine column exists at all — only is_active boolean + deleted_at soft-delete + tenant_kind classification) — explicitly out of scope (Phase 2 of this consolidation, not authorized here). B-TRUTH Gen 0/1 Retirement Wave 1 (2026-08-30): retired 3 of the remaining 11 callers outright — app/admin/companies/[companyId]/{onboarding,data-intake,workforce}/page.tsx were 100% read-only synthetic dashboards (workforce additionally had a real-looking createDemoWorker mutation, explicitly a demo stub, no DB writes) with no unique capability beyond real live surfaces (real upload: /admin/data-intake; real worker provisioning: /admin/workers, B104; real per-tenant pipeline status: the Workspace tab) — deleted, not redirected to a fake equivalent, matching the B171 precedent. CompanyTabNav reduced from 8 to 5 tabs. All dangling links updated to real destinations (root Control Room, users/page.tsx, admin pipeline page, WorkforceQuickAccessPanel, lib/workforce/workforce-rules.ts getWorkforceRoute — now always /admin/workers). 8 real callers remain: layout.tsx, page.tsx (root Control Room), users/page.tsx, ReportFactoryService, CompanyIntelligenceService, WorkforceQuickAccessPanel, workforce-baseline admin page, app/admin/pipeline/page.tsx (DEMO_COMPANY_ID) — still none safely migratable (root Control Room retains required capabilities — BTI display, user-lifecycle mutations, Decision Pack period comparison — with no canonical replacement; see Phase 1 findings). No service caller graph (LifecycleService, ReportFactoryService, CompanyIntelligenceService, CompanyOnboardingService, WorkforceBaselineService, AccountProvisioningService, BudgetToHumanImpactService, WorkerProvisioningService, CompanyDataIntakeService) reached zero callers — root page.tsx alone keeps each one alive — so I9 is unchanged (26 files/38 imports); this wave removed parallel-truth callers, not synthetic import statements. components/admin/RosterImportModal.tsx is now orphaned (its sole caller, workforce/page.tsx, was deleted) — left in place, not deleted (zero callers ≠ dead, per this same registry\'s own discipline). B-TRUTH Root Control Room Wave 2 (2026-08-30): decomposed root page.tsx capability-by-capability. REMOVED (duplicate of Gen 3 workspace, which already reads real analytics.kora_index_result/decision_pack_version): KORA Index Output (Section E + Tile 6), Decision Pack Factory status (Section G). RETIRED, not migrated (no canonical source — analytics.decision_pack_version has no previous-version linkage): Decision Pack period comparison. CANONICALIZED (moved to the Gen 3 workspace tab as new minimal persisted reads, no recomputation): BTI (analytics.bti_result, fields with no real column honestly omitted rather than fabricated) and Lifecycle/Audit (audit.audit_log, tenant-scoped by the real tenantId already resolved on that tab) — both behaviorally tested for tenant isolation. REMOVED as fake-mutation UI with no real backend, not migrated (Section H Access & Users; the worker roster mutation table in Section I): user invite/revoke/reset/disable/delete and worker invite/disable/delete-demo controls are gone; Section H is now a link to the retained, already read-only /users tab; Section I keeps only its aggregate summary tiles. accountProvisioningService, scoringSimulatorService, budgetToHumanImpactService, reportFactoryService, lifecycleService are no longer imported by root page.tsx. Caller graph after: TenantService 8 (unchanged — tenant/header/Section B/C still needed), ReportFactoryService 1 (pipeline only), AccountProvisioningService 3 (pipeline, users, my-kora), BudgetToHumanImpactService 4, CompanyIntelligenceService 1 (unchanged, header risk badge), CompanyOnboardingService 1 (unchanged), CompanyDataIntakeService 4 (unchanged, Section D retained), WorkerProvisioningService 7 (unchanged, summary tiles retained), **LifecycleService 0 — fully orphaned**. LifecycleService.ts and its sole seed file (data/synthetic/lifecycle-audit.json) were deleted this wave (not just left orphaned): zero remaining runtime callers, live replacement verified working, satisfying Master Plan §32\'s own rule that a synthetic path may be removed once its live migration is complete and verified. I9 reduced 26→25 files (38→37 imports) — the first genuine I9 reduction since CC-018. Root page.tsx remaining gaps (still real, still undecomposed further this wave): Tenant Overview profile fields (Section B — no analytics.tenant columns), Operational Readiness tiles reading the same fake tenant/CompanyIntelligenceService risk aggregation, Data Intake summary (Section D), worker roster summary tiles (Section I). Root Control Room is smaller but still a real synthetic-identity-rooted page — not marked canonical. B-TRUTH Root Control Room Wave 3 (2026-08-30): removed Section B (Tenant Overview, RETIRE_LEGACY — 9/12 fields have no analytics.tenant column and no confirmed product requirement, not extending the schema to preserve demo CRM richness) and Section C Tiles 2/7/8 (Onboarding/Decision Pack/Advisor — REMOVE_DUPLICATE/RETIRE_LEGACY) and Section D (Data Intake summary — REMOVE_DUPLICATE against the Gen 3 workspace tab\'s latestBatch/uef fields). companyDataIntakeService is no longer imported by root page.tsx (2 real callers remain elsewhere: app/admin/pipeline, ReportFactoryService/CompanyIntelligenceService — not zero, not deleted). Worker summary (Section I, Tiles 4/5) and CompanyIntelligenceService (header risk badge) left untouched — RETAIN_REQUIRED_GAP and INVESTIGATE respectively; worker data is >80% B-WORKER-gapped with no real equivalent, and CompanyIntelligenceService aggregates 6 synthetic services under a heuristic with no defined current-product methodology, failing all 4 of Phase 7\'s canonicalization criteria. I9 unchanged (25 files/37 imports) — no service reached zero callers this wave. `ROOT_CONTROL_ROOM_FUTURE = KEEP_TEMPORARILY` (Wave 3\'s own conclusion) — SUPERSEDED same day by the Root Control Room Wave 3 HARDENING pass. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30): re-evaluated Wave 3\'s KEEP_TEMPORARILY conclusion against the governing principle "a missing canonical capability is a product gap, not justification for keeping synthetic product truth in runtime." All three remaining outputs failed it — Tile 1 (Tenant Status: TenantService.tenant_status, no canonical state machine exists), Section I (Worker Provisioning summary: 100% synthetic except total_workers, which has a real but unreachable-from-here analog; the rest is B-WORKER\'s future domain), and the header risk badge (CompanyIntelligenceService.getCompanyIntelligenceRecord: an undefined ad-hoc heuristic over 6 synthetic services) are each a real gap, not license to render a fake value in the meantime. Removing all three left only navigation chrome built on an already-synthetic company_name — nothing substantive survived. The real Company Console (CompanyConsolePanel, /api/admin/company-console) never linked to this bare root path in the first place; its quickActions go straight to the Gen 3 sub-tabs (workspace/evidence/preview/submissions) by real tenant_code (caller-graph search of every `admin/companies/${...}` template literal in app/ and components/ confirmed this). The only two entry points that ever pointed at bare root were themselves demo-only — app/admin/pipeline\'s DEMO_COMPANY_ID quick link (removed this pass) and users/page.tsx\'s self-referential breadcrumb/nav (unchanged, out of scope: Users tab). page.tsx was rewritten as a thin redirect (requireKoraAdmin -> redirect to the Gen 3 workspace tab for the same companyId), reusing the exact same real analytics.tenant.tenant_code existence check the workspace route already performs — no new compatibility mapping invented; a synthetic id like "meridiana-group" now honestly 404s there instead of rendering fabricated data here. `ROOT_CONTROL_ROOM_FUTURE = READY_TO_RETIRE`. Caller-count effect of this pass: tenantService 8->7, workerProvisioningService 7->6, companyIntelligenceService 1->0 (now fully orphaned — see svc.company-intelligence); companyDataIntakeService, budgetToHumanImpactService, scoringSimulatorService, companyOnboardingService unchanged (root already did not call them, per Wave 2/3). I9 unchanged (25 files/37 imports) — no service reached zero *synthetic-import* callers; CompanyIntelligenceService reaching zero *runtime* callers is a distinct fact (zero callers != DEAD, not deleted). Files modified this pass: app/admin/companies/[companyId]/page.tsx (rewritten to a redirect), app/admin/pipeline/page.tsx (removed the now-always-404 Company Control Room quick link), and 6 test files updated to match (b175-admin-company-hydration-date, cc013-canonical-contract, cc014-decision-pack-adversarial, root-control-room-wave2, root-control-room-wave3, tenant-identity-read-path-audit). COMMIT: NO / PUSH: NO per this pass\'s explicit instruction — awaiting a separate human commit decision. CC-019A — Legacy Company Users Surface Retirement (2026-08-31): retired app/admin/companies/[companyId]/users/page.tsx outright (100% synthetic, read-only demo account display; its only "create user" CTA linked to an already-broken flat route, /admin/company-users, not real functionality) — real, more capable replacement already existed and was left untouched: app/admin/company-users-live (Supabase-backed /api/admin/company-users, supports real GET/POST/PATCH, independent of TenantService/AccountProvisioningService, keyed by tenantId = analytics.tenant.id, a UUID). CompanyTabNav\'s Users tab removed rather than relinked, since the tab nav only carries tenant_code (companyId), not the UUID company-users-live needs — bridging the two was explicitly out of scope (no new resolver invented). app/admin/pipeline\'s "Utenti Aziendali" quick link removed for the same reason (would have 404\'d), matching this same file\'s own prior "Company Control Room" quick-link removal precedent. Caller-count effect: tenantService 7->6 (the file itself is gone, not just its call site); accountProvisioningService keeps its other 2 callers (pipeline demo, my-kora demo) — not retired. layout.tsx, ReportFactoryService, CompanyIntelligenceService, and the DEMO_RUNTIME callers (admin/pipeline, workforce-baseline, WorkforceQuickAccessPanel) are all unchanged. I9 unchanged (24 files/36 imports) — TenantService.ts remains in the allowlist via its other 6 callers. CC-019B (layout.tsx\'s own tenantService.getTenant() call) remains open, not touched by this slice. CC-019B — Gen3 Shared Layout Tenant Identity Canonicalization (2026-08-31): layout.tsx no longer imports TenantService — its company_name resolution now queries analytics.tenant by tenant_code, identical to the Gen3 children. Caller-count effect: tenantService 6->5 (admin/pipeline, workforce-baseline, WorkforceQuickAccessPanel — DEMO_RUNTIME — ReportFactoryService, CompanyIntelligenceService). TenantService.ts implementation untouched. I9 unchanged (24/36). CC-019 remains open beyond A+B: ReportFactoryService and CompanyIntelligenceService are separately-classified pending-decision consumers, not part of this decomposition. B-TRUTH First Canonical Seed Group (2026-09-01, side effect — the primary migration was svc.workforce-baseline, not this service): app/admin/companies/workforce-baseline/page.tsx no longer imports tenantService — both its own data and its company selector now read live endpoints (GET /api/admin/workforce-baseline, GET /api/admin/tenants). Caller-count effect: tenantService 5->4 (admin/pipeline, WorkforceQuickAccessPanel — DEMO_RUNTIME — ReportFactoryService, CompanyIntelligenceService). TenantService.ts implementation itself untouched; its own migration remains separately gated, unaffected by this seed group.', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.uef-review', domain: 'Methodology', primaryPath: 'services/uef-review/UEFReviewService.ts', purpose: 'HISTORICAL — isolated demo UEF review queue — read services/ingestion-pipeline output, not the live UEF path.', status: 'DEAD', futureCore: false, dependencies: ['svc.ingestion-pipeline'], competingWith: [], decisionRef: 'B-TRUTH UEFReview Retirement (2026-09-03)', notes: 'DELETED (file no longer exists at primaryPath, kept here only as historical record). CORRECTION (2026-09-03, established at retirement time): several earlier notes on this entry and on svc.ingestion-pipeline stated demo-guard-01 "explicitly asserts getReviewSummary must NOT be removed" and treated that as a deliberate, method-level, permanent protection — that was STALE. It described the guard\'s wording BEFORE B-TRUTH Decision Pack Ratification (PR #131, 2026-09-02) rewrote tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts. Rereading the guard\'s current full text at retirement time showed every substantive assertion reads app/company/kora-index/page.tsx\'s source (the page with the real historical bug, DEMO-DEP-RO) — never UEFReviewService.ts\'s. The only place this file\'s existence was checked was an incidental scope-boundary inventory line, mechanically updated for every retirement this session (ReportGeneratorService, FinancialGovernanceService, PreviewScoringAdapter, DynamicScoringPreviewService) — not an elevated, permanent requirement. RETIREMENT (2026-09-03): independently re-verified before deletion — all 11 IUEFReviewService methods individually confirmed zero real callers (not just the two previously known, getReviewSummary/getAllReviewedPipelineRows, which dropped to zero only after DynamicScoringPreviewService\'s own retirement removed their last caller); zero type-only callers (IUEFReviewService interface had zero external importers). No unique methodology: every method is a filter/remap over ingestionPipelineService.analyzeBatch()\'s demo classification output, itself non-authoritative. The real, canonical UEF-review product UX (app/admin/uef-review/, app/company/uef-review/ — already a locked shell per B106/B147, /api/admin/uef/* reading analytics.uef_record via analytics.fn_admin_uef_review()) was confirmed to have zero reference to this class, independent of this retirement. Canonical scoring-record build (lib/live/uef-to-scoring-records.ts, buildScoringRecordsFromApprovedUef) also confirmed independent. demo-guard-01\'s substantive fallback-prohibition assertions (page never imports uefReviewService, never calls getReviewSummary, no liveUefSummary ?? uefReviewService pattern) are unchanged and unweakened — verified still present and passing. IngestionPipelineService and EligibilityGateService are explicitly untouched by this PR — see tests/unit/b-truth-uef-review-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. Direct consequence of this deletion (documented, not acted on): IngestionPipelineService drops from 1 to 0 real callers (fully orphaned by caller count). Not retired or modified — a separate, later, independently-audited slice. UEFReviewService.ts was never an I9 allowlist entry — I9 unaffected (17/27 unchanged).', deletableWhen: 'Already deleted — this entry is the historical record of that deletion.' },
  { id: 'svc.worker-achievements', domain: 'Worker', primaryPath: 'services/worker-achievements/WorkerAchievementService.ts', purpose: 'Worker recognition layer, worker-private. CC-00 Worker/Account canonicalization audit (2026-09-05): DEFER_PRODUCT_DECISION — no canonical achievement domain object exists anywhere; its /my-kora callers are an already-disclosed PREVIEW/synthetic surface, not a One-Truth violation.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05)', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): "3 callers." status: CANONICAL. CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05, worker/account cluster governance slice): re-classified CANONICAL -> CONSOLIDATE — the prior label was inaccurate: this service is 100% synthetic (data/synthetic/worker-achievements.json, zero DB query, zero worker-scoped filtering — getAchievements()/getAchievementStats() return the same global fixture regardless of caller), not a canonical implementation of a real domain object. Exhaustive check: zero DB migrations reference "achievement" anywhere in supabase/migrations/ — there is no canonical achievement/evidence/recognition table today. Per this slice\'s own explicit critical rule ("Do not invent an Achievement model inside CC-00"), no replacement model was built. Disposition is DEFER_PRODUCT_CAPABILITY, not RETIRE_SYNTHETIC_RUNTIME: its 3 real callers (app/my-kora/page.tsx, app/my-kora/dynamic-cv/page.tsx) sit on a route that already, unconditionally, discloses its synthetic nature (BoundaryBadge mode="PREVIEW" ... suffix="dati sintetici", shown to every visitor, not just anonymous demo personas) — zeroing the achievement numbers out would visibly change an already-shipped surface\'s content for every real WORKER who reaches it via the real requireWorkerUser-adjacent /my-kora layout guard, which is itself a My KORA product-shape decision this slice is not authorized to make (Part 8 of this slice\'s own task: "Do NOT redesign page structure... Do NOT decide whether My KORA survives long-term"). Recorded as a deferred B-WORKER capability in this slice\'s own Deferred Worker Capability Register (tests/unit/cc00-worker-account-canonicalization.test.ts): a real "worker achievements / portable development record" capability would require a genuine evidence/recognition domain model (persisted, tenant-scoped, tied to real UEF/contribution records) that does not exist today and is not this slice\'s to invent. I9 unaffected (8/13 unchanged) — data/synthetic/worker-achievements.json remains required. No code was modified. See tests/unit/cc00-worker-account-canonicalization.test.ts for the regression guard recording this finding.', deletableWhen: null },
  { id: 'svc.worker-attribution', domain: 'Worker', primaryPath: 'services/worker-attribution/WorkerAttributionService.ts', purpose: 'Pure explainability classification, no DB.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: '6 callers.', deletableWhen: null },
  { id: 'svc.worker-iu-computation', domain: 'Worker/Methodology', primaryPath: 'services/worker-iu-computation/WorkerIUComputationService.ts', purpose: 'Per-worker IU calculation, functional exports (B161).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: ['svc.iu-computation'], decisionRef: null, notes: '7 real callers (functional exports — computeBaseWorkerPIBRows, etc.). Overlap with iu-computation not resolved.', deletableWhen: null },
  { id: 'svc.worker-opportunity', domain: 'Worker', primaryPath: 'services/worker-opportunity/WorkerOpportunityService.ts', purpose: 'Rule-based opportunity suggestions, no LLM.', status: 'FUTURE_CORE', futureCore: true, dependencies: [], competingWith: ['svc.activation-opportunity'], decisionRef: null, notes: 'Master Plan §33: "base tecnica di Exposure". 4 callers.', deletableWhen: null },
  { id: 'svc.worker-pib', domain: 'PIB', primaryPath: 'services/worker-pib/WorkerPIBService.ts', purpose: 'Worker-self PIB read/aggregate.', status: 'FUTURE_CORE', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Master Plan §33: prerequisite for canonical CF. 13 callers. Isolation independently verified (SECURITY DEFINER-only access, migration 027).', deletableWhen: null },
  { id: 'svc.worker-pillar-adoption', domain: 'Worker', primaryPath: 'services/worker-pillar-adoption/WorkerPillarAdoptionService.ts', purpose: 'Company-level pillar distribution only — privacy-safe aggregate-only design.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'CC-018/B-TRUTH SEED GROUP #1 (2026-08-30): canonical source moved from data/synthetic/company-aggregates.json to analytics.activation_result.pillar_distribution — the same column already canonical for the Decision Pack (B-PACK). Async, tenant-scoped (db, tenantId), no synthetic import remains. Sole caller (WorkerAdoptionPanel) is currently unmounted on any live route (deliberately, per B105/B133) — this migration is source-level, ready for whenever the panel is re-integrated; new consumer: app/api/company/pillar-adoption (session-authenticated). CC-002 fixed a local N≥10 threshold duplicate here (I2), unchanged.', deletableWhen: null },
  { id: 'svc.worker-provisioning', domain: 'Worker', primaryPath: 'services/worker-provisioning/WorkerProvisioningService.ts', purpose: 'Worker identity/account/roster provisioning. CC-00 Worker/Account canonicalization audit (2026-09-05): DEFER_PRODUCT_DECISION — a real canonical worker-identity path exists (personal.worker_identity, analytics.fn_company_worker_status()) but wiring it in requires new async/client-server data-fetching architecture disproportionate to this bounded slice, and most per-worker fields (department, site, my_kora_enabled) have no canonical column at all.', status: 'CONSOLIDATE', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05)', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): "16 callers." status: CANONICAL, "Matches current branch\'s own work (worker identity lifecycle sprint)." CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05, worker/account cluster governance slice): re-classified CANONICAL -> CONSOLIDATE — the file is 100% synthetic (data/synthetic/worker-roster.json), not a canonical implementation, despite the accurate caller count. Real callers as of this audit: app/admin/pipeline/_components/PilotLifecycleClient.tsx (DEMO_COMPANY_ID, demo-rooted), app/admin/companies/_components/RosterImportModal.tsx (importDemoRoster, explicitly demo-labeled, component-state only, no DB write), components/admin/WorkforceQuickAccessPanel.tsx (real tenant list, \'use client\' component), components/company/cockpit/WorkerAdoptionPanel.tsx (real companyId), and services/worker-space/WorkerSpaceCapabilityService.ts (used by both its Foundation Light preview path and its Pilot+/production_ready=true path). A real canonical worker-identity path already exists and is reachable today: personal.worker_identity (id, worker_ref, status, created_at, tenant_id — queried by app/api/admin/workers/list/route.ts) and analytics.fn_company_worker_status() (a SECURITY DEFINER RPC returning total/invited/active/pending/disabled/coveragePct, already consumed by app/api/company/workers/aggregate/route.ts). This audit confirmed WorkforceQuickAccessPanel.tsx\'s own header comment is accurate and still true: "worker-count/My-KORA/Worker-Space sub-values... remain unmigrated (their own separate, later, B-WORKER-territory slice)... for a canonical tenant not yet present in their still-synthetic roster, these honestly read 0/not-enabled rather than fabricating a count" — meaning the CURRENT synthetic behavior already satisfies "LIVE MUST NEVER FALL BACK TO DEMO OR SYNTHETIC DATA" (a real tenant\'s company_id never matches the synthetic roster\'s demo company_ids, so it reads 0, not fabricated Meridiana data) — this is an honestly-incomplete gap, not a live-data-integrity violation. Full canonicalization is blocked by two independent, non-overlapping constraints: (1) FIELD PARITY — department, site, my_kora_enabled, pib_private_enabled, employer_can_view_individual_pib, included_in_aggregates, privacy_threshold_cluster, and display_name have no canonical column anywhere; this slice\'s own explicit rule ("Do not invent missing fields") forbids fabricating them, and partial canonicalization (fixing only total/invited/active counts while leaving my_kora_enabled permanently synthetic-derived) would introduce a NEW kind of inconsistency not present today (a truthful nonzero total next to a permanently-fake-zero feature-adoption count for the same tenant), which is worse than the current uniformly-honest all-zero state for real tenants. (2) ARCHITECTURE — WorkforceQuickAccessPanel.tsx is a \'use client\' component calling these methods synchronously inside a .map() over the tenant list; the real canonical sources require an async, server-role-authenticated Supabase query. Converting this requires restructuring the client/server data-fetching boundary of 2 real live components and their parent Server Components (a new batched aggregate-fetch path, threaded as props) — this is materially larger than a data-truth substitution and risks crossing into "introduce new worker features" / page-structure redesign, both explicitly forbidden by this slice. This matches, independently, the PRIOR B-TRUTH Root Control Room Wave 3 Hardening finding (this same registry entry\'s neighbor, svc.tenant, preserves the verbatim quote): "worker data is >80% B-WORKER-gapped with no real equivalent." Disposition: DEFER_PRODUCT_DECISION for the full service; the aggregate-count sub-path is recorded as a deferred B-WORKER/live-migration capability requiring an explicit, separately-authorized architecture change, not invented here. No tenant_kind branch, no KoraTest special case introduced or found. I9 unaffected (8/13 unchanged) — data/synthetic/worker-roster.json remains required. No code was modified. See tests/unit/cc00-worker-account-canonicalization.test.ts for the regression guard recording this finding.', deletableWhen: null },
  { id: 'svc.worker-space', domain: 'Worker', primaryPath: 'services/worker-space/WorkerSpaceCapabilityService.ts', purpose: 'Determines worker space capability per tenant (NOT_ENABLED / ENABLED / PILOT_READY). Not itself an I9 entry (no direct data/synthetic/** import) but depends entirely on workerProvisioningService.getWorkersForCompany() for both its Foundation Light and Pilot+ paths.', status: 'CANONICAL', futureCore: false, dependencies: ['svc.worker-provisioning'], competingWith: [], decisionRef: null, notes: '8 callers. CC-00 Worker/Account synthetic-service canonicalization audit (2026-09-05): confirmed untouched, consistent with svc.worker-provisioning\'s own DEFER_PRODUCT_DECISION finding — this service\'s _pilotCapability() (production_ready=true path) and _previewCapability() both read the same still-synthetic roster, so even a live, promoted Pilot+ tenant\'s worker-space capability is computed from a company_id lookup that can never match the synthetic fixture\'s demo company_ids (honestly reads 0 workers / NOT_ENABLED, never fabricated data) — the same "honestly incomplete, not fabricated" gap documented on svc.worker-provisioning, not a new or worse one introduced here. No code was modified.', deletableWhen: null },
  { id: 'svc.workforce-baseline', domain: 'Company', primaryPath: 'lib/live/workforce-baseline-view.ts', purpose: 'Baseline workforce metrics — canonical live view over personal.workforce_baseline (B-TRUTH first seed group, closed 2026-09-01).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'B-TRUTH First Canonical Seed Group (2026-09-01): services/workforce-baseline/WorkforceBaselineService.ts (synthetic, data/synthetic/workforce-baseline.json) DELETED entirely — confirmed single real caller (app/admin/companies/workforce-baseline/page.tsx) migrated onto GET /api/admin/workforce-baseline (new handler; the existing POST/persistWorkforceBaseline write path was already canonical and required zero changes) via lib/live/workforce-baseline-view.ts. Field disposition: KEEP/DERIVE total_workers, segment_breakdown->aggregateGroups, minimum company threshold (30, matching CompanyOnboardingService\'s own rule), minimum_group_size (stored, N>=10) — DROP/DEFER upload-process stats (Ingestion-domain territory, out of this task\'s scope), editorial completeness score, warnings/limitations text, and readiness.activation_reach_ready/distribution_equity_ready (would require cross-referencing analytics.activation_result, not touched here) — no schema added, no placeholder value invented for any dropped field. The page\'s own company selector was also repointed from the synthetic tenantService.getTenants() to the already-live GET /api/admin/tenants, avoiding a TenantService.ts entanglement (that service\'s own migration remains separately gated, unaffected by this seed group). tests/integration/rls-13-workforce-baseline-tenant-kind-parity.test.ts (DB-backed, mandatory CI gate) proves a DEMO-kind and a LIVE-kind tenant\'s personal.workforce_baseline rows are read back identically — no tenant_kind branch anywhere in the query. I9 reduced 23->22 files (35->34 imports) — second genuine reduction via a real caller migration, after CC-052\'s Commons work.', deletableWhen: 'Already deleted — this entry records the migration; primaryPath now points at the canonical live replacement.' },

  // ── lib/* domain aggregates ─────────────────────────────────────────────────
  { id: 'lib.kora-engine', domain: 'Methodology', primaryPath: 'lib/kora-engine/', purpose: '14-stage IU/Index/BTI/Confidence/Equity computation engine (25 files) — the CC-002 I6/I7-protected core.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Matches doc 10 methodology. run-kora-pipeline.ts orchestrates; consumed by LiveScoringAdapter. D-A (CC-011): confidence-engine.ts is the canonical Confidence implementation — scale/banding centralized via normalizeConfidenceScore/getConfidenceBand; lib/live/persistence.ts consumes them rather than owning independent methodology.', deletableWhen: null },
  { id: 'lib.decision-pack', domain: 'Reporting', primaryPath: 'lib/decision-pack/', purpose: 'Live Decision Pack PDF/HTML generation (4 files: pdf-data, pdf-runtime, pdf-strategy, html-template).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: ['svc.report-factory', 'svc.report-generator'], decisionRef: 'CC-013 / D-B', notes: 'D-B resolved (CC-005/CC-013), OPTION A: canonical KORA Decision Pack implementation. Builder/renderer/PDF-runtime seam already existed cleanly pre-CC-013 (pdf-data.ts builds the content model from canonical persisted rows, html-template.ts and pdf-runtime.ts are pure functions of that model with zero DB access) — formalized via doc header, no module split needed. All 4 live routes (company preview/pdf, admin preview/pdf) confirmed to use this exact path with no accidental duplication. Confirmed single implementation of the live PDF layer itself (2 independent passes found no second PDF generator). CC-005/D-B CLOSEOUT (2026-09-02): see the CC-005 CLOSEOUT / D-B RESOLUTION header block above — formal ratification, plus proof (tests/unit/cc005-decision-pack-limitations-migration.test.ts) that every mandatory disclaimer from ReportGeneratorService.getDecisionPackLimitations() is already present here, and that no demo-specific legacy wording leaks into live output.', deletableWhen: null },
  { id: 'lib.kora-link', domain: 'KORA Link', primaryPath: 'lib/kora-link/', purpose: 'Identity/token-based worker activation channel (8 files).', status: 'FUTURE_CORE', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Master Plan §33: "FUTURE CORE, gap DG-07 chiusi [VERIFIED]". Flag-gated off (KORA_LINK_ENABLED=false) but functionally complete; RLS-dense (migration 035 alone ~23 policies).', deletableWhen: null },
  { id: 'lib.kora-contribution', domain: 'Contribution', primaryPath: 'lib/kora-contribution/contribution-methodology.ts', purpose: 'Contribution doctrine/policy constants (score presentation mode, index-independence flags, FL-vs-Pilot+ path documentation) — not a computation implementation.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Corrected by the B-TRUTH Contribution audit and protected port (2026-09-01): full-file read confirms zero computation and zero imports — pure exported constants (CONTRIBUTION_IS_KORA_INDEX_COMPONENT, CONTRIBUTION_SCORE_PRESENTATION_MODE, CONTRIBUTION_FL_PATH/CONTRIBUTION_PILOT_PATH, etc.) plus doctrine comments, kept up to date with the DB-backed port. Not a competing implementation of svc.kora-contribution — the prior "genuine fragmentation" / INVESTIGATE classification was a misread of an unconsumed-computation file as a second implementation. Consumed by lib/partner-activities + lib/live/contribution-lineage.ts, both real, unrelated downstreams.', deletableWhen: null },
  { id: 'lib.scoring-result', domain: 'Scoring', primaryPath: 'lib/scoring-result/index.ts', purpose: 'Enforced single scoring consumption hook (useScoringResult).', status: 'CANONICAL', futureCore: false, dependencies: ['svc.scoring.facade'], competingWith: [], decisionRef: null, notes: 'In-code contract: app/components must never import ScoringSimulatorService/DynamicScoringPreviewService/run-kora-pipeline directly. CC-002 found 6 real violations of this contract (not fixed — CC-010/B-TRUTH scope).', deletableWhen: null },
  { id: 'lib.methodology-config', domain: 'Methodology', primaryPath: 'lib/methodology-config/v0.1.ts', purpose: 'Versioned weights/thresholds loader.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Matches CLAUDE.md\'s "v0.1.ts" filename requirement. Consumed by report-factory, kora-engine, scoring. B-BC (CC-009): added getBCByActionFamily(), reading data/methodology/methodology-config.json\'s new bc_by_action_family key — same pattern as nm_functions/shrinkage/pib. B-SNAP (CC-015): added getMethodologySnapshot()/computeConfigHash()/getFactorStatuses() — single construction authority for the Methodology Snapshot; deterministic sha256 over macroblock/BC/NM/shrinkage/PIB/calibration config only (never timestamps, tenant identity, or results).', deletableWhen: null },
  { id: 'lib.privacy', domain: 'Privacy', primaryPath: 'lib/privacy/group-threshold.ts', purpose: 'N≥10 privacy suppression module — pure functions (suppressSmallGroups, validateNoSmallGroups, etc.).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'CC-002: DEFAULT_MIN_GROUP_SIZE now re-exports the canonical lib/constants/kora.ts value (I2). Added detectGroupTotalReconciliationRisk() (I5) — proves but does not fix a live differencing risk (see CC-002 report §14 remaining risks).', deletableWhen: null },
  { id: 'lib.permissions', domain: 'Privacy', primaryPath: 'lib/permissions/index.ts', purpose: 'Access-matrix / route-list resolution (getAccessibleRoutes vs getDemoNavigationRoutes).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: ['svc.role-permission', 'svc.access-control'], decisionRef: null, notes: '10 callers.', deletableWhen: null },
  { id: 'lib.auth', domain: 'Auth', primaryPath: 'lib/auth/access-matrix.ts', purpose: 'Real Supabase auth helpers, canAccess() authorization gate — the live, wired-in authorization mechanism. DEMO_VIEWER role retired entirely (CC-00, 2026-09-26) — not replaced by another role with a different name.', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'CC-00 DEMO_VIEWER role retirement (2026-09-26)', notes: 'Imported directly by middleware.ts. CC-00 DEMO_VIEWER role retirement (2026-09-26): DEMO_VIEWER removed entirely from the runtime role model, across every layer of this domain — founder decision "DEMO_VIEWER = RETIRE", not replaced by another role with a different name, no new tenant-scoped demo role, no second commercial-runtime access mode. lib/constants/kora.ts: DEMO_KORA_ROLES deleted; KORA_ROLES = ACTIVE_KORA_ROLES + FUTURE_KORA_ROLES only; DEMO_VIEWER moved to REMOVED_KORA_ROLES alongside COMPANY_VIEWER. lib/auth/kora-session.ts: KoraDemoUser interface, requireDemoUser(), requireDemoAccess() (the one documented KORA_ADMIN /demo-preview exception), getCurrentDemoUser(), and isDemoUser() all deleted; isKoraAuthError/isKoraAdmin/isCompanyUser/isWorkerUser/isPartnerUser union signatures narrowed to drop KoraDemoUser. lib/auth/demo-guard.tsx (requireDemoGate(), already zero real callers before this slice — no /demo/** route called it after the prior 4-slice route retirement sequence) deleted outright. lib/auth/access-matrix.ts: all 8 DEMO_VIEWER: {...} rows removed from MATRIX (KoraRole, derived from KORA_ROLES, no longer includes it — TypeScript itself now rejects a DEMO_VIEWER row). lib/auth/role-home.ts: the `role === \'DEMO_VIEWER\' → \'/demo\'` branch removed; falls through to \'/login\' (fails closed, never a privileged default). app/auth/callback/route.ts: the DEMO_VIEWER invite branch removed; falls through to the same unknown-role → /login branch. middleware.ts: DEMO_VIEWER_ALLOWED_PREFIXES constant and the isDemoViewer confinement-redirect block removed; the x-pathname response header (whose sole purpose was letting requireDemoGate() build a ?next= redirect URL) removed too — zero remaining consumers. app/api/admin/demo/provision-viewer/route.ts (the only DEMO_VIEWER account-provisioning path anywhere in the codebase; no admin UI ever called it) deleted outright — DEMO_VIEWER_PROVISIONING_RETIRED = YES. docs/access-matrix.md updated to keep the authoritative doc in sync with the code (a live regression test, tests/unit/pilot-saas-01-role-architecture-invariants.test.ts, asserts the doc\'s table header matches KORA_ROLES exactly — this is not optional cosmetic doc-editing, it is required to keep the ROLE-01 doc/code parity invariant true). Remaining /demo/** surfaces (app/demo/page.tsx, app/demo/future-vision/page.tsx) are unaffected in classification — both were already public, unguarded static presentation before this slice (requireDemoAccess() was never called by either route; DEMO_VIEWER admission only ever mattered for the now-fully-retired gated sub-routes) — DEMO_ROOT_CLASS = STATIC_PRESENTATION, FUTURE_VISION_CLASS = STATIC_PRESENTATION, both unchanged. No Supabase Auth user, app_metadata, or role was mutated — this is a code/runtime retirement only (AUTH_DB_MUTATION_PERFORMED = NO); any legacy Supabase Auth account still carrying app_metadata.kora_role = \'DEMO_VIEWER\' now authenticates but lands back on /login on every login attempt and is rejected (403/known-role-mismatch, same as any other non-matching role) by every real require*User() guard — EXISTING_DEMO_VIEWER_ACCOUNT_RISK = LOW, not MATERIAL (no privilege gain, no crash, no auth loop; only a mildly confusing "logged in but nowhere to go" UX for what were always synthetic test accounts) — recorded as an operational follow-up (KORA_ADMIN should manually deactivate any lingering DEMO_VIEWER Supabase Auth users via the Dashboard), not a blocking condition for this slice. Zero KORA_ADMIN exceptions of any kind remain in kora-session.ts — a stronger invariant than before (previously exactly one, requireDemoAccess(), documented and deliberate). B-WORKER, My KORA product design, and final scoring are all explicitly untouched. I9 unaffected (8/13 unchanged — this slice touched zero data/synthetic/** imports). CC-00 remains OPEN. See tests/unit/cc00-demo-viewer-retirement.test.ts for the regression guard proving both the retirement and the scope boundary.', deletableWhen: null },
  { id: 'lib.worker-identity', domain: 'Worker', primaryPath: 'lib/worker-identity/', purpose: 'Core worker identity lifecycle helpers (2 files).', status: 'CANONICAL', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Matches current branch HEAD (worker identity lifecycle protection, migration 048).', deletableWhen: null },
  { id: 'lib.live', domain: 'Live data', primaryPath: 'lib/live/', purpose: 'Non-demo Supabase-backed data mapping and persistence layer (10 files).', status: 'CANONICAL', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Includes contribution-lineage.ts, which carries its own in-code "NAMING DISAMBIGUATION (C-6)" note acknowledging a naming collision between its ContributionRole concept and KORA Contribution — self-flagged, unresolved debt, not fixed here. B-SNAP (CC-015): persistence.ts creates one methodology_snapshot row per calculation via the canonical getMethodologySnapshot() builder and links it to every result table it writes — no independent snapshot construction here, same discipline as the CC-011 Confidence scale/banding contract.', deletableWhen: null },
  { id: 'lib.types.domains.evidence', domain: 'Evidence', primaryPath: 'lib/types/domains/evidence.ts', purpose: 'Type scaffold for a first-class Evidence entity — types only, no persistence, explicitly for Pilot+.', status: 'FUTURE_CORE', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Own header: "No persistence in KORA Foundation Light: types only... Pilot+ engineer: implement EvidenceRepository." Verified by direct read (CC-002).', deletableWhen: null },
  { id: 'lib.ingestion-cluster', domain: 'Ingestion', primaryPath: 'lib/data-intake/', purpose: 'Parsing/upload/interpretation helper cluster (lib/data-intake, roster-import, upload, ingestion — 23 files total).', status: 'INVESTIGATE', futureCore: false, dependencies: [], competingWith: ['svc.ingestion-normalizer', 'svc.ingestion-pipeline', 'svc.ingestion-simulator', 'svc.company-data-intake'], decisionRef: null, notes: 'BACKLOG — HUMAN TRIAGE. CC-002 traced part of this cluster: ingestion-normalizer→ingestion-pipeline is a real 2-stage chain; ingestion-simulator and company-data-intake are separate parallel entry points. The four lib/ sub-directories themselves were not individually caller-traced. Finding: MIXED.', deletableWhen: null },

  // ── App surfaces (top-level aggregates) ────────────────────────────────────────
  { id: 'app-surface.worker', domain: 'Worker', primaryPath: 'app/worker/', purpose: 'Live worker workspace, real Supabase JWT session (12 routes).', status: 'COMPLETE', futureCore: true, dependencies: ['svc.worker-provisioning', 'svc.worker-pib'], competingWith: ['app-surface.my-kora'], decisionRef: 'CC-024 / D-D', notes: 'Neutral status — CC-024\'s 12-dimension architecture matrix owns the consolidation decision, not a line-count comparison.', deletableWhen: null },
  { id: 'app-surface.my-kora', domain: 'Worker', primaryPath: 'app/my-kora/', purpose: 'Preview worker workspace, demo-state session (9 routes) — self-documented in-file as PREVIEW mode.', status: 'COMPLETE', futureCore: true, dependencies: ['svc.my-kora-preview'], competingWith: ['app-surface.worker'], decisionRef: 'CC-024 / D-D', notes: 'Neutral status. Master Plan §19 B-WORKER row states "/my-kora gira su AccessControlService" — CC-001R verified this claim does NOT match current code (app/my-kora/page.tsx does not import AccessControlService); flagged as an untagged (no [VERIFIED]) Master Plan claim, not corrected here.', deletableWhen: null },
  { id: 'app-surface.admin', domain: 'Admin', primaryPath: 'app/admin/', purpose: 'Admin/governance/diagnostics workspace (30 subdirectories, mixed live + synthetic).', status: 'COMPLETE', futureCore: false, dependencies: [], competingWith: [], decisionRef: null, notes: 'Not individually traced per-subdirectory. Includes diagnostic-only tooling (kora-link-lab, live-spine-diagnostics, provisioning-diagnostics) that a future pass may want to split out separately.', deletableWhen: null },
  { id: 'app-surface.landing', domain: 'Marketing', primaryPath: 'app/page.tsx', purpose: 'Public, unauthenticated marketing landing page. As of CC-00 Public Landing canonicalization (2026-09-26), carries zero synthetic runtime dependency and zero private/tenant-level data — static product copy plus real, versioned methodology facts (macroblock weights, pillar definitions) only.', status: 'COMPLETE', futureCore: false, dependencies: ['lib.methodology-config'], competingWith: [], decisionRef: 'CC-00 Public Landing canonicalization (2026-09-26)', notes: 'PRIOR STATE (accurate before this slice, preserved for context): the page imported data/synthetic/kora-index-outputs.json and data/synthetic/company-aggregates.json directly, reading a specific fictional company\'s ("Meridiana Group", scenario S1) KORA Index value, Confidence Score, Safeguard status, per-macroblock score, and per-pillar IU share, and displayed each as if illustrating a real product result — including naming the fictional company by name next to its claimed score in the "Anatomia del KORA Index" section. This is exactly the test-company-presented-as-customer-proof failure mode CC-00 exists to remove. CANONICALIZATION (2026-09-26): both synthetic imports are removed entirely — not relabeled as "illustrative" while still reading the fixture, actually removed, since the goal was to remove the runtime dependency, not merely disclose it. Every per-company synthetic value (index score, confidence %, safeguard status, macroblock scores, pillar shares) is either (a) replaced with the real, static, canonical equivalent already versioned in lib/methodology-config/v0.1.ts\'s getMacroblockWeights() (REACH 25% / QUALITY 30% / EQUITY 25% / BTI 20%, matching CLAUDE.md §5 exactly — real for every company, not per-tenant), or (b) removed outright with no invented replacement number (no fourth "quietly re-fake it" option was taken; per-pillar IU share had no real static equivalent and was deleted, not approximated). The Index Anatomy card no longer names any company — its score display was changed from a specific claimed value to the real static scale itself ("0–100"), its safeguard chip lists the 3 real states (CLEAR/WARNING/FLAGGED) instead of asserting one specific status, and its Confidence Score chip states the real architectural fact ("esterno · peso 0") instead of a specific percentage. The macroblock bars were repurposed from showing a fake per-company score to showing the real per-macroblock weight — same visual structure (UI salvage), honest content. The hero SVG\'s Impact Field gauge was already 100% decorative (a hardcoded client-side count-up to 34, in components/landing/LandingMotion.tsx, never bound to any fixture) and needed no change; its two adjacent badges (Confidence Score, Activation Safeguard) previously did read the synthetic per-company values and were corrected the same way as the Index Anatomy chips. The footer\'s explicit "synthetic_demo_data: true" disclosure is removed — no longer true, so no longer shown, rather than kept as a now-inaccurate blanket disclaimer. Foundation Light package pricing (lib/landing/packages.ts) was already a real static config file, untouched. No new public database query was introduced anywhere — the replacement source is a static, versioned TypeScript config module, not a live tenant read; PUBLIC_TENANT_LEVEL_DATA_ALLOWED remains NO, unexercised, since no tenant-level table is queried by this page at all, before or after. I9: app/page.tsx removed entirely from the allowlist (12->11 files, 18->16 imports) — neither data/synthetic/kora-index-outputs.json nor data/synthetic/company-aggregates.json became zero-consumer overall, both retain multiple other real consumers, verified by repo-wide grep before this change. No /demo/** route was retired or touched. DEMO_VIEWER role, B-WORKER, My KORA, and final scoring all untouched. CC-00 remains OPEN. See tests/unit/cc00-public-landing-canonicalization.test.ts for the regression guard proving both the canonicalization and the scope boundary.', deletableWhen: null },
  { id: 'app-surface.demo', domain: 'Demo', primaryPath: 'app/demo/', purpose: 'Standalone demo/pitch surface, reduced to 2 routes (app/demo/ root hub, app/demo/future-vision/) after 4 CC-00 route-retirement slices — advisor, ai-onboarding, benchmarks, gtm, guide, index-registry, network, partner, and portfolio have all been retired, canonicalized into, or salvaged onto their real counterparts. DEMO_VIEWER role itself is now also fully retired (CC-00, 2026-09-26) — the remaining 2 routes are public static presentation with no role-specific guard.', status: 'CONSOLIDATE', futureCore: false, dependencies: ['svc.demo-data', 'svc.scoring-simulator', 'svc.access-control'], competingWith: [], decisionRef: 'CC-00 DEMO_VIEWER Retirement (2026-09-06) — supersedes B-TRUTH / D-C', notes: 'PRIOR HISTORY (accurate as of its own time, preserved verbatim): Master Plan §13 explicitly names this cluster: "11 pagine /demo con ruolo DEMO_VIEWER, synth-only [DECISION REQUIRED D-C]" — count independently verified (exactly 11 page.tsx files under app/demo/). D-C RESOLVED (founder decision, 2026-08-31): these 11 pages are intentionally retained as DEMO_RUNTIME, classified the same way as the Ingestion/UEF chain (see svc.ingestion-pipeline, svc.uef-review) — NOT required to be retired merely to satisfy One Product/One Truth. Governing invariant: DEMO MAY USE SYNTHETIC DATA; LIVE MUST NEVER FALL BACK TO DEMO OR SYNTHETIC DATA. Retention is conditional, not unconditional: acceptable only while (1) these pages remain clearly identifiable as demo experiences, (2) they do not masquerade as live company data, (3) live authenticated company surfaces never depend on or fall back to them, (4) demo/live separation remains structurally testable, (5) they are excluded from canonical analytical truth. This resolution does not authorize synthetic data in any canonical live path — status remains CONSOLIDATE (not CANONICAL, not FROZEN) pending the still-open remediation items this cluster depends on (svc.demo-data, svc.scoring-simulator, svc.access-control — all still CANONICAL/unretired per Master Plan §32). SUPERSESSION (2026-09-06, CC-00 DEMO_VIEWER Retirement founder decision): the founder has since ratified that DEMO_VIEWER must be retired entirely — it must not evolve into a permanent preview role, must not justify a second synthetic product truth, and must not receive live canonical cross-company product access. This explicitly supersedes D-C\'s conclusion above for the routes and role D-C covered; D-C\'s own text is preserved verbatim above as the historical record of what was true and ratified at that earlier time, not silently erased or reinterpreted. A controlled, capability-preserving retirement sequence was planned (salvage real value first, kill duplicated/fake truth second, retire the DEMO_VIEWER role only after no route depends on it) — see the CC-00 Founder-Ratified Retirement Gate record for the full route-by-route disposition (MOVE_TO_REAL_KORA / DEFER_TO_N_OR_NB / KILL) and the 4-slice sequence. INDEX REGISTRY CANONICALIZATION (2026-09-06, first slice of that sequence): app/demo/index-registry/ (page + layout) is retired outright. Its real value — a compact "companies by current KORA Index" view — already lived, and continues to live, in app/admin/page.tsx\'s own Intelligence Grid panel (KORA_ADMIN-only, unchanged auth model), now reading canonical analytics.tenant + analytics.kora_index_result directly via lib/live/admin-cross-company-view.ts\'s buildIndexRegistryView() — reusing the SAME already-fetched kora_index_result rows Platform Analytics already queries, no second query, no duplicated business logic. scenario_id (the one field with no canonical equivalent) is dropped, not replaced by an invented substitute; reporting_period/confidence_score/methodology_version_id/calibration_status/is_synthetic were consumed only by the now-retired demo page and are not carried forward. getIndexRegistryPreview() and its IndexRegistryEntry interface are removed from services/admin-preview/AdminPreviewService.ts (zero remaining callers). The two dangling nav references (lib/navigation/admin-nav-groups.ts\'s "Registro KORA Index" entry, app/demo/page.tsx\'s own homepage card) are removed to avoid a dead link — a minimal correction, not a redesign. DEMO_VIEWER role itself, and every other /demo/** route (portfolio, network, advisor, partner, ai-onboarding, gtm, benchmarks, guide, future-vision, and /demo itself), remain untouched — separate, later slices of the same planned sequence. B-WORKER and final scoring untouched. CC-00 remains OPEN. See tests/unit/cc00-index-registry-canonicalization.test.ts for the regression guard proving both the canonicalization and the scope boundary. PARTNER DEMO CAPABILITY SALVAGE + CONTROLLED RETIREMENT (2026-09-12, second slice of that sequence): app/demo/partner/ (page + layout) is retired outright, after a full capability inventory against the real app/partner/** surface (workspace, activity-bookings + detail, activity-catalog + [activityId], aggregate-signals, initiatives, kora-link + initiatives, privacy-boundary, relationships — 8 real sub-surfaces, not just workspace as earlier gates had assumed). Result: every meaningful capability the demo page showed was already present, and usually more mature, on the real surface (partner profile → app/partner/workspace\'s live network.partner_profile; services/opportunities → app/partner/activity-catalog\'s fiscal-category/pillar/KORA-Index-eligibility model; activation requests → app/partner/activity-bookings\' worker-initiated booking cards with an explicit worker-data boundary panel; activation cohorts and the ≥10 privacy threshold → app/partner/privacy-boundary\'s dedicated can/cannot-see model for both partner and company; the closing "Partner Access Boundary" footer → app/partner/privacy-boundary in full, split more precisely into a partner-side and a company-side list) — or was already named, unprompted, as a deferred future capability on app/partner/workspace/page.tsx\'s own existing "Funzionalità future — prossimamente" section (evidence protocol/audit status; aggregate activation performance; contact requests from companies; KORA coordination for collective initiatives — each already labeled "— prossimamente" before this slice touched anything), meaning no new capability discovery happened here, only confirmation that the real product had already scoped what the demo was faking. One demo section (a financial/billing "Resoconto attività e fatturazione" preview, with per-period estimated amounts and invoice/payout status) is flagged as having been out of authorized scope from the start — Foundation Light\'s Red Lines forbid payment, wallet, checkout, or voucher logic even framed as a non-persistent preview — so its removal is a compliance correction, not merely a duplicate-removal. A handful of remaining sections (per-company "aziende in perimetro" scope cards, a daily operations agenda, static availability windows, and a partner action log) had no real equivalent and were not required by anything documented; building real equivalents now would have been a Partner UX redesign this slice was explicitly scoped not to attempt, so they are recorded as killed-not-salvaged rather than carried forward speculatively. PARTNER_UI_REDIRECT_SCOPE was assessed MINIMAL: zero UI or code moved into app/partner/** (the real surface already covers or has already deferred everything of value) — this was a pure deletion. Nothing under app/demo/partner/ was linked from app/demo/page.tsx\'s own homepage nav (already an orphaned route, same pattern as Index Registry before it) and no synthetic fixture file was uniquely consumed by it (all demo data was inline in the page itself). Gated /demo/** layouts drop from 5 to 4 (advisor, ai-onboarding, network, portfolio remain); total /demo/** page routes drop from 10 to 9. DEMO_VIEWER role itself, and every other /demo/** route (portfolio, network, advisor, ai-onboarding, gtm, benchmarks, guide, future-vision, and /demo itself), remain untouched — separate, later slices of the same planned sequence. B-WORKER and final scoring untouched. CC-00 remains OPEN. See tests/unit/cc00-partner-demo-retirement.test.ts for the regression guard proving both the retirement and the scope boundary. COMPANY PORTFOLIO CAPABILITY SALVAGE + CANONICALIZATION (2026-09-12, third slice of that sequence): app/demo/portfolio/ (page + layout) is retired outright. Its backing method, getCompanyPortfolioPreview(), simulated a capability that already existed, canonically and more richly, at app/admin/companies/page.tsx ("Company Console"); app/admin/page.tsx\'s "Company Readiness Matrix" panel (its other real caller) now reuses the same already-fetched, already-canonical `registry` array (buildIndexRegistryView(), unchanged) it already fetches for the Intelligence Grid — no new query, no new interface field. Full field-by-field disposition (sector, territory, is_primary_demo, demo_note) recorded on svc.admin-preview\'s own entry above, including the finding that `territory` is not mere decoration but an already-identified Master Plan v2.1 future ontology object ("TERRITORY / LOCAL ENTITY — da tipizzare") deliberately deferred, not invented, here. Two dangling nav references removed (app/demo/page.tsx\'s homepage card, lib/navigation/admin-nav-groups.ts\'s "Portfolio Aziende" entry) and one redirect corrected (next.config.ts\'s /admin/portfolio now points at /admin/companies). While editing that same redirect array, a pre-existing, unrelated bug from the prior Index Registry slice was found and fixed: /admin/index-registry was still redirecting to the now-deleted /demo/index-registry — corrected to /admin. Gated /demo/** layouts drop from 4 to 3 (advisor, ai-onboarding, network remain); total /demo/** page routes drop from 9 to 8. DEMO_VIEWER role itself, and every remaining /demo/** route, are untouched — separate, later slices. AI Onboarding, benchmark/network/advisor, B-WORKER, My KORA, and final scoring all untouched. CC-00 remains OPEN. See tests/unit/cc00-portfolio-canonicalization.test.ts for the regression guard proving both the retirement and the scope boundary. RESIDUAL /DEMO/** CONTROLLED RETIREMENT (2026-09-26, fourth and final-for-now slice of that sequence): the 4th slice retired every remaining /demo/** route except the root hub and future-vision. Route-by-route disposition — app/demo/advisor/ (AD-01 Advisor Workspace, 1224 lines): REAL_PRODUCT_DUPLICATE/DEMO_ONLY mixed — its evidenceReliabilityIntelligenceService.computeFromData()/getPillarEvidenceBreakdown() dependency is a real, canonical, shared computation service with other confirmed live callers, untouched; its OTHER dependency, services/advisor-evidence-review/AdvisorEvidenceReviewService.ts, had zero other callers anywhere and is deleted (see that service\'s own registry entry). app/demo/network/ (A-06 KORA Activation Network, 889 lines): DEMO_ONLY — 100% hardcoded territory/partner/gap/matching/initiative/map data, zero canonical backing (network.partner_profile lacks matching/coverage/gap-diagnostic fields); was the last real caller of getAdvisorNetworkPreview(). app/demo/benchmarks/ (A-04 Benchmark, 108 lines): DEMO_ONLY — its own header comment already stated the exact "nessun benchmark empirico disponibile prima della calibrazione Delphi post-pilot" disclaimer CLAUDE.md §6 already requires; no separate deferred-capability note needed. app/demo/guide/ (415 lines): STATIC_PRESENTATION with zero unique value — a pure navigation/doctrine page whose content only linked to real /company/* surfaces, /demo/future-vision, and /pilot; independently found (not flagged by this task\'s own prompt) to be the actual href target of the public marketing landing page\'s (app/page.tsx) two "Esplora la demo" CTAs — both retargeted to /demo, along with next.config.ts\'s /demo-guide redirect (now -> /demo) and app/demo/future-vision/page.tsx\'s own "<- Demo Guide" back-link (now "<- Demo" -> /demo). app/demo/gtm/ (A-16 GTM & Validazione, 609 lines): MIXED, retired in full — Scenario Presenter and Demo Script A/B/C sections had no canonical duplicate but were judged, per this task\'s own explicit instruction, not worth preserving solely for founder convenience; Privacy Story duplicated doctrine already in CLAUDE.md; Pilot Package A-E duplicated /pilot + lib/landing/packages.ts; GTM Pipeline duplicated /admin/founder-validation; Gate & Methodology Status duplicated Admin Home\'s own Methodology Governance panel (established the prior slice). app/demo/ai-onboarding/: DEMO_ONLY, its last remaining capability retired along with getAIOnboardingPreview() itself (real callers app/admin/data-intake, app/admin/uef-review, app/admin/pipeline already existed independently, established two slices prior). app/demo/page.tsx (root hub): KEPT — DEMO_ROOT_REAL_PRODUCT_VALUE = YES; SECTION_ECOSYSTEM, SECTION_PIPELINE, and INTERNAL_TOOLS removed in full (every link they held pointed at a route retired this same slice); SECTION_INTELLIGENCE (6 real links) and SECTION_ROADMAP (future-vision) survive unchanged; DEMO_SCENARIOS (S1/S2 hardcoded scenario cards) explicitly out of this slice\'s scope, untouched. app/demo/future-vision/page.tsx: KEPT for a reason distinct from ordinary product-value analysis — constitutionally protected as a Future Vision static mockup screen (CLAUDE.md §10 explicitly allows it pre-Gate-2; §16 requires labeling; Red Line #10 forbids making it appear active) — only its "<- Demo Guide" back-link target was corrected. AdminPreviewService.ts is rewritten to a single method, getGateStatusPreview() (see its own entry for the full method-by-method disposition); I9 reduces 11->8 files, 16->13 imports; source-batches.json deleted (zero remaining consumers, verified). A pre-existing, unrelated dead-code chain was found and deleted as a transparently-disclosed bonus, unprompted by this task: components/demo/DemoGuideContent.tsx had zero importers anywhere in the repo, cascading to 3 further orphaned sub-components (PipelineConnectorBanner.tsx, WorkspaceSwitcher.tsx, StakeholderPaths.tsx — none imported synthetic data themselves). Gated /demo/** layouts drop from 3 to 0 — zero DEMO_VIEWER-gated layouts remain anywhere in the repo. Total /demo/** page routes drop from 8 to 2 (app/demo/page.tsx, app/demo/future-vision/page.tsx). DEMO_VIEWER role itself is explicitly NOT removed this slice — it still exists, still admitted by requireDemoAccess(), scheduled for a later, separate slice per this task\'s own explicit constraint. No live data was exposed to DEMO_VIEWER anywhere in this slice — every change was either a deletion or a retarget onto an already-public route. B-WORKER, My KORA, and final scoring untouched. CC-00 remains OPEN. See tests/unit/cc00-residual-demo-retirement.test.ts for the regression guard proving both the retirement and the scope boundary. DEMO_VIEWER ROLE RETIREMENT (2026-09-26, fifth and final slice of the founder-ratified retirement sequence, and a separate CC-00 slice from the 4 route-retirement slices above): the DEMO_VIEWER role itself — the last remaining piece of the sequence, deliberately deferred until no /demo/** route depended on it — is now retired entirely from KORA\'s runtime role model. Not replaced by another role with a different name; no new tenant-scoped demo role; no second commercial-runtime access mode. See lib.auth\'s own registry entry for the full code-level retirement record (kora-session.ts, demo-guard.tsx, access-matrix.ts, role-home.ts, middleware.ts, the provisioning endpoint). app/demo/page.tsx and app/demo/future-vision/page.tsx are unaffected in classification — both were already public, unguarded static presentation before this slice; DEMO_VIEWER admission only ever mattered for the gated sub-routes the prior 4 slices already retired. CC-00 remains OPEN — worker/account synthetic services and final scoring are still separate, later slices. See tests/unit/cc00-demo-viewer-retirement.test.ts for the regression guard proving both the retirement and the scope boundary.', deletableWhen: null },

  // ── Dead route (not a service) ──────────────────────────────────────────────
  { id: 'app.company-reports-board-pack', domain: 'Reporting (dead route)', primaryPath: 'app/company/reports/board-pack/page.tsx', purpose: '13-line redirect-only page to /api/company/decision-pack — no content renders.', status: 'DEAD', futureCore: false, dependencies: [], competingWith: [], decisionRef: 'Master Plan §32 Safe Deletion Plan', notes: 'Verified: pure redirect() call, no logic. Master Plan §32: "13 L, solo redirect [VERIFIED]". Referenced by 2 files (app/company/status/page.tsx, lib/feature-discovery/index.ts) and 4 tests exercising the redirect itself — those link targets would need updating on deletion.', deletableWhen: 'After B-REG, once the 2 referencing files are repointed directly at /api/company/decision-pack and the 4 redirect-behavior tests are updated or removed.' },

  // ── DB / schema capability (only where genuinely architectural) ────────────────
  { id: 'db.commons-booking-schema', domain: 'Commons', primaryPath: 'supabase/migrations/025_commons_booking_contribution.sql', purpose: 'commons.booking table — dual tenant_id (worker_tenant_id, post_tenant_id) cross-tenant pattern.', status: 'CANONICAL', futureCore: true, dependencies: [], competingWith: [], decisionRef: null, notes: 'Master Plan §33 [VERIFIED]. SECURITY DEFINER function booking_aggregate_for_promoter() enforces the cross-tenant boundary. This is the concrete evidence behind the Master Plan\'s Program/Participation/Case ontology correction (§3.0).', deletableWhen: null },
  { id: 'db.kora-link-schema', domain: 'KORA Link', primaryPath: 'supabase/migrations/034_kora_link_schema.sql', purpose: 'kora_link.* schema (links, link_assignments, link_batches, link_delivery_records, link_events, link_replacements, link_activation_acknowledgements, revocations, audit_log).', status: 'FUTURE_CORE', futureCore: true, dependencies: ['lib.kora-link'], competingWith: [], decisionRef: null, notes: 'Hardened through migrations 035/036/039/042. auth.uid()-based identity resolution confirmed inside SECURITY DEFINER RPC bodies (I4).', deletableWhen: null },
  { id: 'db.worker-pib-schema', domain: 'PIB', primaryPath: 'supabase/migrations/018_worker_pib.sql', purpose: 'personal.worker_pib — isolated PIB storage, SECURITY DEFINER-only access.', status: 'FUTURE_CORE', futureCore: true, dependencies: ['svc.worker-pib', 'svc.pib-aggregation'], competingWith: [], decisionRef: null, notes: 'I3 confirmed: zero application-role policies on this table (migration 027 refactor). Never employer-visible.', deletableWhen: null },
];

// ── B. TARGET ONTOLOGY IMPLEMENTATION MAP — domain objects ─────────────────────
// Describes the DOMAIN, not the code. An object with no code yet has no
// ARCHITECTURE_REGISTRY entry — it only appears here as TO_BUILD.

export type OntologyImplementationStatus = 'EXISTS' | 'PARTIAL' | 'TO_BUILD' | 'UNCERTAIN';

export interface OntologyObject {
  id: string;
  status: OntologyImplementationStatus;
  currentRepresentation: string;
  /** Code/schema paths that back the current representation, if any. */
  primaryPaths: string[];
  /** Future CC/block that would build or complete this object, if known. */
  implementationBlock: string | null;
  notes: string;
}

export const TARGET_ONTOLOGY: OntologyObject[] = [
  { id: 'Organization', status: 'EXISTS', currentRepresentation: 'analytics.tenant table (no gap) — but services/tenant/TenantService.ts is NOT a real access layer over it (100% synthetic, see svc.tenant).', primaryPaths: ['supabase/migrations/001_live_v1_foundation.sql', 'app/api/admin/tenants/route.ts', 'app/api/admin/company-console/route.ts'], implementationBlock: null, notes: 'The underlying table has no gap and is already the live source for /api/admin/tenants, /api/admin/company-console, /api/admin/tenants/[id]/promote-to-pilot, lib/decision-pack/pdf-data.ts, /api/company/workspace. TenantService.ts is listed as a primary path only historically — corrected here (B-TRUTH Tenant Identity read-path audit, 2026-08-30) since it does not query this table at all. See svc.tenant for the read-path consolidation status.' },
  { id: 'ProgramDefinition', status: 'TO_BUILD', currentRepresentation: 'Flat programs.json (data/synthetic/programs.json), company_id-scoped, no owner-type split (company/partner/territory) — a data-shape analog only, not code implementing this object.', primaryPaths: [], implementationBlock: 'N1', notes: 'Zero code hits for the term "ProgramDefinition" anywhere in the repo (confirmed by exhaustive grep, CC-001R + CC-001 both independently).' },
  { id: 'ProgramParticipation', status: 'TO_BUILD', currentRepresentation: 'Not separated from ProgramDefinition.', primaryPaths: [], implementationBlock: 'N1', notes: 'Same gap as ProgramDefinition.' },
  { id: 'InvestmentCase', status: 'TO_BUILD', currentRepresentation: 'No representation found.', primaryPaths: [], implementationBlock: 'N2', notes: 'Zero code hits.' },
  { id: 'EvidencePlan', status: 'TO_BUILD', currentRepresentation: 'No representation found.', primaryPaths: [], implementationBlock: null, notes: 'Zero code hits, distinct from the Evidence type scaffold (see Evidence entry below).' },
  { id: 'DecisionRule', status: 'TO_BUILD', currentRepresentation: 'No representation found — distinct from the runtime eligibility-gate.ts classifier.', primaryPaths: [], implementationBlock: null, notes: 'Zero code hits.' },
  { id: 'Delivery / Opportunity', status: 'PARTIAL', currentRepresentation: 'Fragmented across 3 independent "opportunity" surfaces plus the Commons cross-tenant booking pattern.', primaryPaths: ['services/worker-opportunity/', 'app/worker/opportunities/', 'app/company/opportunities/', 'app/my-kora/opportunities/', 'supabase/migrations/025_commons_booking_contribution.sql'], implementationBlock: null, notes: 'Not unified under one Delivery object tied to a ProgramDefinition.' },
  { id: 'Observation', status: 'TO_BUILD', currentRepresentation: 'Closest analog is the UEF record (services/uef-review/) — not formally typed as an Observation with an eligible/exposed/aware/activated state chain, so not counted as implementing code for this object.', primaryPaths: [], implementationBlock: null, notes: 'No append-only eligible/exposed/aware/activated event table found in any of the 48 migrations.' },
  { id: 'Measurement', status: 'PARTIAL', currentRepresentation: 'IU, KORA Index, Confidence, Contribution all exist as computed outputs, but not unified under one lineage_id / snapshot model.', primaryPaths: ['lib/kora-engine/'], implementationBlock: 'B-LIN', notes: 'No kora.calculation_lineage table found.' },
  { id: 'InvestmentReview', status: 'TO_BUILD', currentRepresentation: 'No representation found.', primaryPaths: [], implementationBlock: 'N7', notes: 'Zero code hits.' },
  { id: 'DecisionEvent', status: 'TO_BUILD', currentRepresentation: 'No append-only, trigger-enforced table or type found anywhere.', primaryPaths: [], implementationBlock: 'N8', notes: 'Confirms I13 "assente" — zero CREATE TRIGGER for any decision/event table across all 48 migrations.' },
  { id: 'SubsequentObservation', status: 'TO_BUILD', currentRepresentation: 'No representation — Master Plan itself notes this is expected pre-second-cycle (time-limited, not code-limited).', primaryPaths: [], implementationBlock: null, notes: 'Master Plan: "si popola al secondo ciclo reale".' },
  { id: 'Worker', status: 'EXISTS', currentRepresentation: 'personal.worker_identity + lib/worker-identity/*.', primaryPaths: ['supabase/migrations/007_worker_provisioning.sql', 'supabase/migrations/048_worker_identity_lifecycle_protection.sql', 'lib/worker-identity/'], implementationBlock: null, notes: 'Mature, actively hardened — matches current branch HEAD.' },
  { id: 'PIB', status: 'EXISTS', currentRepresentation: 'personal.worker_pib, SECURITY DEFINER-only aggregation.', primaryPaths: ['supabase/migrations/018_worker_pib.sql', 'supabase/migrations/027_worker_individual_rls_refactor.sql', 'services/worker-pib/', 'services/pib-aggregation/'], implementationBlock: null, notes: 'Isolation independently confirmed — zero application-role policies (I3).' },
  { id: 'Partner', status: 'EXISTS', currentRepresentation: 'network.partner_identity / partner_profile.', primaryPaths: ['supabase/migrations/010_partner_profile.sql', 'supabase/migrations/012_partner_identity.sql'], implementationBlock: null, notes: 'No gap found.' },
  { id: 'Advisor', status: 'TO_BUILD', currentRepresentation: 'No representation. Its former demo-only surface (app/demo/advisor, backed by services/advisor-evidence-review/) was retired by CC-00 Residual /demo/** controlled retirement (2026-09-26, see svc.advisor-evidence-review\'s own entry) — nothing implements this object anymore. ADVISOR remains a real, active session role (RLS-enforced) with no canonical advisor roster/assignment/workspace model.', primaryPaths: [], implementationBlock: null, notes: 'Gap is vs CLAUDE.md\'s own documented app structure, not vs the Master Plan. Was PARTIAL (service existed, demo-only) before the 2026-09-26 retirement; downgraded to TO_BUILD, not silently removed — deferred as a real NETWORK-track capability, not resolved here.' },
  { id: 'Territory / Local Entity', status: 'TO_BUILD', currentRepresentation: 'No representation, typed or untyped: data/synthetic/collective-initiatives.json (formerly the only file with an untyped territory string field) was deleted by the B-TRUTH Contribution protected port (2026-09-01). commons.contribution_event / commons.post (migration 025) carry no territory column.', primaryPaths: [], implementationBlock: null, notes: 'Master Plan §1 [VERIFIED]: "territorio ... non ancora tipizzate" — still true, and now without even the informal synthetic representation. Re-typing (if/when scoped) starts from zero, not from a partial synthetic form.' },
  { id: 'Evidence', status: 'PARTIAL', currentRepresentation: 'Fields on UEF/ImpactUnit records; a types-only scaffold for a first-class entity exists but is not implemented.', primaryPaths: ['lib/types/domains/evidence.ts'], implementationBlock: 'Gate 2 (per the scaffold\'s own header)', notes: 'Verified by direct read (CC-002): "No persistence in KORA Foundation Light: types only."' },
  { id: 'Benchmark Cohort / Memory', status: 'TO_BUILD', currentRepresentation: 'No representation. Its former disclaimed narrative copy / demo mockup, app/demo/benchmarks (a UI placeholder, never code implementing this object), was retired by CC-00 Residual /demo/** controlled retirement (2026-09-26) — the same "no empirical benchmark before Delphi calibration" requirement it stated is already preserved in CLAUDE.md §6, so no separate deferred-capability note is needed for its removal.', primaryPaths: [], implementationBlock: 'N9', notes: 'Matches Master Plan\'s own "Stage 0" expectation until ≥10 companies.' },
  { id: 'MethodologySnapshot', status: 'EXISTS', currentRepresentation: 'B-SNAP (CC-015, D-F Option C): analytics.methodology_snapshot — one immutable row per calculation, all 9 Master Plan §11 fields (methodology_version, taxonomy_version, need_taxonomy_version, bc_calibration_version, contribution_config_version, factor_statuses, pipeline_version, config_hash, calculation_timestamp), plus provenance (AS_ORIGINALLY_CALCULATED / RESTATED_UNDER_METHODOLOGY). Referenced via nullable methodology_snapshot_id FK from activation_result, confidence_result, kora_index_result, bti_result (closes the prior BTI provenance gap), and impact_unit — historical rows keep NULL, never rewritten. Single construction authority: getMethodologySnapshot() in lib/methodology-config/v0.1.ts. need_taxonomy_version is `text NULL` (Needs domain not built) — the Master Plan\'s own nullable-for-"not yet applicable" convention (matching NeedObservation.related_program_definition_id / ProgramBrief.resulting_program_definition_id), not a status object inferred from the unrelated §23 factor-status vocabulary. Table has a BEFORE UPDATE OR DELETE trigger rejecting mutation for every role including service_role (true DB-level immutability, not just RLS/application discipline).', primaryPaths: ['supabase/migrations/049_methodology_snapshot.sql', 'lib/methodology-config/v0.1.ts', 'lib/live/persistence.ts'], implementationBlock: 'B-SNAP', notes: 'The pre-existing "quattro nomi" (KORA Methodology v0.1 / KORA-METHOD-v1.0 / v0.1.ts / kora_index_v3) remain untouched as historical/technical-only references — D-F ratified KORA Index v1.0 as the product label (getProductVersion(), same value as legacy getMethodologyVersion()) and "1.0" as the separate canonical methodology_version (getCanonicalMethodologyVersion()) for all NEW calculations; legacy methodology_version_id/methodology_version columns keep writing the product label unchanged, explicitly documented as legacy compatibility metadata, not the authority. One live bug fixed as part of this block: lib/kora-engine/run-kora-pipeline.ts\'s zero-data branch previously hardcoded methodologyVersion: \'KORA-METHOD-v1.0\' instead of calling getMethodologyVersion() like the normal path — now aligned. config_hash covers macroblock/BC/NM/shrinkage/PIB/calibration config only — version-label fields (taxonomy_version, bc_calibration_version, contribution_config_version) are deliberately excluded from the hash but remain independently distinguishable as their own persisted snapshot fields (proven by test).' },
  { id: 'Data Lineage', status: 'TO_BUILD', currentRepresentation: 'No canonical lineage reconstruction path found.', primaryPaths: [], implementationBlock: 'B-LIN', notes: 'Confirms I12 "assente" — zero lineage_id hits repo-wide (2 independent exhaustive greps).' },
  { id: 'KORA Link', status: 'EXISTS', currentRepresentation: 'Full schema, RLS, RPC — flag-disabled (KORA_LINK_ENABLED=false) but functionally complete.', primaryPaths: ['lib/kora-link/', 'supabase/migrations/034_kora_link_schema.sql'], implementationBlock: null, notes: 'Master Plan §33 [VERIFIED]: "gap DG-07 chiusi".' },
  { id: 'KORA Contribution', status: 'EXISTS', currentRepresentation: 'Service (single computation authority, DB-backed as of the B-TRUTH port 2026-09-01) + doctrine constants file, correctly kept separate from KORA Index.', primaryPaths: ['services/kora-contribution/KoraContributionService.ts', 'lib/kora-contribution/contribution-methodology.ts'], implementationBlock: null, notes: 'B-TRUTH Contribution audit (2026-09-01) corrected the CC-002 "genuine fragmentation" finding: lib/kora-contribution/contribution-methodology.ts is pure doctrine/policy constants, not a second computation implementation — the two files are non-competing (one computes, one documents policy). See lib.kora-contribution / svc.kora-contribution above for the full record.' },
];
