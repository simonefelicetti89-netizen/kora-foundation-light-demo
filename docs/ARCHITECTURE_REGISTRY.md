# KORA Architecture Registry

**Generated file — do not hand-edit.** Source: `lib/architecture/registry.ts`.
Regenerate with: `npx tsx scripts/generate-architecture-doc.ts`.

---

## Governance

- **This document (Architecture Registry)** describes the **CODE** — real components that exist in the repository today, classified by `ArchitectureStatus`.
- **The Target Ontology Implementation section below** describes the **target DOMAIN MODEL** the Master Plan defines — objects that may or may not have code behind them yet. These are two different axes and must never be conflated: a domain object can be `TO_BUILD` while unrelated code implementing a different capability is `CANONICAL`.
- **`docs/KORA_OFFICIAL_IMPLEMENTATION_MASTER_PLAN_v2.1.md` is the architectural truth.** This registry — like the rest of the repository — is the **operational truth**: what actually exists, not what should exist. Where they disagree, the Master Plan's own Read-Before-Write protocol governs (report `STATE_MATCH = NO`, do not silently reconcile).
- Classifications here do not anticipate any decision reserved for a future CC block or D-letter (D-A Confidence, D-B Decision Pack, D-C One Truth, D-D worker surfaces). Where two components compete, both carry the same `decisionRef` and neither is elevated.

---

## Architecture Components

78 components across 25 domains.

| ID | Domain | Path | Status | Future Core | Decision Ref | Notes |
|---|---|---|---|---|---|---|
| `app-surface.admin` | Admin | `app/admin/` | COMPLETE | — | — | Not individually traced per-subdirectory. Includes diagnostic-only tooling (kora-link-lab, live-spine-diagnostics, provisioning-diagnostics) that a future pass may want to split out separately. |
| `app-surface.demo` | Demo | `app/demo/` | CONSOLIDATE | — | B-TRUTH / D-C | Master Plan §13 explicitly names this cluster: "11 pagine /demo con ruolo DEMO_VIEWER, synth-only [DECISION REQUIRED D-C]" — count independently verified (exactly 11 page.tsx files under app/demo/). D-C RESOLVED (founder decision, 2026-08-31): these 11 pages are intentionally retained as DEMO_RUNTIME, classified the same way as the Ingestion/UEF chain (see svc.ingestion-pipeline, svc.uef-review) — NOT required to be retired merely to satisfy One Product/One Truth. Governing invariant: DEMO MAY USE SYNTHETIC DATA; LIVE MUST NEVER FALL BACK TO DEMO OR SYNTHETIC DATA. Retention is conditional, not unconditional: acceptable only while (1) these pages remain clearly identifiable as demo experiences, (2) they do not masquerade as live company data, (3) live authenticated company surfaces never depend on or fall back to them, (4) demo/live separation remains structurally testable, (5) they are excluded from canonical analytical truth. This resolution does not authorize synthetic data in any canonical live path — status remains CONSOLIDATE (not CANONICAL, not FROZEN) pending the still-open remediation items this cluster depends on (svc.demo-data, svc.scoring-simulator, svc.access-control — all still CANONICAL/unretired per Master Plan §32). |
| `app-surface.my-kora` | Worker | `app/my-kora/` | COMPLETE | Y | CC-024 / D-D | Neutral status. Master Plan §19 B-WORKER row states "/my-kora gira su AccessControlService" — CC-001R verified this claim does NOT match current code (app/my-kora/page.tsx does not import AccessControlService); flagged as an untagged (no [VERIFIED]) Master Plan claim, not corrected here. |
| `app-surface.worker` | Worker | `app/worker/` | COMPLETE | Y | CC-024 / D-D | Neutral status — CC-024's 12-dimension architecture matrix owns the consolidation decision, not a line-count comparison. |
| `app.company-reports-board-pack` | Reporting (dead route) | `app/company/reports/board-pack/page.tsx` | DEAD | — | Master Plan §32 Safe Deletion Plan | Verified: pure redirect() call, no logic. Master Plan §32: "13 L, solo redirect [VERIFIED]". Referenced by 2 files (app/company/status/page.tsx, lib/feature-discovery/index.ts) and 4 tests exercising the redirect itself — those link targets would need updating on deletion. |
| `db.commons-booking-schema` | Commons | `supabase/migrations/025_commons_booking_contribution.sql` | CANONICAL | Y | — | Master Plan §33 [VERIFIED]. SECURITY DEFINER function booking_aggregate_for_promoter() enforces the cross-tenant boundary. This is the concrete evidence behind the Master Plan's Program/Participation/Case ontology correction (§3.0). |
| `db.kora-link-schema` | KORA Link | `supabase/migrations/034_kora_link_schema.sql` | FUTURE_CORE | Y | — | Hardened through migrations 035/036/039/042. auth.uid()-based identity resolution confirmed inside SECURITY DEFINER RPC bodies (I4). |
| `db.worker-pib-schema` | PIB | `supabase/migrations/018_worker_pib.sql` | FUTURE_CORE | Y | — | I3 confirmed: zero application-role policies on this table (migration 027 refactor). Never employer-visible. |
| `lib.auth` | Auth | `lib/auth/access-matrix.ts` | CANONICAL | — | — | Imported directly by middleware.ts. |
| `lib.decision-pack` | Reporting | `lib/decision-pack/` | CANONICAL | — | CC-013 / D-B | D-B resolved (CC-005/CC-013), OPTION A: canonical KORA Decision Pack implementation. Builder/renderer/PDF-runtime seam already existed cleanly pre-CC-013 (pdf-data.ts builds the content model from canonical persisted rows, html-template.ts and pdf-runtime.ts are pure functions of that model with zero DB access) — formalized via doc header, no module split needed. All 4 live routes (company preview/pdf, admin preview/pdf) confirmed to use this exact path with no accidental duplication. Confirmed single implementation of the live PDF layer itself (2 independent passes found no second PDF generator). CC-005/D-B CLOSEOUT (2026-09-02): see the CC-005 CLOSEOUT / D-B RESOLUTION header block above — formal ratification, plus proof (tests/unit/cc005-decision-pack-limitations-migration.test.ts) that every mandatory disclaimer from ReportGeneratorService.getDecisionPackLimitations() is already present here, and that no demo-specific legacy wording leaks into live output. |
| `lib.ingestion-cluster` | Ingestion | `lib/data-intake/` | INVESTIGATE | — | — | BACKLOG — HUMAN TRIAGE. CC-002 traced part of this cluster: ingestion-normalizer→ingestion-pipeline is a real 2-stage chain; ingestion-simulator and company-data-intake are separate parallel entry points. The four lib/ sub-directories themselves were not individually caller-traced. Finding: MIXED. |
| `lib.kora-contribution` | Contribution | `lib/kora-contribution/contribution-methodology.ts` | CANONICAL | — | — | Corrected by the B-TRUTH Contribution audit and protected port (2026-09-01): full-file read confirms zero computation and zero imports — pure exported constants (CONTRIBUTION_IS_KORA_INDEX_COMPONENT, CONTRIBUTION_SCORE_PRESENTATION_MODE, CONTRIBUTION_FL_PATH/CONTRIBUTION_PILOT_PATH, etc.) plus doctrine comments, kept up to date with the DB-backed port. Not a competing implementation of svc.kora-contribution — the prior "genuine fragmentation" / INVESTIGATE classification was a misread of an unconsumed-computation file as a second implementation. Consumed by lib/partner-activities + lib/live/contribution-lineage.ts, both real, unrelated downstreams. |
| `lib.kora-engine` | Methodology | `lib/kora-engine/` | CANONICAL | — | — | Matches doc 10 methodology. run-kora-pipeline.ts orchestrates; consumed by LiveScoringAdapter. D-A (CC-011): confidence-engine.ts is the canonical Confidence implementation — scale/banding centralized via normalizeConfidenceScore/getConfidenceBand; lib/live/persistence.ts consumes them rather than owning independent methodology. |
| `lib.kora-link` | KORA Link | `lib/kora-link/` | FUTURE_CORE | Y | — | Master Plan §33: "FUTURE CORE, gap DG-07 chiusi [VERIFIED]". Flag-gated off (KORA_LINK_ENABLED=false) but functionally complete; RLS-dense (migration 035 alone ~23 policies). |
| `lib.live` | Live data | `lib/live/` | CANONICAL | — | — | Includes contribution-lineage.ts, which carries its own in-code "NAMING DISAMBIGUATION (C-6)" note acknowledging a naming collision between its ContributionRole concept and KORA Contribution — self-flagged, unresolved debt, not fixed here. B-SNAP (CC-015): persistence.ts creates one methodology_snapshot row per calculation via the canonical getMethodologySnapshot() builder and links it to every result table it writes — no independent snapshot construction here, same discipline as the CC-011 Confidence scale/banding contract. |
| `lib.methodology-config` | Methodology | `lib/methodology-config/v0.1.ts` | CANONICAL | — | — | Matches CLAUDE.md's "v0.1.ts" filename requirement. Consumed by report-factory, kora-engine, scoring. B-BC (CC-009): added getBCByActionFamily(), reading data/methodology/methodology-config.json's new bc_by_action_family key — same pattern as nm_functions/shrinkage/pib. B-SNAP (CC-015): added getMethodologySnapshot()/computeConfigHash()/getFactorStatuses() — single construction authority for the Methodology Snapshot; deterministic sha256 over macroblock/BC/NM/shrinkage/PIB/calibration config only (never timestamps, tenant identity, or results). |
| `lib.permissions` | Privacy | `lib/permissions/index.ts` | CANONICAL | — | — | 10 callers. |
| `lib.privacy` | Privacy | `lib/privacy/group-threshold.ts` | CANONICAL | — | — | CC-002: DEFAULT_MIN_GROUP_SIZE now re-exports the canonical lib/constants/kora.ts value (I2). Added detectGroupTotalReconciliationRisk() (I5) — proves but does not fix a live differencing risk (see CC-002 report §14 remaining risks). |
| `lib.scoring-result` | Scoring | `lib/scoring-result/index.ts` | CANONICAL | — | — | In-code contract: app/components must never import ScoringSimulatorService/DynamicScoringPreviewService/run-kora-pipeline directly. CC-002 found 6 real violations of this contract (not fixed — CC-010/B-TRUTH scope). |
| `lib.types.domains.evidence` | Evidence | `lib/types/domains/evidence.ts` | FUTURE_CORE | Y | — | Own header: "No persistence in KORA Foundation Light: types only... Pilot+ engineer: implement EvidenceRepository." Verified by direct read (CC-002). |
| `lib.worker-identity` | Worker | `lib/worker-identity/` | CANONICAL | Y | — | Matches current branch HEAD (worker identity lifecycle protection, migration 048). |
| `svc.access-control` | Demo/Access | `services/access-control/AccessControlService.ts` | CONSOLIDATE | — | B-TRUTH | Zero callers repo-wide (verified). Master Plan §32 names it for removal at end of B-TRUTH alongside demo-data/scoring-simulator. |
| `svc.account` | Admin | `services/account/AccountProvisioningService.ts` | CONSOLIDATE | — | B-TRUTH AccountProvisioningService Pipeline Role Migration (2026-09-06) | PRIOR HISTORY (accurate as of its own time, preserved verbatim): "13 real callers." — this count was stale by the time this migration re-verified it: independent repo-wide re-verification found exactly 2 real callers (getAccountsForCompany() from app/admin/pipeline/_components/PilotLifecycleClient.tsx, getCurrentDemoUser() from app/my-kora/page.tsx), plus 19 other zero-caller methods (createCompanyUserDraft/createCompanyAdminDraft, inviteCompanyUser/revokeInvite/resetInvite, disableUser/suspendUser/deleteDemoUser, getUserAccessProfile, canAccessCompany/canAccessAdmin, getVisibleSections, getAccountStatusBadge/getInvitationStatusBadge, getAccountsForTenant, getKoraStaffAccounts, getWorkerAccountsForCompany, getCompanyAdmins, getPrimaryCompanyAdmin) not removed by this migration — no opportunistic cleanup, out of this migration's narrow pipeline-role scope. NARROWING (2026-09-06): the 2 real callers were traced into exactly two distinct responsibilities — PIPELINE_ADMIN_PROVISIONING (getAccountsForCompany, whose caller only ever read accounts.length > 0, fed into the Pilot Lifecycle's hasCompanyUser flag) and MY_KORA_SESSION_IDENTITY (getCurrentDemoUser, resolving which synthetic persona/company_id the My KORA preview renders as). Only the pipeline role was migrated: a new pure view builder, lib/live/account-provisioning-status-view.ts, reads Supabase Auth (auth.users + app_metadata.kora_tenant_id/kora_role) via the same filter app/api/admin/company-users/route.ts's own GET handler already uses (COMPANY_ROLES = ['COMPANY_ADMIN']) — not a new auth system, a read of the existing canonical one. getAccountsForCompany() itself was deleted (pipeline-only, zero remaining callers). getCurrentDemoUser() and its data/synthetic/user-accounts.json dependency are UNTOUCHED — that fixture is still required by the surviving method and is NOT removed from the I9 allowlist (12/20 unchanged by this PR). No tenant_kind branch, no KoraTest special case — the canonical read is identical for any tenant. AdminPreviewService, final scoring, and B-WORKER (WorkerProvisioningService, WorkerAchievementService, WorkerSpaceCapabilityService) are explicitly untouched. See tests/unit/b-truth-accountprovisioning-pipeline-role-migration.test.ts for the regression guard proving both the migration and the scope boundary. |
| `svc.activation-opportunity` | Worker | `services/activation-opportunity/ActivationOpportunityService.ts` | COMPLETE | — | — | 3 callers. Overlap with worker-opportunity not resolved. |
| `svc.activation-safeguard` | Methodology | `services/activation-safeguard/ActivationSafeguardService.ts` | CANONICAL | — | — | 6 callers. Matches CLAUDE.md thresholds exactly. |
| `svc.admin-preview` | Admin/Demo | `services/admin-preview/AdminPreviewService.ts` | FROZEN | — | — | 7 callers, demo-only. |
| `svc.advisor-evidence-review` | Advisor | `services/advisor-evidence-review/AdvisorEvidenceReviewService.ts` | COMPLETE | — | — | Demo-only caller (app/demo/advisor); no live /advisor workspace route exists despite CLAUDE.md documenting one. |
| `svc.booking-request` | Commons (legacy) | `services/booking-request/BookingRequestService.ts` | DEAD | — | Master Plan §32 Safe Deletion Plan | Verified: 24 lines, zero callers anywhere (no import, no dynamic/string reference, no test), no backing DB table. Master Plan §32 independently confirms: "24 L, ritorna []/null [VERIFIED]" — commons/BookingService resta. |
| `svc.bti-intelligence` | Financial | `services/bti-intelligence/BTIIntelligenceService.ts` | COMPLETE | — | — | 1 caller. Confirmed distinct responsibility from budget-to-human-impact (imports only its result type, not the service) — not a duplicate. |
| `svc.budget-to-human-impact` | Financial | `services/budget-to-human-impact/BudgetToHumanImpactService.ts` | DEAD | — | B-TRUTH Retire Orphan Synthetic BTI Chain (2026-08-31) | DELETED (file no longer exists at primaryPath, kept here only as historical record). Its 3 remaining callers — DynamicScoringPreviewService, ReportGeneratorService, CompanyIntelligenceService — were each independently confirmed to have zero reachable callers from any app/ entry point (ReportGeneratorService and CompanyIntelligenceService were already established as orphaned in prior B-TRUTH work this same week; DynamicScoringPreviewService's only caller is the orphaned ReportGeneratorService). The real, live BTI path already exists and is independently verified: analytics.bti_result, read directly by app/api/admin/company-workspace/route.ts. Its sole seed file (data/synthetic/budget-to-human-impact.json) was deleted alongside it — zero remaining consumers, proven before deletion. The 3 consumers were trimmed (BTI-specific calls removed, replaced with null-safe equivalents matching their own pre-existing null-handling branches) and left otherwise unredesigned and unretired — their overall registry fate remains a separate, still-open decision (svc.report-generator and svc.company-intelligence stay INVESTIGATE). One previously-unlisted reference was found and fixed: components/kora-index/EconomicReliefPanel.tsx held a type-only import of EconomicReliefSummary, copied locally into the component (plain data shape, no behavior) rather than relocated — the component remains orphaned and otherwise untouched. I9 reduced 25->24 files (37->36 imports) — the first genuine I9 reduction since Root Control Room Wave 2. |
| `svc.care-economy` | Intelligence | `services/care-economy/CareEconomyIntelligenceService.ts` | COMPLETE | — | — | 3 callers. |
| `svc.commons` | Commons | `services/commons/CommonsService.ts` | CANONICAL | — | — | 7 callers. Governance Patch 01 (2026-08-31): Master Plan §13 explicitly names this service's dual discovery-path issue — "CommonsService, che importa commons-initiatives.json per la discovery mentre getPublishedInitiatives legge dal DB live — due percorsi di scoperta nello stesso servizio" — as something to eliminate/consolidate, and §30 explicitly reserves CC-052 ("Ritiro discovery sintetica CommonsService") for that fix. Status left CANONICAL (the service functions; the defect is tracked via decisionRef, not hidden) pending CC-052's own dedicated audit — not reclassified further by this patch. CC-051/B-024 closeout (2026-08-31): migration 024_commons_initiative_fields.sql (opening_grade, geolocation, capacities, cross-company RLS policy commons_post_worker_cross_company_select) is confirmed CLOSED — additive-only, idempotent (ADD COLUMN/INDEX/POLICY IF NOT EXISTS guards throughout except the single ADD CONSTRAINT, standard for a migration applied exactly once via the tracked migration ledger), zero conflicts with migrations 025-048 (only 025 and 046 reference commons.post at all, neither touches 024's columns), verified applying cleanly against a fresh Postgres via the mandatory DB-backed CI gate (all 42 migrations through 049). No rollback artifact exists for 024 specifically (supabase/rollback/ has entries for 029-048 but not 024) — additive/backward-compatible nature makes one lower-risk to omit, but this is a real, not a fabricated, gap. tests/unit/b165-commons-initiatives.test.ts verifies the RLS policy and opening_grade CHECK constraints structurally (asserts exact SQL text) but there is no live-DB behavioral test proving the commons_post_worker_cross_company_select policy actually grants cross-tenant visibility against real rows — a known gap, not a blocker to this closeout, worth closing before CC-052 relies on it. The migration file's own header ("Gate 2 OPEN — written, NOT applied") is stale and asserted verbatim by b165's own "Gate 2 annotation" test; left uncorrected in this pass since fixing it requires also updating that test, both out of this closeout's narrow scope. CC-051/B-024 = COMPLETE. CC-052's schema prerequisite is satisfied; CC-052 is unblocked. CC-052 (2026-08-31): PARTIAL, not complete. Finding: getPublishedInitiatives() is confirmed the SOLE authority for real worker/company/admin-facing initiative discovery — every genuine "find and join a real initiative" caller (app/api/commons/initiatives/route.ts, app/company/commons/page.tsx via a direct commons.post query) already reads live, zero synthetic dependency; §13's "due percorsi di scoperta" concern does not apply to the actual product discovery mechanism today. What remains synthetic is narrower than "discovery": getByPillar/getByType were dead code (zero callers) and are now deleted. The 3 surviving synthetic methods (getInitiatives, getFeaturedInitiatives, getNetworkStats) have exactly 2 real callers left, both already-governed, explicitly-labeled PREVIEW surfaces, not discovery: app/commons/page.tsx (B185 — its own layout.tsx states "shared network preview routes — not public... synthetic demo data labelled 'Rete pilota dimostrativa'", KORA_ADMIN-only in practice) and app/my-kora/page.tsx's featured-commons widget (B81-B PREVIEW route, D-D/CC-024 territory — "Pilot+: WorkerSessionProvider resolves LIVE session... no component changes required — only provider and service swap", i.e. its own documented future is D-D's job, not CC-052's). Real, verified parity gap found: lib/commons/types.ts's CommonsInitiative (synthetic — description, initiative_type, owner_organization, owner_sector, location_type enum, visibility, end_date, capacity, participants_enrolled, status lifecycle open/upcoming/full/completed, activation_potential, verification_possible, tags, and a literal synthetic_demo_data: true type field) has NO live equivalent on commons.post even after migration 024 — CommonsPostWorkerView carries only id/title/body/category/pillar/timestamps/opening_grade/geo/capacity_internal/capacity_cross. Confirmed by app/company/commons/page.tsx's own direct live query, which selects a much narrower field set and renders none of the richer fields. Retiring the remaining synthetic import requires either a schema-extension decision (new columns for status lifecycle/activation_potential/verification_possible/participant counts — real new product functionality) or a product-redesign/formal-reclassification decision for these 2 preview surfaces — both explicitly out of this task's narrow scope (no new schema, no product-semantics change, no touching D-D/B-WORKER territory). CC_052_COMPLETE = NO. Recommend a founder decision on these 2 preview surfaces' future before any further synthetic-import retirement here. I9 unchanged by this pass (24 files/36 imports) — dead-code removal does not affect the allowlist since the file itself remains listed. CC-052 FINAL CLOSURE (2026-08-31): field disposition ratified (KEEP/RENAME description/end_date/capacity, DERIVE owner_organization/owner_sector/participants_enrolled/status, DROP visibility/tags, DEFER activation_potential/location_type/stored verification_possible — no schema added for the deferred set, no placeholder values invented). Both remaining callers migrated onto canonical live discovery, zero schema change required: app/commons/page.tsx is now a server component reading the new getPublishedInitiativesAdmin() (this file), enriched via a lib/commons/discovery-view.ts adapter — analytics.tenant join for owner_organization/owner_sector, the pre-existing commons.booking_aggregate_for_promoter() SECURITY DEFINER RPC (services/commons/BookingService.ts, unchanged) for participants_enrolled, and a pure date/capacity derivation for status. app/my-kora/page.tsx's widget now fetches the same canonical /api/commons/initiatives route a real session uses, degrading to an honest empty state (not a synthetic fallback) since that page's own session is demo-state with no live JWT (B81-B, D-D territory, unchanged by this task). data/synthetic/commons-initiatives.json deleted; the CommonsService synthetic class (getInitiatives/getFeaturedInitiatives/getNetworkStats, getByPillar/getByType already removed) deleted entirely — this file now contains only getPublishedInitiatives and getPublishedInitiativesAdmin, both live. tests/unit/b97b-commons-foundation.test.ts (entirely about the removed class) deleted; tests/unit/cc052-commons-discovery-view.test.ts added for the new adapter's pure functions; tests/integration/rls-11-commons-cross-company.test.ts added and wired into the mandatory DB-backed CI gate, closing the exact gap the CC-051/B-024 closeout note above flagged (no live-DB behavioral proof of commons_post_worker_cross_company_select) — proves a WORKER discovers a cross_company post from another tenant but not a company_internal one, with a same-tenant positive control. I9 reduced 24->23 files (36->35 imports) — the first reduction attributable to a full caller migration, not a dead-code trim. Master Plan §13's "due percorsi di scoperta nello stesso servizio" defect is resolved: one discovery path, live, RLS-scoped, used identically by a real tenant and a future NO-DEMO-RUNTIME synthetic tenant (Patch 03) alike. CC_052_COMPLETE = YES. The migration file header / b165 "Gate 2 annotation" test staleness noted in the CC-051 entry above remains open, unrelated to this closure, left for a dedicated follow-up. |
| `svc.commons.booking` | Commons | `services/commons/BookingService.ts` | CANONICAL | Y | — | Master Plan §33 Do-Not-Delete: "vivo e canonico" [VERIFIED]. 5 live API routes, SECURITY DEFINER cross-tenant boundary check (migration 025). |
| `svc.company-data-intake` | Ingestion | `services/company-data-intake/CompanyDataIntakeService.ts` | DEAD | — | B-TRUTH CompanyDataIntakeService Canonical Migration (2026-09-05) | DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-05), PR 3 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan (PR 1 = B-TRUTH KoraTest Canonical Foundation; PR 2 = B-TRUTH TenantService Canonical Migration): independently re-verified before deletion — both real runtime callers (app/admin/pipeline/_components/PilotLifecycleClient.tsx, services/report-factory/ReportFactoryService.ts) individually confirmed, zero type-only callers. Only the 3 fields either real caller actually consumed (batch_count, intake_status, review_required_rows — out of ~16 fields on the legacy getDataReadinessSummary() shape) were migrated, via a new shared pure view builder, lib/live/data-intake-status-view.ts, fed by analytics.source_batch (batch existence + latest batch_status by created_at, matching the same "latest row" selection precedent already used elsewhere in lib/live and the canonical scoring route) and analytics.uef_record WHERE review_status='pending_review' (the same counting query app/api/admin/uef/review/route.ts's own GET handler already uses, reused not reinvented). The legacy 'blocked_missing_required_fields' intake-status value is NOT reproduced — it was a post-hoc heuristic for malformed rows that, in the real pipeline, are already rejected at the canonical upload boundary (PII-scan/validation in accept/route.ts) before a source_batch row is ever created; neither real caller's behavior ever depended on this value firing — this is a stale-heuristic retirement, not a capability loss. app/admin/pipeline/page.tsx (a Server Component since the prior PR) fetches this canonical view once and passes it to both PilotLifecycleClient.tsx and, transitively, ReportFactoryService — avoiding a duplicate query, same discipline as the CanonicalTenantStatus parameter added by the prior PR. ReportFactoryService.getDecisionPackFactoryStatus/computeBlockingReasons gained a third parameter, dataIntake: CanonicalDataIntakeStatus, replacing the internal companyDataIntakeService dependency entirely — its own still-synthetic hasKoraIndex/getLatestDecisionPackVersion checks and Decision Pack version source are UNCHANGED, unmigrated, explicitly out of scope (a separate, later slice). No tenant_kind branch introduced — identical view/query shape for KoraTest Srl (tenant_kind=TEST) and any tenant_kind=LIVE tenant. One real gap found and fixed in scripts/koratest-canonical-seed.ts (PR 1's own foundation script) while validating this migration against real KoraTest data: it never updated source_batch.batch_status to 'approved' after scoring completed (unlike the real run-approved-batch/route.ts, which does) — fixed by adding the same update, in its own labeled step. Its 3 sole seed files (data/synthetic/company-budget-fiscal-plans.json, company-raw-data-batches.json, company-raw-data-rows.json) deleted alongside it — confirmed zero remaining real consumers of any of the three. Its type family (CompanyBudgetFiscalPlan, CompanyRawDataBatch, CompanyRawDataRow, CompanyDataReadinessSummary, CompanyDataIntakeStatus, all defined in @/lib/types, not by this file) was deliberately NOT deleted — no opportunistic cleanup, confirmed zero other consumers but left in place, same discipline as every prior B-TRUTH retirement this session. AdminPreviewService, AccountProvisioningService, and ReportFactoryService's Decision Pack version source are explicitly untouched — one PR = one bounded migration. CompanyDataIntakeService.ts was an I9 allowlist entry — twelfth genuine I9 reduction via a real caller migration: 14->13 files (24->21 imports). See tests/unit/b-truth-company-data-intake-canonical-migration.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): GOVERNANCE CORRECTION (2026-08-30, Demo/Orphan Chain Hardening): status corrected CANONICAL -> CONSOLIDATE. The previous CANONICAL label conflated two distinct things: (1) the Data Intake PRODUCT DOMAIN, which is genuinely canonical and live — lib/data-intake/* (registered as lib.ingestion-cluster; csv/excel parsers, column mapping, evidence attachment/provenance), analytics.source_batch, analytics.uploaded_record, analytics.uef_record, and the live /admin/data-intake/* and /admin/uef/* API routes, all verified reachable and DB-backed; and (2) THIS SERVICE, CompanyDataIntakeService, which reads 100% synthetic JSON (data/synthetic/company-budget-fiscal-plans.json, company-raw-data-batches.json, company-raw-data-rows.json) and executes none of the canonical path. COMPANY_DATA_INTAKE_SERVICE_IS_CANONICAL = NO. Reading synthetic demo rows does not become canonical merely because the product domain has a live implementation elsewhere. CONSOLIDATE (not LEGACY) because it remains reachable intentionally for the retained pipeline demo surface (KEEP_DEMO decision, B-TRUTH Demo/Orphan Chain Audit) — the same pattern already used for svc.report-factory and svc.tenant — not because it is frozen/incompatible-with-canonicalization; runtime convergence with the canonical ingestion path (lib.ingestion-cluster) remains simply incomplete, not abandoned. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30) terminology correction: 3 runtime callers (app/admin/pipeline/page.tsx, services/report-factory/ReportFactoryService.ts, services/company-intelligence/CompanyIntelligenceService.ts) — none is a production/live caller. Pipeline hardcodes DEMO_COMPANY_ID = 'meridiana-group'; ReportFactoryService's only caller is pipeline (same constant), so its call is demo-rooted too. CompanyIntelligenceService's call is unreachable dead code (see svc.company-intelligence — 0 callers). Do not describe any of these as "real"/"production" callers merely to distinguish them from tests; the correct terms are runtime caller (a literal call site) vs. production/live caller (reachable via a real user/admin flow with a real tenant id) vs. demo caller (reachable only through a synthetic-identity demo flow). B-TRUTH Demo/Orphan Chain Audit (2026-08-30): reachability trim — verified per-method, repo-wide (including internal this.* calls), that the only reachable public surface is getDataReadinessSummary plus the internal helpers it actually calls (getBudgetFiscalPlan, getRawDataBatches, getRawDataRows, getNextAction, and 6 private classification predicates). Removed 12 public methods with zero callers anywhere (getAvailableCompanies, getFiscalPerimeterSummary, getRawDataRowsForBatch, getRowsReadyForIngestion, getEligibleCandidates, getLimitedCandidates, getBlockedCandidates, getStructuralPolicyRows, getReviewRequiredRows, getRowsWithMissingFields, validateRawDataBatch, getPipelineLinks) — none reached internally, none tested. tsc + full suite (289 files/11128 tests) unchanged after removal. Previously "6 callers" — stale, pre-dates B-TRUTH Gen 0/1 Retirement Wave 1 and Root Control Room Wave 2/3, which removed several. |
| `svc.company-intelligence` | Admin | `services/company-intelligence/CompanyIntelligenceService.ts` | DEAD | — | CC-020A Company Intelligence Capability Retirement (2026-08-31, narrowed) | CC-020A (2026-08-31) confirmed 0 runtime callers repo-wide, unchanged from the prior INVESTIGATE finding. First pass of this retirement (same day) also deleted CompanyOnboardingService as a claimed 'second-order orphan' — that was WRONG and has been reverted (see svc.company-onboarding): CompanyOnboardingService is a Master-Plan-anchored competing implementation of svc.company-setup, not disposable just because its only caller was this service. This entry records the corrected, narrower, capability-level decision: (1) CompanyIntelligenceService.resolveRiskLevel() is an ad-hoc heuristic with no Master Plan methodology anywhere in the frozen text; (2) no N1-N14/NB capability needs, supersedes, or overlaps it (checked explicitly against Investment Map, Needs Map, Decision Memory, Economic Case/N11, Cockpit/N12, Portfolio Review/N13 — N13 is about a company reviewing ITS OWN investment portfolio, not KORA managing its client tenant roster; the word 'portfolio' is shared, the referent is not); (3) no canonical replacement SERVICE exists for this specific aggregation — do not claim otherwise — but every live-relevant field it read already has a live source elsewhere (analytics.tenant.onboarding_status/data_readiness_status/decision_pack_status, already read directly by /api/admin/company-console; analytics.kora_index_result, read by the Gen3 workspace tab; real worker provisioning at /admin/workers). This is an OBSOLETE-CAPABILITY retirement (the business need is gone), distinct in kind from a LIVE-MIGRATION retirement (BTI, Lifecycle, Pillar Adoption) where a real replacement took over the same function — Master Plan §32's literal deletion bar ('live migration complete and verified') does not literally cover this case; this decisionRef is the explicit, capability-level founder record §32 implicitly requires for it. If KORA ever needs a cross-tenant admin readiness/action-queue view again, it should be built fresh against the live fields named above, not by reviving this heuristic. See also docs/KORA_OFFICIAL_IMPLEMENTATION_MASTER_PLAN_v2.1_PATCH_01.md §5, which documents this same finding at the governance-patch level. |
| `svc.company-onboarding` | Company | `services/company-onboarding/CompanyOnboardingService.ts` | CANONICAL | — | B-TRUTH Company Onboarding Canonicalization (2026-09-01) | FIXED ARCHITECTURE DECISION (this task, 2026-09-01, explicit founder instruction): CompanySetup and CompanyOnboarding are NOT competing implementations. Distinct responsibilities — CompanySetup is the pre-provisioning wizard/setup flow (services/company-setup/CompanySetupService.ts, in-code templates, zero synthetic dependency); CompanyOnboarding is post-provisioning readiness/status logic. This supersedes the CC-020A / Master Plan §33 "competing implementation" framing recorded in this entry's own prior history (see tests/unit/cc020a-retire-company-intelligence.test.ts for that superseded record). Synthetic read (data/synthetic/company-onboarding.json) retired; canonical authority is analytics.tenant (onboarding_status/data_readiness_status/decision_pack_status, already read live elsewhere by /api/admin/company-console) + personal.workforce_baseline (via the already-canonical lib/live/workforce-baseline-view.ts, not re-derived) through a new pure view builder, lib/live/company-onboarding-view.ts. Derived logic preserved: isFoundationLightEligible, getPipelineReadiness, getNextBestAction, getPrivacyThresholdWarnings — all now DB-backed, same LIVE/DEMO-kind-tenant code path (no tenant_kind branch). 7 accessor methods with no canonical equivalent (rich CompanyProfile fields, HR KPI context, program-data budgets — none stored live anywhere in the schema) and zero real callers were retired, not fabricated. Zero real runtime callers existed before or after this port — canonicalized per this task's own explicit instruction to do so regardless. See lib/live/company-onboarding-view.ts header for the full KEEP/DERIVE/DROP field disposition and tests/unit/btruth-company-onboarding-view.test.ts / tests/integration/rls-15-company-onboarding-tenant-kind-parity.test.ts for verification. |
| `svc.company-setup` | Company | `services/company-setup/CompanySetupService.ts` | INVESTIGATE | — | — | Zero real callers (only route-string literals reference it). Master Plan §32/§33 state explicitly, twice: "company-setup e report-generator restano INVESTIGATE" — this status is unchanged by the B-TRUTH Company Onboarding Canonicalization (2026-09-01), which corrected only the competingWith claim: CompanySetup and CompanyOnboarding are distinct responsibilities (pre-provisioning wizard vs. post-provisioning readiness/status), not competing implementations. See svc.company-onboarding. |
| `svc.confidence-score` | Methodology | `services/confidence-score/ConfidenceScoreService.ts` | CONSOLIDATE | — | CC-011 / D-A | D-A resolved (CC-004/CC-011): lib/kora-engine/confidence-engine.ts is canonical. Still zero production callers — retained non-canonical pending a formal retirement decision, not deleted or marked DEAD per Master Plan discipline (no callers ≠ dead). |
| `svc.demo-data` | Demo | `services/demo-data/DemoDataService.ts` | CONSOLIDATE | — | B-TRUTH | One test-only caller; most demo pages import data/synthetic/*.json directly instead. Master Plan §32 names it for removal at end of B-TRUTH. |
| `svc.dynamic-cv` | Worker | `services/dynamic-cv/DynamicCVService.ts` | FUTURE_CORE | Y | — | Master Plan §33 Do-Not-Delete list. 5 callers. |
| `svc.dynamic-scoring` | Scoring | `services/dynamic-scoring/DynamicScoringPreviewService.ts` | DEAD | — | B-TRUTH Preview Scoring Retirement (2026-09-03) | DELETED (both files no longer exist at their paths, kept here only as historical record). Prior note ("6 callers, narrow documented use... reachable via IScoringService.ts, PreviewScoringAdapter.ts, lib/scoring-result/index.ts") was STALE/WRONG, corrected here: independently re-verified, IScoringService.ts and lib/scoring-result/index.ts only ever mentioned DynamicScoringPreviewService in comments ("NEVER import..."), never a real import — lib/scoring-result/index.ts's actual dispatch only ever branched on environment === 'demo'/'live', never wired a PREVIEW path despite IScoringService.ts documenting a "3 scoring paths" model. The ONLY real import was PreviewScoringAdapter.ts, whose own only real-ish consumer was ReportGeneratorService's Decision Pack preview section — retired 2026-09-02. RETIREMENT (2026-09-03): both files independently re-verified to have zero real runtime callers (static, dynamic, barrel, and type-only imports all checked, repo-wide) before deletion. No unique methodology: every DynamicScoringPreviewService computation was either an explicitly self-labelled proxy approximation ("stima proxy", hardcoded WB=0.35/EQ=0.40 placeholders) or direct reuse of activationSafeguardService.evaluate() / scoringSimulatorService.computeKoraIndexV3() (both unchanged, unaffected). ScoringPathMode narrowed from 'DEMO'\|'PREVIEW'\|'LIVE' to 'DEMO'\|'LIVE' in services/scoring/IScoringService.ts (confirmed zero other real consumers of the 'PREVIEW' literal via that type — DemoScoringAdapter/LiveScoringAdapter use only 'DEMO'/'LIVE'). tests/unit/b89b-architecture-hardening.test.ts's adapter-conformance suite updated to test only DemoScoringAdapter/LiveScoringAdapter (2, not 3) — coverage for both remaining adapters unchanged. UEFReviewService.ts (DynamicScoringPreviewService's sole upstream dependency) is untouched — not retired, not modified; its own real-caller count is now 0 (documented, not acted on) but it remains protected by demo-guard-01's separate, deliberate, method-level assertion, a sequencing decision unrelated to this retirement. ReportFactoryService, the rest of the Ingestion/UEF legacy chain (IngestionPipelineService, EligibilityGateService, UEFReviewService), ExplainabilityService live glossary, and the final scoring group are explicitly untouched. Neither file was ever an I9 allowlist entry — I9 unaffected by this retirement (17/27 unchanged). See tests/unit/b-truth-preview-scoring-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. |
| `svc.eligibility-gate` | Ingestion | `services/eligibility-gate/EligibilityGateService.ts` | DEAD | — | B-TRUTH Eligibility Gate Retirement (2026-09-03) | DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-03): independently re-verified before deletion — all 4 public methods (classifyAction, classifyActions, getActionTaxonomy, getEligibilitySummary) individually confirmed zero real callers, zero type-only callers. CORRECTION to this entry's own prior claims (recorded here, not silently dropped): this service's own header, and this entry's prior "4 callers" note, both implied real usage — the header specifically claimed "Admin BCM Mapping Review (AI Upload Studio)", "Pre-ingestion operator classification UI", and "Taxonomy exploration" as live consumers. Tracing the actual live routes found this stale: no taxonomy-exploration UI exists anywhere, and the real, scoring-authoritative eligibility engine, lib/kora-engine/eligibility-gate.ts (classifyEligibilityBatch), is used directly — with its own explicit "no duplication" comment — by all 3 real admin data-intake routes (app/api/admin/data-intake/upload-preview/route.ts, preview/route.ts, accept/route.ts). tests/unit/eligibility-gate.test.ts's pre-existing "B71 regression guard" describe block independently corroborates that the live scoring pipeline routes through the canonical file, never through this one. Legacy rule nuances present only in the retired file (CCNL/contractual_mandatory override via CCNL_IMPROVEMENT_SIGNALS, Academy/Operations ambiguity detection via hasAmbiguitySignal, keyword-matching against data/synthetic/action-taxonomy.json via findBestTaxonomyMatch) were inventoried and classified: none qualify as MUST_MIGRATE_BEFORE_RETIREMENT — the service's own header already disclaimed scoring authority ("IT DOES NOT CONTROL SCORING"), canonical was always the sole scoring-authoritative engine regardless of this file's existence, and zero live callers currently exercised any of this file's own rules. Its sole seed file, data/synthetic/action-taxonomy.json, deleted alongside it — confirmed exactly ONE real, value-level consumer (this service itself); the other apparent hits (a UI step-label string in app/admin/kora-activation-layer/page.tsx, a code comment in lib/partner-activities/catalog.ts, and a prior retirement test's own non-usage assertion) are non-functional, confirmed by direct re-check, not trusted from any prior audit. Its two re-exported data contracts, EligibilityClassificationInput and EligibilityClassificationResult, are defined in @/lib/types (not by this file) and are kept unchanged — no opportunistic cleanup; their prior real importers, IngestionPipelineService.ts and IngestionSimulatorService.ts, were both already independently retired by earlier PRs, leaving zero remaining type-only importers of this file specifically. This is a stale-claim correction and zero-caller cleanup, NOT a canonical methodology deletion — classifyEligibilityBatch and its full BLOCKED/LIMITED/ELIGIBLE keyword tables, INDIVIDUAL_SENSITIVE_SIGNALS privacy-priority check, and B15 UEF-reviewed passthrough logic are entirely unaffected. EligibilityGateService.ts was an I9 allowlist entry — tenth genuine I9 reduction via a real caller migration: 16->15 files (26->25 imports). See tests/unit/b-truth-eligibility-gate-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): 4 callers. F-03/F-04 dependency-blocker resolution (2026-09-02): its two exported data contracts, EligibilityClassificationInput and EligibilityClassificationResult, moved to the neutral @/lib/types module (needed there to make PipelineAnalyzedRow, itself relocated for the F-03 fix, self-contained). This service re-exports both names for backward compatibility with its existing importers (IngestionPipelineService.ts, IngestionSimulatorService.ts) — no behavior change, pure type relocation. B-TRUTH Ingestion Pipeline Retirement (2026-09-03): IngestionPipelineService.ts deleted (unrelated retirement, its own bounded PR) — this service's only real value-level caller (classifyAction()) is gone, real caller count drops to 0 (fully orphaned by caller count). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains CANONICAL; its own future disposition remains a separate, later decision. |
| `svc.equity-access` | Intelligence | `services/equity-access/EquityAccessIntelligenceService.ts` | COMPLETE | — | — | 6 callers. |
| `svc.evidence-reliability` | Intelligence | `services/evidence-reliability/EvidenceReliabilityIntelligenceService.ts` | COMPLETE | — | — | 8 callers. |
| `svc.executive-intelligence` | Admin | `services/executive-intelligence/ExecutiveIntelligenceService.ts` | CANONICAL | — | — | 4 real callers (functional export computeExecutiveIntelligence — company/kora-index page, decision-pack pdf-data, ExecutiveIntelligencePanel, test). |
| `svc.explainability` | Methodology | `services/explainability/ExplainabilityService.ts` | CANONICAL | — | B-TRUTH Explainability Synthetic Retirement (2026-09-02) | Prior note (pre-retirement) claimed "3 callers (ExplainabilityPanel.tsx, MethodologyGlossary.tsx — both real, live...)" — CORRECTED here: independently re-verified, ExplainabilityPanel.tsx is NOT actually rendered/imported anywhere (zero real callers of the component itself; the prior claim conflated a type-only import with real reachability). Only MethodologyGlossary.tsx is a confirmed real, live caller (getConceptExplanation() only). RETIREMENT (2026-09-02): removed the synthetic-backed branch (getExplanation, getTopWeakComponents, getTopStrongComponents, getNextBestActions, getLimitations, getWarnings — all confirmed zero real callers, including after ReportGeneratorService's own separate retirement removed their last real-ish consumer) and its sole synthetic import, data/synthetic/explainability-records.json (deleted — confirmed zero real consumers; a nominal, non-functional string-literal reference in the final-scoring-group-protected DemoDataService.ts's unwired SeedResourceType stub was the only other mention, left untouched). The live glossary (CONCEPT_GLOSSARY, getConceptExplanation, listConceptKeys, ConceptExplanation) is unchanged, no synthetic dependency, MethodologyGlossary.tsx unaffected. ExplainabilityComponentRef/ExplainabilityAction/ExplainabilityRecord kept as pure type declarations (smallest safe change) since ExplainabilityPanel.tsx still has a type-only import of ExplainabilityRecord, even though that component is itself unreachable. Status stays CANONICAL — the live glossary is real, canonical, and unaffected by this retirement; this was a partial-file retirement, not a service retirement. See tests/unit/b-truth-explainability-synthetic-retirement.test.ts for the regression guard. Ninth genuine I9 reduction via a real caller migration (eighth counting the file itself, per the allowlist header): 18->17 files (28->27 imports). |
| `svc.financial-governance` | Financial | `services/financial-governance/FinancialGovernanceService.ts` | DEAD | — | B-TRUTH FinancialGovernance Retirement (2026-09-02) | DELETED (file no longer exists at primaryPath, kept here only as historical record). 1 caller (ReportGeneratorService). B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted — real caller count dropped to 0 (fully orphaned). RETIREMENT (2026-09-02, separate bounded PR): independently re-verified before deletion — zero real runtime callers, zero type-only callers (its locally-defined types — PillarBudgetLine, BTIIndicators, FinancialGovernanceRecord, FinancialGovernanceResult, BudgetSummary — had zero external importers; a same-named but unrelated PillarBudgetLine already exists independently in lib/types/index.ts, confirmed not the same type and not imported from this file). No unique methodology: the service was a pure synthetic-JSON-to-interface mapper (own fields self-labeled informational_only/no_payment_execution/no_fund_custody), never computing anything. Its BTI-shaped output (economic_relief_spend, deep_activation_spend, activation_debt_eur, bti_score, budget totals) is a near 1:1 conceptual match for the real, already-canonical BTI Engine output (analytics.bti_result, read directly by lib/decision-pack/pdf-data.ts) — no capability lost, no methodology migrated because none was uniquely owned. Its sole seed file, data/synthetic/financial-governance.json, deleted alongside it — confirmed zero other consumers before deletion. See tests/unit/b-truth-financial-governance-retirement.test.ts for the regression guard proving both the deletion and the scope boundary (ReportFactoryService, ExplainabilityService, PreviewScoringAdapter, DynamicScoringPreviewService, the Ingestion/UEF legacy chain, and the final scoring group all explicitly untouched). Seventh genuine I9 reduction via a real caller migration: 19->18 files (29->28 imports). |
| `svc.founder-validation` | Admin | `services/founder-validation/FounderValidationService.ts` | CANONICAL | — | — | 2 callers. CLAUDE.md-mandated internal/admin-only. |
| `svc.ingestion-normalizer` | Ingestion | `services/ingestion-normalizer/IngestionNormalizerService.ts` | DEAD | — | B-TRUTH Ingestion Normalizer Retirement (2026-09-03) | DELETED (file no longer exists at primaryPath, kept here only as historical record). PRIOR HISTORY: "2 callers. Confirmed real chain: consumed by ingestion-pipeline." B-TRUTH Ingestion Pipeline Retirement (2026-09-03): IngestionPipelineService.ts deleted (unrelated retirement, its own bounded PR) — this service's sole real caller was gone, real caller count dropped to 0. RETIREMENT (2026-09-03): independently re-verified before deletion — all 6 IIngestionNormalizerService methods (normalizeRow, normalizeBatch, detectMissingFields, inferSourceType, inferMandatoryStatus, getMissingDataQuestions) individually confirmed zero real callers; zero type-only callers. Its own header always correctly scoped it as internal-only ("should not be imported directly in app/ routes or components") — this retirement is a straightforward orphan-of-an-already-retired-consumer, not a stale-claim correction. Its type family (RawIngestionRow, NormalizedIngestionRow) was confirmed to have zero other real consumers — the one apparent hit, a comment in services/iu-computation/IUComputationService.ts, is prose, not an import; the types themselves were deliberately NOT deleted in this PR (no opportunistic cleanup — they remain pure, harmless data contracts in @/lib/types). No unique live data semantics: the real, canonical live ingestion/data-intake path (MappingConfidenceService, lib/data-intake/missing-field-analysis.ts, classifyEligibilityBatch, operating on the canonical RawUploadedRecord type family) is confirmed entirely independent — it solves related problems (source classification, missing-field detection) via its own separate implementation, never reusing this file's logic. Never an I9 allowlist member (no direct data/synthetic/** import) — I9 unaffected by this retirement (16/26 unchanged). EligibilityGateService is explicitly untouched by this PR — remains its own separately-authorized future slice. See tests/unit/b-truth-ingestion-normalizer-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. |
| `svc.ingestion-pipeline` | Ingestion | `services/ingestion-pipeline/IngestionPipelineService.ts` | DEAD | — | B-TRUTH Ingestion Pipeline Retirement (2026-09-03) | DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-03): independently re-verified before deletion — all 5 IIngestionPipelineService methods (analyzeRow, analyzeBatch, getIngestionSummary, getReviewQueue, getKoraReadyRecords) individually confirmed zero real callers; its last real-ish caller, UEFReviewService, was itself retired in the immediately preceding PR. No unique methodology: the private governance-flag derivation logic (deriveDestination/deriveReviewStatus/buildKoraReadyRecord) is a non-authoritative demo approximation of the same eligibility concept lib/kora-engine/eligibility-gate.ts's classifyEligibilityBatch already implements for real, live scoring — confirmed independent (zero references), RLS-16-proven correct for both tenant kinds without this file. Its sole seed file, data/synthetic/ingestion-samples.json, deleted alongside it — confirmed zero other real consumers (the two "demo data preserved" prose mentions on this entry and in the allowlist governed an earlier point when this exact retirement had not yet been authorized). Direct consequences (documented, not acted on): EligibilityGateService drops from 1 to 0 real callers (this file was its sole caller); IngestionNormalizerService drops from 1 to 0 real callers likewise. Neither modified nor retired by this PR — each remains its own separately-authorized future slice. demo-guard-01's substantive fallback-prohibition assertions (page never imports IngestionPipelineService, no ingestion-samples reference) are unchanged and unweakened; its ingestion-samples.json existence check was updated to reflect the new truth. See tests/unit/b-truth-ingestion-pipeline-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. IngestionPipelineService.ts was an I9 allowlist entry — tenth genuine I9 reduction via a real caller migration: 17->16 files (27->26 imports). PRIOR HISTORY (accurate as of its own time, preserved verbatim): B-TRUTH Retire Orphan Ingestion/UEF Chain (2026-08-31) attempted retirement of this service and STOPPED on finding it deliberately protected. Corrected caller count: 2 real callers — services/uef-review/UEFReviewService.ts (every method routes through getReviewRecords() -> ingestionPipelineService.analyzeBatch(), a hard dependency, not optional) and services/report-generator/ReportGeneratorService.ts (0 production callers itself). The prior "6 callers (..., IUComputationService, ...)" note was stale/wrong: services/iu-computation/IUComputationService.ts only imported the TYPE PipelineAnalyzedRow from this file, had zero value/method dependency on it, and is itself REACHABLE_CANONICAL via lib/kora-engine/run-kora-pipeline.ts — a completely separate, live concern from this demo chain. F-03 dependency-blocker resolution (2026-09-02): that type-only import has now been removed entirely — PipelineAnalyzedRow (and its own EligibilityClassificationResult/Input dependency) moved to the neutral @/lib/types module; IngestionPipelineService.ts re-exports PipelineAnalyzedRow from there for its one remaining real importer (UEFReviewService.ts). IUComputationService.ts has ZERO import of any kind from this file as of this resolution — confirmed by direct grep, not merely type-erasure reasoning. B-TRUTH Ingestion/UEF Classification (2026-08-31): reclassified CANONICAL -> FROZEN (matching the svc.admin-preview precedent for "intentionally demo-only, not evolving"). Zero canonical app/ path imports this service directly — reachability alone would suggest ORPHAN — but tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts is a pre-existing, deliberately authored, currently-passing regression guard proving this is NOT ordinary dead code: it documents a real historical bug (DEMO-DEP-RO) where app/company/kora-index/page.tsx silently fell back to this chain's synthetic data when its live fetch was loading/failed, blending demo numbers into a real, session-authenticated company page. That bug is fixed (the live page has zero reference to either service, confirmed) and the test explicitly asserts UEFReviewService.getReviewSummary must NOT be removed, DynamicScoringPreviewService and ReportGeneratorService may keep using it as "isolated demo-only" consumers, and data/synthetic/ingestion-samples.json must NOT be deleted ("demo data preserved"). Invariant this chain's isolation enforces: LIVE MUST NEVER FALL BACK TO DEMO UEF DATA. Not retired; not deletable without first revisiting demo-guard-01's own explicit test assertions, which is a separate decision from this classification pass. Governance Patch 01 (2026-08-31): status: FROZEN here is a pragmatic reuse of the nearest existing ArchitectureStatus value, not a claim that Master Plan §33's official FROZEN definition ("lo sviluppo è fermo, non che la capability sia indisponibile" — development stopped, not unavailable) applies; this component is actively used as a demo data source, not stopped. No dedicated DEMO_RUNTIME status exists in the enum; that gap is recorded for a future, explicitly-authorized schema change, not created here. Also per Patch 01 §3: DEMO_RUNTIME classification here is transitional within the B-TRUTH window, not a permanent exception to the I9=0 / CC-022 closure target. B-TRUTH Ingestion/UEF PR1 (2026-09-02, RLS-16, tests/integration/rls-16-ingestion-tenant-kind-parity.test.ts): PROOF, not retirement. Confirmed for real against local Postgres that a canonical synthetic/example tenant (tenant_kind DEMO, ordinary analytics.tenant row, no OP-001 involvement) can traverse the exact same canonical Ingestion/UEF runtime a LIVE-kind tenant uses — analytics.source_batch, personal.uploaded_record, analytics.uef_record, classifyEligibilityBatch (lib/kora-engine/eligibility-gate.ts, canonical), buildScoringRecordsFromApprovedUef (lib/live/uef-to-scoring-records.ts), runKoraPipeline (unchanged) — and reaches identical material pipeline output (scoringMode, eligibilitySummary, pillarDistribution, koraIndex.value/macroblocks) to a LIVE-kind tenant fed the same deterministic fixture, with matching negative tenant isolation. This is the evidence base for a future PR2 retirement decision on this cluster — it does NOT itself retire, canonicalize, or reclassify svc.ingestion-pipeline/svc.uef-review/svc.ingestion-simulator/svc.eligibility-gate/svc.report-generator, none of which this PR touches. Status remains FROZEN. B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted — real caller count drops from 2 to 1 (UEFReviewService.ts only, still the hard dependency). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains FROZEN, demo-guard-01's protection unchanged. B-TRUTH UEFReview Retirement (2026-09-03): CORRECTION — the "the test explicitly asserts UEFReviewService.getReviewSummary must NOT be removed... not deletable without first revisiting demo-guard-01's own explicit test assertions" framing above was found to be STALE at retirement time: it described demo-guard-01's wording before PR #131 (2026-09-02) rewrote it; the current guard's substantive assertions read app/company/kora-index/page.tsx's source, not UEFReviewService.ts's. services/uef-review/UEFReviewService.ts deleted separately (its own bounded PR) — this file's real caller count drops from 1 to 0 (fully orphaned by caller count). NOT retired by this PR — explicitly out of scope; this note is a factual consequence-of-deletion record only. Status remains FROZEN, demo-guard-01's protection unchanged and unweakened (verified at retirement time). See svc.uef-review for the full corrected record. |
| `svc.ingestion-simulator` | Ingestion | `services/ingestion-simulator/IngestionSimulatorService.ts` | DEAD | — | B-TRUTH Ingestion/UEF PR2 — IngestionSimulatorService retirement (2026-09-02) | DELETED (file no longer exists at primaryPath, kept here only as historical record). Prior "5 callers" note was stale/unverified at time of retirement — re-confirmed by direct repo-wide grep immediately before deletion: ZERO real value/method-level callers anywhere (`ingestionSimulatorService.` matched nothing outside its own file), and exactly ONE real type-only dependency (services/mapping-confidence/MappingConfidenceService.ts imported SourceType from it, feeding the real live app/api/admin/data-intake/upload-preview/route.ts). F-04 dependency-blocker resolution (2026-09-02, prior PR) had already moved EligibilityGateSummary to @/lib/types for the same reason (3 real, live product UI components — EligibilityGatePanel/EligibilitySummaryReport/ActionPlanReport — importing it for type only, already fed real data from liveCtx.eligibility, never from this service). This PR additionally moved SourceType to @/lib/types (kept deliberately distinct from the pre-existing IngestionSourceType there — different value sets, 'lms' vs 'lms_training' — merging them would have silently changed MappingConfidenceService's real sourceBonus() switch-case behavior) and fixed a duplicate-EligibilityGateSummary-interface bug the F-04 move had accidentally introduced (a second copy alongside an already-existing 2026-05-23 original in lib/types/index.ts, silently tolerated by TypeScript interface merging). data/synthetic/source-batches.json and data/synthetic/ingestion-samples.json are NOT deleted — both remain legitimately imported by other, non-retired files (AdminPreviewService.ts and IngestionPipelineService.ts respectively; the latter is also explicitly protected by tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts). Proof basis: RLS-16 (tests/integration/rls-16-ingestion-tenant-kind-parity.test.ts, Ingestion/UEF PR1) — confirmed a canonical synthetic tenant already traverses the real Ingestion/UEF runtime and runKoraPipeline authority without this service. This is a bounded, single-surface retirement — svc.ingestion-pipeline, svc.eligibility-gate, svc.uef-review, svc.report-generator, svc.dynamic-scoring, svc.company-data-intake, svc.scoring-simulator, and the final scoring group are explicitly NOT touched by this decision and remain their own, separately-verified future slices. |
| `svc.iu-computation` | Methodology | `services/iu-computation/IUComputationService.ts` | CANONICAL | — | — | 16 callers. This is the CC-002 I7 golden-case protected component. Overlap with worker-iu-computation not resolved. B-BC (CC-009): BC per action family moved to lib/methodology-config/v0.1.ts (getBCByActionFamily()) — no longer a local literal; I7 golden IU outputs unchanged (verified, 31/31 pass). |
| `svc.kora-contribution` | Contribution | `services/kora-contribution/KoraContributionService.ts` | CANONICAL | Y | — | B-TRUTH Contribution Protected Port (2026-09-01): synthetic-seed input retired (getContribution/getContributionSummary/getContributionScore/getCollectiveInitiatives/getContributionInitiatives/getSummaryV2 removed, 0 real callers each confirmed by grep before removal); the sole real caller of the retired getSummaryV2 (app/company/contribution/page.tsx) is rewired onto the new async getContributionV2Live(), DB-backed via commons.contribution_event + commons.post (lib/kora-contribution/contribution-pipeline-input.ts). The protected methodology authority (computeContributionV2 / computeProvisionalScore / computeFromPipelineResult) is byte-for-byte unchanged — only its input source moved. 3 real (non-test) importers verified by grep: app/company/contribution/page.tsx, app/api/company/contribution/live/route.ts, lib/kora-contribution/contribution-pipeline-input.ts. Master Plan §33 Do-Not-Delete. The prior "competingWith lib.kora-contribution" claim was corrected by the B-TRUTH Contribution audit (2026-09-01): lib/kora-contribution/contribution-methodology.ts is pure doctrine/policy constants (zero computation, zero competing implementation) — see lib.kora-contribution below. |
| `svc.life-diversity` | Intelligence | `services/life-diversity/LifeDiversityService.ts` | COMPLETE | — | — | 8 callers. |
| `svc.lifecycle` | Admin | `services/lifecycle/LifecycleService.ts` | DEAD | — | B-TRUTH Root Control Room Wave 2 (2026-08-30) | DELETED (file no longer exists at primaryPath, kept here only as historical record). Real audit.audit_log was always a separate, live mechanism — this shadow reached zero runtime callers when its sole consumer (Root Control Room Section J) was retired, and Lifecycle/Audit moved to the Gen 3 workspace tab reading the real table, tenant-scoped, behaviorally verified. Its seed file (data/synthetic/lifecycle-audit.json) was deleted alongside it — it had no other consumer. |
| `svc.mapping-confidence` | Ingestion | `services/mapping-confidence/MappingConfidenceService.ts` | CANONICAL | — | — | 2 callers. |
| `svc.my-kora-preview` | Worker | `services/my-kora-preview/MyKoraPreviewService.ts` | COMPLETE | — | CC-024 / D-D (at the app-surface level, not this service) | 14 callers. |
| `svc.pib-aggregation` | PIB | `services/pib-aggregation/PIBAggregationService.ts` | CANONICAL | Y | — | 3 callers. Master Plan §33: prerequisite for canonical CF. |
| `svc.privacy-visibility` | Privacy | `services/privacy-visibility/PrivacyVisibilityService.ts` | CONSOLIDATE | — | after B-INV | 1 caller. Master Plan §32: "dopo B-INV, I2 copre lo strato canonico" — scheduled for likely removal once I2 canonically covers this, per the Master Plan itself. |
| `svc.report-factory` | Reporting | `services/report-factory/ReportFactoryService.ts` | DEAD | — | B-TRUTH ReportFactoryService Canonical Decision Pack Status Migration (2026-09-06) | DELETED (file no longer exists at primaryPath, kept here only as historical record). PRIOR HISTORY (accurate as of its own time, preserved verbatim): D-B resolved (CC-005/CC-013): lib.decision-pack is canonical; ReportFactoryService never becomes a second authority. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30) removed its only other caller (root page.tsx) — 1 real caller remains, app/admin/pipeline/page.tsx (a demo caller — hardcoded DEMO_COMPANY_ID). B-TRUTH Demo/Orphan Chain Audit (2026-08-30): reachability trim — verified per-method, repo-wide, that pipeline calls exactly one method (getDecisionPackFactoryStatus); removed 10 other public methods (version history, generation, readiness, sections, export actions, change summary, period comparison, metric deltas, limitations, previous-comparable-version) plus 1 dead private method (isTenantActive) that had zero callers anywhere — not from pipeline, not internally, not from any test (guard-style "not.toContain" assertions only, unaffected). Kept: getDecisionPackFactoryStatus (public, reachable) and getLatestDecisionPackVersion (public, called only internally). Still 100% synthetic (data/synthetic/decision-pack-versions.json, zero DB queries) — that divergence is unchanged, just narrowed to the single reachable method; not a One-Truth violation because the demo tenant (DEMO_COMPANY_ID) has no live analytics.tenant row to read canonical data from in the first place. F-05 dependency-blocker audit (2026-09-02): confirmed by direct grep that ReportFactoryService.ts has NO import of any kind (value or type) from services/report-generator/ReportGeneratorService.ts. CC-005/D-B CLOSEOUT (2026-09-02): this file's 2 remaining public methods (getDecisionPackFactoryStatus, getLatestDecisionPackVersion) have no canonical replacement needed (demo-only output, no unique methodology). B-TRUTH ReportGenerator Retirement (2026-09-02): ReportGeneratorService.ts deleted separately — this file was explicitly untouched at that time, still pending its own future retirement decision. B-TRUTH TenantService Canonical Migration (2026-09-04): getDecisionPackFactoryStatus/computeBlockingReasons gained a CanonicalTenantStatus ({id, isActive} \| null) parameter, replacing an internal tenantService.getTenant() call — still-synthetic hasKoraIndex/getLatestDecisionPackVersion checks unchanged. B-TRUTH CompanyDataIntakeService Canonical Migration (2026-09-05): gained a third parameter, dataIntake: CanonicalDataIntakeStatus, replacing an internal companyDataIntakeService dependency — Decision Pack version source still unmigrated, explicitly deferred to PR 4. RETIREMENT (2026-09-06, B-TRUTH ReportFactoryService Canonical Decision Pack Status Migration, PR 4 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan): independently re-verified before deletion — the sole real runtime caller, app/admin/pipeline/_components/PilotLifecycleClient.tsx, was traced field-by-field and found to read exactly ONE field, latest_status ("=== 'ready'"), out of the legacy 9-field DecisionPackFactoryStatus return shape. Per this migration's "map only what the canonical model actually supports, do not fake 1:1 parity with legacy synthetic fields" rule, the blocking_reasons/warnings/next_action/can_generate apparatus (built from a private hasKoraIndex() call into the still-synthetic ScoringSimulatorService demo path) was DROPPED, not migrated — it never had a real consumer. Final scoring is untouched by this migration, not because a canonical replacement was avoided, but because nothing downstream ever needed hasKoraIndex()'s result. Replacement: a new shared pure view builder, lib/live/decision-pack-status-view.ts, reading directly from analytics.decision_pack_version (status, created_at), fetched once by app/admin/pipeline/page.tsx (Server Component) and passed down as a decisionPack prop — same fetch-once-thread-down discipline as PR 2/PR 3. Version-selection rule: latest by created_at, matching lib/live/data-intake-status-view.ts's own precedent and the operator-flow route's GET handler for the same table — not invented for this migration. No tenant_kind branch introduced; view is identical for KoraTest Srl (tenant_kind=TEST) and any tenant_kind=LIVE tenant. CanonicalTenantStatus needed no relocation — repo-wide grep confirmed zero remaining real or type-only importers once the getDecisionPackFactoryStatus call site was removed, so the type was deleted along with the file, not moved. Its sole seed file, data/synthetic/decision-pack-versions.json, deleted alongside it — confirmed zero other consumers before deletion. AccountProvisioningService, AdminPreviewService, and B-WORKER are explicitly untouched — one PR = one bounded migration. See tests/unit/b-truth-reportfactory-canonical-decision-pack-status.test.ts for the regression guard proving both the deletion and the scope boundary. |
| `svc.report-generator` | Reporting | `services/report-generator/ReportGeneratorService.ts` | DEAD | — | B-TRUTH ReportGenerator Retirement (2026-09-02) | DELETED (file no longer exists at primaryPath, kept here only as historical record). D-B resolved (CC-005/CC-013, ratified in PR #131): lib.decision-pack is canonical, ReportGeneratorService was NOT. CORRECTED CALLER COUNT (established pre-retirement): the previous "3 real callers (IScoringService.ts, PreviewScoringAdapter.ts, +1 test)" note was stale — CC-005 verified by direct import-statement search that this was actually ZERO real callers; those two files only mentioned it in comments (never imported it), and the one test did static source-string analysis, never instantiated it. Master Plan §32/§33 stated explicitly, twice: "restano INVESTIGATE" — that classification governed the standing default absent a specific founder decision; this task (B-TRUTH ReportGenerator Retirement, 2026-09-02) is exactly that founder-authorized decision (Master Plan §30's "One Truth per gruppo di seed... Sì per gruppo" per-group approval mechanism), independently re-verified before deletion: zero real runtime callers (static, dynamic, barrel, and type-only imports all checked, repo-wide), all 7 capabilities' disposition re-confirmed unchanged from PR #131. RETIREMENT (2026-09-02): deleted generate(), getCurrentCompanyDecisionPack(), getDecisionPackVersionHistory(), getDecisionPackExportActions(), and the generateCompanyDecisionPack() assembly along with the rest of the file — all 4 RETIRE + 1 RETIRE-as-assembly items from the PR #131 disposition. getDecisionPackLimitations() required no porting (already present in lib.decision-pack, proven by tests/unit/cc005-decision-pack-limitations-migration.test.ts). getDecisionPackReadiness()'s DEFERRED PRODUCT REQUIREMENT — see the CC-005 CLOSEOUT / D-B RESOLUTION header block above — survives this deletion verbatim in that governance text; no runtime implementation was added. Sole exported type (ReportType) had zero external importers, confirmed by repo-wide grep before deletion — nothing needed relocation. ReportGeneratorService.ts was never itself an I9 allowlist entry (it consumed synthetic data only transitively, via ScoringSimulatorService/IngestionPipelineService/etc., each separately allowlisted) — I9 is unaffected by this retirement (19/29 unchanged). ReportFactoryService, the Ingestion/UEF legacy chain (IngestionPipelineService, EligibilityGateService, UEFReviewService, DynamicScoringPreviewService, PreviewScoringAdapter, ExplainabilityService, FinancialGovernanceService), and the final scoring group are explicitly untouched by this PR — see tests/unit/b-truth-report-generator-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. Direct consequence of this deletion (documented, not acted on): IngestionPipelineService drops from 2 to 1 real caller (UEFReviewService only); UEFReviewService drops from 2 to 1 real caller (DynamicScoringPreviewService only); ExplainabilityService drops from 3 to 2 real callers (both live-safe, static-glossary-only — its synthetic-backed getExplanation() now has zero callers anywhere); FinancialGovernanceService drops from 1 to 0 real callers (now fully orphaned). None of these 4 services were modified or retired by this PR — each remains its own separately-authorized future slice. |
| `svc.role-permission` | Privacy | `services/role-permission/RolePermissionService.ts` | CANONICAL | — | — | 2 callers — low relative to "mandatory gatekeeper" status, worth CC-002/I1 follow-up scrutiny. |
| `svc.scenario` | Demo | `services/scenario/ScenarioService.ts` | FROZEN | — | — | 4 callers, demo-only. |
| `svc.scoring-simulator` | Scoring | `services/scoring-simulator/ScoringSimulatorService.ts` | CONSOLIDATE | — | B-TRUTH | 21 callers (highest usage in this registry) yet Master Plan §32 explicitly schedules removal at end of B-TRUTH — high current usage does not make it the architectural target. |
| `svc.scoring.facade` | Scoring | `services/scoring/IScoringService.ts` | CANONICAL | — | — | Consumed only via lib/scoring-result/index.ts (useScoringResult). CC-002 found 6 real bypasses of this facade — see I5/M-02 notes, not fixed in CC-002/CC-003. |
| `svc.submission-feedback` | Demo | `services/submission-feedback/SubmissionFeedbackService.ts` | FROZEN | — | — | 3 callers. |
| `svc.tenant` | Core | `services/tenant/TenantService.ts` | DEAD | — | B-TRUTH TenantService Canonical Migration (2026-09-04) | DELETED (file no longer exists at primaryPath, kept here only as historical record). RETIREMENT (2026-09-04), PR 2 of the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION plan (PR 1 = B-TRUTH KoraTest Canonical Foundation, 2026-09-03/04): independently re-verified before deletion — all 3 real runtime callers (app/admin/pipeline/page.tsx, components/admin/WorkforceQuickAccessPanel.tsx, services/report-factory/ReportFactoryService.ts) individually confirmed, zero type-only callers. Each migrated onto a canonical analytics.tenant read: app/admin/pipeline/page.tsx became a thin async Server Component (PILOT_LIFECYCLE_TENANT_CODE = 'KORATEST-01', an ordinary tenant_code lookup, no special-case branching), rendering the new app/admin/pipeline/_components/PilotLifecycleClient.tsx (every other step on that page — worker provisioning, account provisioning, scoring, data intake — is UNCHANGED, still keyed by the pre-existing DEMO_COMPANY_ID = 'meridiana-group' constant, a separate, later migration slice); WorkforceQuickAccessPanel.tsx now receives its tenant list as a prop from its already-async parent (app/admin/companies/page.tsx), which queries analytics.tenant with no tenant_kind filter (no hidden test tenants); ReportFactoryService.getDecisionPackFactoryStatus/computeBlockingReasons now accept an already-fetched CanonicalTenantStatus ({ id, isActive } \| null) parameter instead of calling tenantService.getTenant() themselves, reusing the same canonical read app/admin/pipeline already performs rather than duplicating it, and avoiding making a previously-synchronous method async merely to satisfy an internal dependency; ReportFactoryService's own still-synthetic hasKoraIndex/getIntakeStatus/getLatestDecisionPackVersion checks are UNCHANGED, unmigrated, explicitly out of scope (a separate, later slice). No tenant_kind product branch was introduced anywhere in this migration. Its sole seed file, data/synthetic/tenants.json, deleted alongside it — confirmed zero remaining real consumers (3 remaining text hits were governance-comment prose, not imports). Its type family (KoraTenant, KoraTenantStatus, TenantReadiness, ReadinessItemStatus, defined in @/lib/types, not by this file) was deliberately NOT deleted — no opportunistic cleanup, services/worker-space/WorkerSpaceCapabilityService.ts still legitimately imports it, unrelated to this migration, explicitly out of scope (B-WORKER territory). AdminPreviewService, CompanyDataIntakeService, and AccountProvisioningService are explicitly untouched — one PR = one bounded migration; their own future disposition remains separate, later decisions. TenantService.ts was an I9 allowlist entry — eleventh genuine I9 reduction via a real caller migration: 15->14 files (25->24 imports). See tests/unit/b-truth-tenantservice-canonical-migration.test.ts for the regression guard proving both the deletion and the scope boundary. PRIOR HISTORY (accurate as of its own time, preserved verbatim): Competing surfaces are not individually registered (covered under app-surface.admin, "not individually traced per-subdirectory"): app/admin/companies/_components/CompanyConsolePanel.tsx (via /api/admin/company-console), app/admin/company-workspace-live, app/admin/company-users-live. B-TRUTH Tenant Identity read-path audit (2026-08-30): status corrected from the prior CANONICAL — TenantService is 100% synthetic (data/synthetic/tenants.json, zero DB queries); the prior "CANONICAL" classification and "24 callers" count were both stale/wrong. Fresh caller count via direct import search: 15 real callers as of the original audit. B-TRUTH Gen 3 route identity activation (2026-08-30) removed 4 of them — app/admin/companies/[companyId]/{workspace,preview,evidence,submissions}/page.tsx — which were already fully DB-backed (Gen 3, B168.5/B171 consolidation) except for this one stale TenantService existence-gate call; each now queries analytics.tenant directly by tenant_code (no synthetic fallback, is_active=true, behaviorally tested against unknown/synthetic-only ids and DB-error paths). 11 real callers remain, ALL nested under app/admin/companies/[companyId]/* (6 files: layout.tsx, page.tsx, onboarding, data-intake, users, workforce — the "Gen 0/1" pages, 2 entangled services: ReportFactoryService, CompanyIntelligenceService) or driven by a hardcoded DEMO_COMPANY_ID = 'meridiana-group' constant (app/admin/pipeline/page.tsx), or the demo-labeled WorkforceQuickAccessPanel/workforce-baseline admin page — none of these 11 are safely migratable in isolation without either breaking navigation or creating mixed real/synthetic identity on the same page (Phase 8/9 guard). A real, working, DB-backed competing read path already exists and is production-verified: analytics.tenant queried directly by /api/admin/tenants, /api/admin/company-console (feeding CompanyConsolePanel, explicitly "Operational live tenant registry", filtered to tenant_kind=LIVE), /api/admin/tenants/[id]/promote-to-pilot, and lib/decision-pack/pdf-data.ts — none of these import or depend on TenantService. Route-identity finding, CORRECTED by the Admin Route Convergence audit: app/admin/companies/[companyId]/* is not uniformly non-canonical. Four sub-routes (workspace/preview/evidence/submissions — see above) treat [companyId] as tenant_code and are canonical; the remaining Gen 0/1 pages still treat it as an arbitrary tenants.json key ("meridiana-group") with no resolvable mapping to tenant.id/tenant_code. A separate, second live surface also exists at flat query-param routes (app/admin/company-workspace-live, app/admin/company-users-live) for a different use case (post-provisioning entry with a tenantId uuid) — this is pre-existing architectural duplication, not something introduced here, and not yet reconciled with the Gen 3 [companyId] pattern. Also found (B-TRUTH Admin Route Convergence audit): /api/admin/company-console's quickActions had 4 links (viewWorkspace, evidenceArchive, livePreview, submissions) pointing at five flat routes removed by B171 — fixed as part of Gen 3 activation, now pointing at /admin/companies/{tenant_code}/{workspace\|evidence\|preview\|submissions}. quickActions.manageUsers remains unfixed/still broken (/admin/company-users?tenantId=...) — its canonical destination is still architecturally unresolved (app/admin/companies/[companyId]/users is still 100% synthetic; app/admin/company-users-live is read-only and its own header comment claiming B171 consolidation into [companyId]/users is itself stale/incorrect) — left untouched per explicit scope guard. Mutation methods (activate/suspend/archive/restore/deleteDemoTenant) remain pure demo stubs with zero backing implementation in analytics.tenant (no lifecycle state machine column exists at all — only is_active boolean + deleted_at soft-delete + tenant_kind classification) — explicitly out of scope (Phase 2 of this consolidation, not authorized here). B-TRUTH Gen 0/1 Retirement Wave 1 (2026-08-30): retired 3 of the remaining 11 callers outright — app/admin/companies/[companyId]/{onboarding,data-intake,workforce}/page.tsx were 100% read-only synthetic dashboards (workforce additionally had a real-looking createDemoWorker mutation, explicitly a demo stub, no DB writes) with no unique capability beyond real live surfaces (real upload: /admin/data-intake; real worker provisioning: /admin/workers, B104; real per-tenant pipeline status: the Workspace tab) — deleted, not redirected to a fake equivalent, matching the B171 precedent. CompanyTabNav reduced from 8 to 5 tabs. All dangling links updated to real destinations (root Control Room, users/page.tsx, admin pipeline page, WorkforceQuickAccessPanel, lib/workforce/workforce-rules.ts getWorkforceRoute — now always /admin/workers). 8 real callers remain: layout.tsx, page.tsx (root Control Room), users/page.tsx, ReportFactoryService, CompanyIntelligenceService, WorkforceQuickAccessPanel, workforce-baseline admin page, app/admin/pipeline/page.tsx (DEMO_COMPANY_ID) — still none safely migratable (root Control Room retains required capabilities — BTI display, user-lifecycle mutations, Decision Pack period comparison — with no canonical replacement; see Phase 1 findings). No service caller graph (LifecycleService, ReportFactoryService, CompanyIntelligenceService, CompanyOnboardingService, WorkforceBaselineService, AccountProvisioningService, BudgetToHumanImpactService, WorkerProvisioningService, CompanyDataIntakeService) reached zero callers — root page.tsx alone keeps each one alive — so I9 is unchanged (26 files/38 imports); this wave removed parallel-truth callers, not synthetic import statements. components/admin/RosterImportModal.tsx is now orphaned (its sole caller, workforce/page.tsx, was deleted) — left in place, not deleted (zero callers ≠ dead, per this same registry's own discipline). B-TRUTH Root Control Room Wave 2 (2026-08-30): decomposed root page.tsx capability-by-capability. REMOVED (duplicate of Gen 3 workspace, which already reads real analytics.kora_index_result/decision_pack_version): KORA Index Output (Section E + Tile 6), Decision Pack Factory status (Section G). RETIRED, not migrated (no canonical source — analytics.decision_pack_version has no previous-version linkage): Decision Pack period comparison. CANONICALIZED (moved to the Gen 3 workspace tab as new minimal persisted reads, no recomputation): BTI (analytics.bti_result, fields with no real column honestly omitted rather than fabricated) and Lifecycle/Audit (audit.audit_log, tenant-scoped by the real tenantId already resolved on that tab) — both behaviorally tested for tenant isolation. REMOVED as fake-mutation UI with no real backend, not migrated (Section H Access & Users; the worker roster mutation table in Section I): user invite/revoke/reset/disable/delete and worker invite/disable/delete-demo controls are gone; Section H is now a link to the retained, already read-only /users tab; Section I keeps only its aggregate summary tiles. accountProvisioningService, scoringSimulatorService, budgetToHumanImpactService, reportFactoryService, lifecycleService are no longer imported by root page.tsx. Caller graph after: TenantService 8 (unchanged — tenant/header/Section B/C still needed), ReportFactoryService 1 (pipeline only), AccountProvisioningService 3 (pipeline, users, my-kora), BudgetToHumanImpactService 4, CompanyIntelligenceService 1 (unchanged, header risk badge), CompanyOnboardingService 1 (unchanged), CompanyDataIntakeService 4 (unchanged, Section D retained), WorkerProvisioningService 7 (unchanged, summary tiles retained), **LifecycleService 0 — fully orphaned**. LifecycleService.ts and its sole seed file (data/synthetic/lifecycle-audit.json) were deleted this wave (not just left orphaned): zero remaining runtime callers, live replacement verified working, satisfying Master Plan §32's own rule that a synthetic path may be removed once its live migration is complete and verified. I9 reduced 26→25 files (38→37 imports) — the first genuine I9 reduction since CC-018. Root page.tsx remaining gaps (still real, still undecomposed further this wave): Tenant Overview profile fields (Section B — no analytics.tenant columns), Operational Readiness tiles reading the same fake tenant/CompanyIntelligenceService risk aggregation, Data Intake summary (Section D), worker roster summary tiles (Section I). Root Control Room is smaller but still a real synthetic-identity-rooted page — not marked canonical. B-TRUTH Root Control Room Wave 3 (2026-08-30): removed Section B (Tenant Overview, RETIRE_LEGACY — 9/12 fields have no analytics.tenant column and no confirmed product requirement, not extending the schema to preserve demo CRM richness) and Section C Tiles 2/7/8 (Onboarding/Decision Pack/Advisor — REMOVE_DUPLICATE/RETIRE_LEGACY) and Section D (Data Intake summary — REMOVE_DUPLICATE against the Gen 3 workspace tab's latestBatch/uef fields). companyDataIntakeService is no longer imported by root page.tsx (2 real callers remain elsewhere: app/admin/pipeline, ReportFactoryService/CompanyIntelligenceService — not zero, not deleted). Worker summary (Section I, Tiles 4/5) and CompanyIntelligenceService (header risk badge) left untouched — RETAIN_REQUIRED_GAP and INVESTIGATE respectively; worker data is >80% B-WORKER-gapped with no real equivalent, and CompanyIntelligenceService aggregates 6 synthetic services under a heuristic with no defined current-product methodology, failing all 4 of Phase 7's canonicalization criteria. I9 unchanged (25 files/37 imports) — no service reached zero callers this wave. `ROOT_CONTROL_ROOM_FUTURE = KEEP_TEMPORARILY` (Wave 3's own conclusion) — SUPERSEDED same day by the Root Control Room Wave 3 HARDENING pass. B-TRUTH Root Control Room Wave 3 Hardening (2026-08-30): re-evaluated Wave 3's KEEP_TEMPORARILY conclusion against the governing principle "a missing canonical capability is a product gap, not justification for keeping synthetic product truth in runtime." All three remaining outputs failed it — Tile 1 (Tenant Status: TenantService.tenant_status, no canonical state machine exists), Section I (Worker Provisioning summary: 100% synthetic except total_workers, which has a real but unreachable-from-here analog; the rest is B-WORKER's future domain), and the header risk badge (CompanyIntelligenceService.getCompanyIntelligenceRecord: an undefined ad-hoc heuristic over 6 synthetic services) are each a real gap, not license to render a fake value in the meantime. Removing all three left only navigation chrome built on an already-synthetic company_name — nothing substantive survived. The real Company Console (CompanyConsolePanel, /api/admin/company-console) never linked to this bare root path in the first place; its quickActions go straight to the Gen 3 sub-tabs (workspace/evidence/preview/submissions) by real tenant_code (caller-graph search of every `admin/companies/${...}` template literal in app/ and components/ confirmed this). The only two entry points that ever pointed at bare root were themselves demo-only — app/admin/pipeline's DEMO_COMPANY_ID quick link (removed this pass) and users/page.tsx's self-referential breadcrumb/nav (unchanged, out of scope: Users tab). page.tsx was rewritten as a thin redirect (requireKoraAdmin -> redirect to the Gen 3 workspace tab for the same companyId), reusing the exact same real analytics.tenant.tenant_code existence check the workspace route already performs — no new compatibility mapping invented; a synthetic id like "meridiana-group" now honestly 404s there instead of rendering fabricated data here. `ROOT_CONTROL_ROOM_FUTURE = READY_TO_RETIRE`. Caller-count effect of this pass: tenantService 8->7, workerProvisioningService 7->6, companyIntelligenceService 1->0 (now fully orphaned — see svc.company-intelligence); companyDataIntakeService, budgetToHumanImpactService, scoringSimulatorService, companyOnboardingService unchanged (root already did not call them, per Wave 2/3). I9 unchanged (25 files/37 imports) — no service reached zero *synthetic-import* callers; CompanyIntelligenceService reaching zero *runtime* callers is a distinct fact (zero callers != DEAD, not deleted). Files modified this pass: app/admin/companies/[companyId]/page.tsx (rewritten to a redirect), app/admin/pipeline/page.tsx (removed the now-always-404 Company Control Room quick link), and 6 test files updated to match (b175-admin-company-hydration-date, cc013-canonical-contract, cc014-decision-pack-adversarial, root-control-room-wave2, root-control-room-wave3, tenant-identity-read-path-audit). COMMIT: NO / PUSH: NO per this pass's explicit instruction — awaiting a separate human commit decision. CC-019A — Legacy Company Users Surface Retirement (2026-08-31): retired app/admin/companies/[companyId]/users/page.tsx outright (100% synthetic, read-only demo account display; its only "create user" CTA linked to an already-broken flat route, /admin/company-users, not real functionality) — real, more capable replacement already existed and was left untouched: app/admin/company-users-live (Supabase-backed /api/admin/company-users, supports real GET/POST/PATCH, independent of TenantService/AccountProvisioningService, keyed by tenantId = analytics.tenant.id, a UUID). CompanyTabNav's Users tab removed rather than relinked, since the tab nav only carries tenant_code (companyId), not the UUID company-users-live needs — bridging the two was explicitly out of scope (no new resolver invented). app/admin/pipeline's "Utenti Aziendali" quick link removed for the same reason (would have 404'd), matching this same file's own prior "Company Control Room" quick-link removal precedent. Caller-count effect: tenantService 7->6 (the file itself is gone, not just its call site); accountProvisioningService keeps its other 2 callers (pipeline demo, my-kora demo) — not retired. layout.tsx, ReportFactoryService, CompanyIntelligenceService, and the DEMO_RUNTIME callers (admin/pipeline, workforce-baseline, WorkforceQuickAccessPanel) are all unchanged. I9 unchanged (24 files/36 imports) — TenantService.ts remains in the allowlist via its other 6 callers. CC-019B (layout.tsx's own tenantService.getTenant() call) remains open, not touched by this slice. CC-019B — Gen3 Shared Layout Tenant Identity Canonicalization (2026-08-31): layout.tsx no longer imports TenantService — its company_name resolution now queries analytics.tenant by tenant_code, identical to the Gen3 children. Caller-count effect: tenantService 6->5 (admin/pipeline, workforce-baseline, WorkforceQuickAccessPanel — DEMO_RUNTIME — ReportFactoryService, CompanyIntelligenceService). TenantService.ts implementation untouched. I9 unchanged (24/36). CC-019 remains open beyond A+B: ReportFactoryService and CompanyIntelligenceService are separately-classified pending-decision consumers, not part of this decomposition. B-TRUTH First Canonical Seed Group (2026-09-01, side effect — the primary migration was svc.workforce-baseline, not this service): app/admin/companies/workforce-baseline/page.tsx no longer imports tenantService — both its own data and its company selector now read live endpoints (GET /api/admin/workforce-baseline, GET /api/admin/tenants). Caller-count effect: tenantService 5->4 (admin/pipeline, WorkforceQuickAccessPanel — DEMO_RUNTIME — ReportFactoryService, CompanyIntelligenceService). TenantService.ts implementation itself untouched; its own migration remains separately gated, unaffected by this seed group. |
| `svc.uef-review` | Methodology | `services/uef-review/UEFReviewService.ts` | DEAD | — | B-TRUTH UEFReview Retirement (2026-09-03) | DELETED (file no longer exists at primaryPath, kept here only as historical record). CORRECTION (2026-09-03, established at retirement time): several earlier notes on this entry and on svc.ingestion-pipeline stated demo-guard-01 "explicitly asserts getReviewSummary must NOT be removed" and treated that as a deliberate, method-level, permanent protection — that was STALE. It described the guard's wording BEFORE B-TRUTH Decision Pack Ratification (PR #131, 2026-09-02) rewrote tests/unit/demo-guard-01-kora-index-evidence-fallback.test.ts. Rereading the guard's current full text at retirement time showed every substantive assertion reads app/company/kora-index/page.tsx's source (the page with the real historical bug, DEMO-DEP-RO) — never UEFReviewService.ts's. The only place this file's existence was checked was an incidental scope-boundary inventory line, mechanically updated for every retirement this session (ReportGeneratorService, FinancialGovernanceService, PreviewScoringAdapter, DynamicScoringPreviewService) — not an elevated, permanent requirement. RETIREMENT (2026-09-03): independently re-verified before deletion — all 11 IUEFReviewService methods individually confirmed zero real callers (not just the two previously known, getReviewSummary/getAllReviewedPipelineRows, which dropped to zero only after DynamicScoringPreviewService's own retirement removed their last caller); zero type-only callers (IUEFReviewService interface had zero external importers). No unique methodology: every method is a filter/remap over ingestionPipelineService.analyzeBatch()'s demo classification output, itself non-authoritative. The real, canonical UEF-review product UX (app/admin/uef-review/, app/company/uef-review/ — already a locked shell per B106/B147, /api/admin/uef/* reading analytics.uef_record via analytics.fn_admin_uef_review()) was confirmed to have zero reference to this class, independent of this retirement. Canonical scoring-record build (lib/live/uef-to-scoring-records.ts, buildScoringRecordsFromApprovedUef) also confirmed independent. demo-guard-01's substantive fallback-prohibition assertions (page never imports uefReviewService, never calls getReviewSummary, no liveUefSummary ?? uefReviewService pattern) are unchanged and unweakened — verified still present and passing. IngestionPipelineService and EligibilityGateService are explicitly untouched by this PR — see tests/unit/b-truth-uef-review-retirement.test.ts for the regression guard proving both the deletion and the scope boundary. Direct consequence of this deletion (documented, not acted on): IngestionPipelineService drops from 1 to 0 real callers (fully orphaned by caller count). Not retired or modified — a separate, later, independently-audited slice. UEFReviewService.ts was never an I9 allowlist entry — I9 unaffected (17/27 unchanged). |
| `svc.worker-achievements` | Worker | `services/worker-achievements/WorkerAchievementService.ts` | CANONICAL | — | — | 3 callers. |
| `svc.worker-attribution` | Worker | `services/worker-attribution/WorkerAttributionService.ts` | CANONICAL | — | — | 6 callers. |
| `svc.worker-iu-computation` | Worker/Methodology | `services/worker-iu-computation/WorkerIUComputationService.ts` | CANONICAL | — | — | 7 real callers (functional exports — computeBaseWorkerPIBRows, etc.). Overlap with iu-computation not resolved. |
| `svc.worker-opportunity` | Worker | `services/worker-opportunity/WorkerOpportunityService.ts` | FUTURE_CORE | Y | — | Master Plan §33: "base tecnica di Exposure". 4 callers. |
| `svc.worker-pib` | PIB | `services/worker-pib/WorkerPIBService.ts` | FUTURE_CORE | Y | — | Master Plan §33: prerequisite for canonical CF. 13 callers. Isolation independently verified (SECURITY DEFINER-only access, migration 027). |
| `svc.worker-pillar-adoption` | Worker | `services/worker-pillar-adoption/WorkerPillarAdoptionService.ts` | CANONICAL | — | — | CC-018/B-TRUTH SEED GROUP #1 (2026-08-30): canonical source moved from data/synthetic/company-aggregates.json to analytics.activation_result.pillar_distribution — the same column already canonical for the Decision Pack (B-PACK). Async, tenant-scoped (db, tenantId), no synthetic import remains. Sole caller (WorkerAdoptionPanel) is currently unmounted on any live route (deliberately, per B105/B133) — this migration is source-level, ready for whenever the panel is re-integrated; new consumer: app/api/company/pillar-adoption (session-authenticated). CC-002 fixed a local N≥10 threshold duplicate here (I2), unchanged. |
| `svc.worker-provisioning` | Worker | `services/worker-provisioning/WorkerProvisioningService.ts` | CANONICAL | — | — | 16 callers. Matches current branch's own work (worker identity lifecycle sprint). |
| `svc.worker-space` | Worker | `services/worker-space/WorkerSpaceCapabilityService.ts` | CANONICAL | — | — | 8 callers. |
| `svc.workforce-baseline` | Company | `lib/live/workforce-baseline-view.ts` | CANONICAL | — | — | B-TRUTH First Canonical Seed Group (2026-09-01): services/workforce-baseline/WorkforceBaselineService.ts (synthetic, data/synthetic/workforce-baseline.json) DELETED entirely — confirmed single real caller (app/admin/companies/workforce-baseline/page.tsx) migrated onto GET /api/admin/workforce-baseline (new handler; the existing POST/persistWorkforceBaseline write path was already canonical and required zero changes) via lib/live/workforce-baseline-view.ts. Field disposition: KEEP/DERIVE total_workers, segment_breakdown->aggregateGroups, minimum company threshold (30, matching CompanyOnboardingService's own rule), minimum_group_size (stored, N>=10) — DROP/DEFER upload-process stats (Ingestion-domain territory, out of this task's scope), editorial completeness score, warnings/limitations text, and readiness.activation_reach_ready/distribution_equity_ready (would require cross-referencing analytics.activation_result, not touched here) — no schema added, no placeholder value invented for any dropped field. The page's own company selector was also repointed from the synthetic tenantService.getTenants() to the already-live GET /api/admin/tenants, avoiding a TenantService.ts entanglement (that service's own migration remains separately gated, unaffected by this seed group). tests/integration/rls-13-workforce-baseline-tenant-kind-parity.test.ts (DB-backed, mandatory CI gate) proves a DEMO-kind and a LIVE-kind tenant's personal.workforce_baseline rows are read back identically — no tenant_kind branch anywhere in the query. I9 reduced 23->22 files (35->34 imports) — second genuine reduction via a real caller migration, after CC-052's Commons work. |
| `tooling.koratest-canonical-seed` | Ingestion | `scripts/koratest-canonical-seed.ts` | CANONICAL | — | B-TRUTH KoraTest Canonical Foundation (2026-09-03) | PR 1 of the B95-B/B95-C canonical migration plan (see the founder-ratified ONE_PRODUCT_CANONICAL_MIGRATION direction). Additive foundation only — creates a new canonical tenant (analytics.tenant, tenant_kind=TEST, company_name="KoraTest Srl") and its own deterministic input fixture (data/koratest/koratest_input_fixture.json, NOT under data/synthetic/**, NOT reusing lib/live/op001-synthetic-records.ts) via the same input-boundary tables and shapes app/api/admin/data-intake/accept/route.ts and app/api/admin/companies/provision/route.ts already use. Every downstream result — UEF interpretation, eligibility, KORA Index/BTI/activation/confidence, Decision Pack — is produced by invoking the exact same canonical functions a real tenant's upload uses (lib/ingestion/raw-to-uef-interpreter.ts's interpretUploadedRecord, lib/kora-engine/eligibility-gate.ts's classifyEligibilityBatch, lib/live/uef-to-scoring-records.ts's buildScoringRecordsFromApprovedUef, lib/kora-engine/run-kora-pipeline.ts's runKoraPipeline, lib/live/persistence.ts's persistKoraComputationResult, lib/live/decision-pack.ts's persistDecisionPack — all imported unchanged, never reimplemented) — no result table is ever directly inserted by this script. The one human-review step (UEF candidate approval) is stood in for by a deterministic, clearly-labeled automated-operator-approval rule identical to app/api/admin/uef/review/route.ts's own 'approve' branch — the interpreted content itself is never touched by this stand-in. tenant_kind is set exactly once (at tenant creation) and is never read by any of the six canonical functions above — confirmed by tests/unit/b-truth-koratest-canonical-foundation.test.ts's structural guard, matching the pre-existing precedent in app/api/admin/companies/provision/route.ts (the only existing tenant_kind-conditioned branch in this pipeline skips a real external side-effect, the email invite, never product/methodology logic — this script does not even create a Supabase Auth user). KoraTest Srl uses tenant_code 'KORATEST-01', deliberately not 'OP-001' — the one existing hardcoded tenant-identity special case (app/api/admin/scoring/run-approved-batch/route.ts's literal block on tenant_code === 'OP-001') does not apply to it. Idempotent: reuses an existing tenant by tenant_code, reuses an existing source_batch by its natural key (tenant_id, reporting_period, source_name), and skips UEF generation/scoring when already present — no destructive statement anywhere, no cleanup/reset helper (not needed for this PR, not added speculatively). See tests/integration/rls-17-koratest-canonical-foundation.test.ts (DB-backed, skip-safe locally, mirrors RLS-16's safety model) for the proof that the real interpreter and scoring engine actually execute and produce non-fabricated, non-uniform output (the fixture deliberately includes a KW_LIMITED and a KW_BLOCKED row, not just eligible ones), and that a second run is idempotent. This PR does NOT retire, migrate, or change the lifecycle status of TenantService, AccountProvisioningService, CompanyDataIntakeService, ReportFactoryService, or AdminPreviewService (the B95-B/B95-C demo-admin cluster) — those remain their own separately-authorized future migration slices (PRs 2-6 of the same plan). I9 unaffected: 15 files/25 imports unchanged — this script imports no data/synthetic/** path and is not itself scanned by the I9 guard (scripts/ is outside app/services/lib/components). |

---

## Status Distribution

| Status | Count |
|---|---|
| CANONICAL | 32 |
| CONSOLIDATE | 7 |
| COMPLETE | 11 |
| FROZEN | 3 |
| FUTURE_CORE | 7 |
| LEGACY | 0 |
| DEAD | 16 |
| INVESTIGATE | 2 |

---

## DEAD Components

| ID | Path | Replacement | Deletable When | Decision Ref |
|---|---|---|---|---|
| `svc.booking-request` | `services/booking-request/BookingRequestService.ts` | `svc.commons.booking` | After B-REG registry is live and a repo-wide re-grep at CC-003+ time confirms still zero references. | Master Plan §32 Safe Deletion Plan |
| `svc.budget-to-human-impact` | `services/budget-to-human-impact/BudgetToHumanImpactService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Retire Orphan Synthetic BTI Chain (2026-08-31) |
| `svc.company-data-intake` | `services/company-data-intake/CompanyDataIntakeService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH CompanyDataIntakeService Canonical Migration (2026-09-05) |
| `svc.company-intelligence` | `services/company-intelligence/CompanyIntelligenceService.ts` | — | Already deleted — this entry is the historical record of that deletion. | CC-020A Company Intelligence Capability Retirement (2026-08-31, narrowed) |
| `svc.dynamic-scoring` | `services/dynamic-scoring/DynamicScoringPreviewService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Preview Scoring Retirement (2026-09-03) |
| `svc.eligibility-gate` | `services/eligibility-gate/EligibilityGateService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Eligibility Gate Retirement (2026-09-03) |
| `svc.financial-governance` | `services/financial-governance/FinancialGovernanceService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH FinancialGovernance Retirement (2026-09-02) |
| `svc.ingestion-normalizer` | `services/ingestion-normalizer/IngestionNormalizerService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Ingestion Normalizer Retirement (2026-09-03) |
| `svc.ingestion-pipeline` | `services/ingestion-pipeline/IngestionPipelineService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Ingestion Pipeline Retirement (2026-09-03) |
| `svc.ingestion-simulator` | `services/ingestion-simulator/IngestionSimulatorService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Ingestion/UEF PR2 — IngestionSimulatorService retirement (2026-09-02) |
| `svc.lifecycle` | `services/lifecycle/LifecycleService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH Root Control Room Wave 2 (2026-08-30) |
| `svc.report-factory` | `services/report-factory/ReportFactoryService.ts` | `svc.report-generator`, `lib.decision-pack` | Already deleted — this entry is the historical record of that deletion. | B-TRUTH ReportFactoryService Canonical Decision Pack Status Migration (2026-09-06) |
| `svc.report-generator` | `services/report-generator/ReportGeneratorService.ts` | `svc.report-factory`, `lib.decision-pack` | Already deleted — this entry is the historical record of that deletion. | B-TRUTH ReportGenerator Retirement (2026-09-02) |
| `svc.tenant` | `services/tenant/TenantService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH TenantService Canonical Migration (2026-09-04) |
| `svc.uef-review` | `services/uef-review/UEFReviewService.ts` | — | Already deleted — this entry is the historical record of that deletion. | B-TRUTH UEFReview Retirement (2026-09-03) |
| `app.company-reports-board-pack` | `app/company/reports/board-pack/page.tsx` | — | After B-REG, once the 2 referencing files are repointed directly at /api/company/decision-pack and the 4 redirect-behavior tests are updated or removed. | Master Plan §32 Safe Deletion Plan |

---

## FUTURE CORE

Capabilities explicitly preserved per Master Plan §33 (Do-Not-Delete / Future Core) — never reclassify these as DEAD or LEGACY for low current usage.

- `app-surface.my-kora` (COMPLETE) — `app/my-kora/` — Preview worker workspace, demo-state session (9 routes) — self-documented in-file as PREVIEW mode.
- `app-surface.worker` (COMPLETE) — `app/worker/` — Live worker workspace, real Supabase JWT session (12 routes).
- `db.commons-booking-schema` (CANONICAL) — `supabase/migrations/025_commons_booking_contribution.sql` — commons.booking table — dual tenant_id (worker_tenant_id, post_tenant_id) cross-tenant pattern.
- `db.kora-link-schema` (FUTURE_CORE) — `supabase/migrations/034_kora_link_schema.sql` — kora_link.* schema (links, link_assignments, link_batches, link_delivery_records, link_events, link_replacements, link_activation_acknowledgements, revocations, audit_log).
- `db.worker-pib-schema` (FUTURE_CORE) — `supabase/migrations/018_worker_pib.sql` — personal.worker_pib — isolated PIB storage, SECURITY DEFINER-only access.
- `lib.kora-link` (FUTURE_CORE) — `lib/kora-link/` — Identity/token-based worker activation channel (8 files).
- `lib.types.domains.evidence` (FUTURE_CORE) — `lib/types/domains/evidence.ts` — Type scaffold for a first-class Evidence entity — types only, no persistence, explicitly for Pilot+.
- `lib.worker-identity` (CANONICAL) — `lib/worker-identity/` — Core worker identity lifecycle helpers (2 files).
- `svc.commons.booking` (CANONICAL) — `services/commons/BookingService.ts` — Live cross-tenant booking (worker_tenant_id × post_tenant_id pattern).
- `svc.dynamic-cv` (FUTURE_CORE) — `services/dynamic-cv/DynamicCVService.ts` — Worker-self-only Dynamic Impact CV.
- `svc.kora-contribution` (CANONICAL) — `services/kora-contribution/KoraContributionService.ts` — KORA Contribution computation — companion indicator, kept separate from KORA Index.
- `svc.pib-aggregation` (CANONICAL) — `services/pib-aggregation/PIBAggregationService.ts` — Stage 11 of the 14-stage algorithm: PIB = ΣIU per worker.
- `svc.worker-opportunity` (FUTURE_CORE) — `services/worker-opportunity/WorkerOpportunityService.ts` — Rule-based opportunity suggestions, no LLM.
- `svc.worker-pib` (FUTURE_CORE) — `services/worker-pib/WorkerPIBService.ts` — Worker-self PIB read/aggregate.

---

## INVESTIGATE

Components where evidence is insufficient to assign a confident status. Per Master Plan discipline: `INVESTIGATE` is preferred over an aggressive classification when in doubt.

- `lib.ingestion-cluster` — `lib/data-intake/` — BACKLOG — HUMAN TRIAGE. CC-002 traced part of this cluster: ingestion-normalizer→ingestion-pipeline is a real 2-stage chain; ingestion-simulator and company-data-intake are separate parallel entry points. The four lib/ sub-directories themselves were not individually caller-traced. Finding: MIXED.
- `svc.company-setup` — `services/company-setup/CompanySetupService.ts` — Zero real callers (only route-string literals reference it). Master Plan §32/§33 state explicitly, twice: "company-setup e report-generator restano INVESTIGATE" — this status is unchanged by the B-TRUTH Company Onboarding Canonicalization (2026-09-01), which corrected only the competingWith claim: CompanySetup and CompanyOnboarding are distinct responsibilities (pre-provisioning wizard vs. post-provisioning readiness/status), not competing implementations. See svc.company-onboarding.

---

## Target Ontology Implementation

23 target ontology objects (Master Plan §3). Status values: `EXISTS` / `PARTIAL` / `TO_BUILD` / `UNCERTAIN`.

| Object | Status | Current Representation | Paths | Implementation Block | Notes |
|---|---|---|---|---|---|
| Organization | EXISTS | analytics.tenant table (no gap) — but services/tenant/TenantService.ts is NOT a real access layer over it (100% synthetic, see svc.tenant). | `supabase/migrations/001_live_v1_foundation.sql`, `app/api/admin/tenants/route.ts`, `app/api/admin/company-console/route.ts` | — | The underlying table has no gap and is already the live source for /api/admin/tenants, /api/admin/company-console, /api/admin/tenants/[id]/promote-to-pilot, lib/decision-pack/pdf-data.ts, /api/company/workspace. TenantService.ts is listed as a primary path only historically — corrected here (B-TRUTH Tenant Identity read-path audit, 2026-08-30) since it does not query this table at all. See svc.tenant for the read-path consolidation status. |
| ProgramDefinition | TO_BUILD | Flat programs.json (data/synthetic/programs.json), company_id-scoped, no owner-type split (company/partner/territory) — a data-shape analog only, not code implementing this object. | — | N1 | Zero code hits for the term "ProgramDefinition" anywhere in the repo (confirmed by exhaustive grep, CC-001R + CC-001 both independently). |
| ProgramParticipation | TO_BUILD | Not separated from ProgramDefinition. | — | N1 | Same gap as ProgramDefinition. |
| InvestmentCase | TO_BUILD | No representation found. | — | N2 | Zero code hits. |
| EvidencePlan | TO_BUILD | No representation found. | — | — | Zero code hits, distinct from the Evidence type scaffold (see Evidence entry below). |
| DecisionRule | TO_BUILD | No representation found — distinct from the runtime eligibility-gate.ts classifier. | — | — | Zero code hits. |
| Delivery / Opportunity | PARTIAL | Fragmented across 3 independent "opportunity" surfaces plus the Commons cross-tenant booking pattern. | `services/worker-opportunity/`, `app/worker/opportunities/`, `app/company/opportunities/`, `app/my-kora/opportunities/`, `supabase/migrations/025_commons_booking_contribution.sql` | — | Not unified under one Delivery object tied to a ProgramDefinition. |
| Observation | TO_BUILD | Closest analog is the UEF record (services/uef-review/) — not formally typed as an Observation with an eligible/exposed/aware/activated state chain, so not counted as implementing code for this object. | — | — | No append-only eligible/exposed/aware/activated event table found in any of the 48 migrations. |
| Measurement | PARTIAL | IU, KORA Index, Confidence, Contribution all exist as computed outputs, but not unified under one lineage_id / snapshot model. | `lib/kora-engine/` | B-LIN | No kora.calculation_lineage table found. |
| InvestmentReview | TO_BUILD | No representation found. | — | N7 | Zero code hits. |
| DecisionEvent | TO_BUILD | No append-only, trigger-enforced table or type found anywhere. | — | N8 | Confirms I13 "assente" — zero CREATE TRIGGER for any decision/event table across all 48 migrations. |
| SubsequentObservation | TO_BUILD | No representation — Master Plan itself notes this is expected pre-second-cycle (time-limited, not code-limited). | — | — | Master Plan: "si popola al secondo ciclo reale". |
| Worker | EXISTS | personal.worker_identity + lib/worker-identity/*. | `supabase/migrations/007_worker_provisioning.sql`, `supabase/migrations/048_worker_identity_lifecycle_protection.sql`, `lib/worker-identity/` | — | Mature, actively hardened — matches current branch HEAD. |
| PIB | EXISTS | personal.worker_pib, SECURITY DEFINER-only aggregation. | `supabase/migrations/018_worker_pib.sql`, `supabase/migrations/027_worker_individual_rls_refactor.sql`, `services/worker-pib/`, `services/pib-aggregation/` | — | Isolation independently confirmed — zero application-role policies (I3). |
| Partner | EXISTS | network.partner_identity / partner_profile. | `supabase/migrations/010_partner_profile.sql`, `supabase/migrations/012_partner_identity.sql` | — | No gap found. |
| Advisor | PARTIAL | Service exists, demo-only surface (app/demo/advisor) — no live /advisor workspace route despite CLAUDE.md documenting one. | `services/advisor-evidence-review/` | — | Gap is vs CLAUDE.md's own documented app structure, not vs the Master Plan. |
| Territory / Local Entity | TO_BUILD | No representation, typed or untyped: data/synthetic/collective-initiatives.json (formerly the only file with an untyped territory string field) was deleted by the B-TRUTH Contribution protected port (2026-09-01). commons.contribution_event / commons.post (migration 025) carry no territory column. | — | — | Master Plan §1 [VERIFIED]: "territorio ... non ancora tipizzate" — still true, and now without even the informal synthetic representation. Re-typing (if/when scoped) starts from zero, not from a partial synthetic form. |
| Evidence | PARTIAL | Fields on UEF/ImpactUnit records; a types-only scaffold for a first-class entity exists but is not implemented. | `lib/types/domains/evidence.ts` | Gate 2 (per the scaffold's own header) | Verified by direct read (CC-002): "No persistence in KORA Foundation Light: types only." |
| Benchmark Cohort / Memory | TO_BUILD | Only as disclaimed narrative copy / demo mockup (app/demo/benchmarks) — a UI placeholder, not code implementing this object. | — | N9 | Matches Master Plan's own "Stage 0" expectation until ≥10 companies. |
| MethodologySnapshot | EXISTS | B-SNAP (CC-015, D-F Option C): analytics.methodology_snapshot — one immutable row per calculation, all 9 Master Plan §11 fields (methodology_version, taxonomy_version, need_taxonomy_version, bc_calibration_version, contribution_config_version, factor_statuses, pipeline_version, config_hash, calculation_timestamp), plus provenance (AS_ORIGINALLY_CALCULATED / RESTATED_UNDER_METHODOLOGY). Referenced via nullable methodology_snapshot_id FK from activation_result, confidence_result, kora_index_result, bti_result (closes the prior BTI provenance gap), and impact_unit — historical rows keep NULL, never rewritten. Single construction authority: getMethodologySnapshot() in lib/methodology-config/v0.1.ts. need_taxonomy_version is `text NULL` (Needs domain not built) — the Master Plan's own nullable-for-"not yet applicable" convention (matching NeedObservation.related_program_definition_id / ProgramBrief.resulting_program_definition_id), not a status object inferred from the unrelated §23 factor-status vocabulary. Table has a BEFORE UPDATE OR DELETE trigger rejecting mutation for every role including service_role (true DB-level immutability, not just RLS/application discipline). | `supabase/migrations/049_methodology_snapshot.sql`, `lib/methodology-config/v0.1.ts`, `lib/live/persistence.ts` | B-SNAP | The pre-existing "quattro nomi" (KORA Methodology v0.1 / KORA-METHOD-v1.0 / v0.1.ts / kora_index_v3) remain untouched as historical/technical-only references — D-F ratified KORA Index v1.0 as the product label (getProductVersion(), same value as legacy getMethodologyVersion()) and "1.0" as the separate canonical methodology_version (getCanonicalMethodologyVersion()) for all NEW calculations; legacy methodology_version_id/methodology_version columns keep writing the product label unchanged, explicitly documented as legacy compatibility metadata, not the authority. One live bug fixed as part of this block: lib/kora-engine/run-kora-pipeline.ts's zero-data branch previously hardcoded methodologyVersion: 'KORA-METHOD-v1.0' instead of calling getMethodologyVersion() like the normal path — now aligned. config_hash covers macroblock/BC/NM/shrinkage/PIB/calibration config only — version-label fields (taxonomy_version, bc_calibration_version, contribution_config_version) are deliberately excluded from the hash but remain independently distinguishable as their own persisted snapshot fields (proven by test). |
| Data Lineage | TO_BUILD | No canonical lineage reconstruction path found. | — | B-LIN | Confirms I12 "assente" — zero lineage_id hits repo-wide (2 independent exhaustive greps). |
| KORA Link | EXISTS | Full schema, RLS, RPC — flag-disabled (KORA_LINK_ENABLED=false) but functionally complete. | `lib/kora-link/`, `supabase/migrations/034_kora_link_schema.sql` | — | Master Plan §33 [VERIFIED]: "gap DG-07 chiusi". |
| KORA Contribution | EXISTS | Service (single computation authority, DB-backed as of the B-TRUTH port 2026-09-01) + doctrine constants file, correctly kept separate from KORA Index. | `services/kora-contribution/KoraContributionService.ts`, `lib/kora-contribution/contribution-methodology.ts` | — | B-TRUTH Contribution audit (2026-09-01) corrected the CC-002 "genuine fragmentation" finding: lib/kora-contribution/contribution-methodology.ts is pure doctrine/policy constants, not a second computation implementation — the two files are non-competing (one computes, one documents policy). See lib.kora-contribution / svc.kora-contribution above for the full record. |
